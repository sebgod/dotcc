# ZIG-SUPPORT.md

What dotcc's **Zig front-end** supports today. The Zig axis is the second
implementer of the `IFrontend` seam (the C front-end is the first): a `.zig`
input is lexed + parsed by the LALR(1) grammar in [`DotCC.Lib/zig.lalr.yaml`](../DotCC.Lib/zig.lalr.yaml)
(→ generated `DotCC.Zig`), lowered to the neutral typed IR by
[`ZigLowering`](../DotCC.Lib/Frontends/ZigLowering.cs), and emitted by the **same**
C# backend + shell as C. So the lowered surface inherits dotcc's whole runtime
for free — including the libc routing that makes `printf` print.

This is an honest **subset dialect**, not full Zig: the grammar parses a
C-shaped value/type core, and lowering grows behind it deliberately ("fail
loudly, grow on purpose" — anything unlowered throws `IrUnsupportedException`
rather than miscompiling). Value-`comptime` IS supported (Milestone T), and the
comptime-**type** arc has begun: type-as-value aliases (`const T = i32;`, with
`*T`/`?T`/`[]T` composing over the alias) and `@TypeOf` ship as **wall-plan W1**,
`std.ArrayList(T)` as W0, in-function `const P = struct { … };` decls as W2,
**generic functions via `comptime` VALUE params** (`fn addN(comptime N: i32, …)` —
call-site monomorphization, one specialized body per resolved value) as **wall-plan
W3a**, **generic functions via `comptime` TYPE params** (`fn maxOf(comptime T: type,
a: T, b: T) T` — a per-instantiation signature keyed by the resolved type, `maxOf__i32`
/ `maxOf__f64`) as **wall-plan W3b**, and **type-RETURNING functions** (`fn Pair(comptime
T: type) type { return struct { a: T, b: T }; }` — a comptime type constructor that reifies
a fresh struct per resolved type argument, `Pair__i32`) as **wall-plan W4**, and **generic
functions via `anytype` params** (`fn add(a: anytype, b: anytype) @TypeOf(a)` — the parameter
type is INFERRED from the argument and keys a specialization, `add__i32_i32` / `add__f64_f64`)
as **wall-plan W5**. That completes the **generative core** of the monomorphization arc
(the wall-breaking plan, `fable-wall.md`; reasoning updated below), and **`std.debug.print`**
(`std.debug.print("{d} {s}\n", .{n, s})` — the comptime format parsed at lowering time, its
`{…}` placeholders paired positionally with the tuple and lowered to a stderr `fprintf`) lands
as **wall-plan W6** — so the **entire planned arc W0–W6 is now complete**. `std` is **not** modeled in general —
only a curated set of paths resolves: the
allocator paths (`std.mem.Allocator`, `std.heap.page_allocator`/`c_allocator`/`FixedBufferAllocator`/`ArenaAllocator`;
see the Allocators section), a few `std.mem` slice helpers (`eql`, `copyForwards`,
`span`, `zeroes`), and **`std.ArrayList(T)`** (wall-plan W0, the modern unmanaged
API — see the Types table); everything else errors clearly. Legend: ✅
supported (parses **and** lowers + runs) · 🚧 parses but does not lower yet (loud
error at the use site) · 🚫 not supported.

## Design intent — C interop without `@cImport`

Modern Zig **removed `@cImport`** (deprecated 0.16, gone in the 0.17-dev pin);
standalone-file C translation no longer exists (it's a build-system
`translate-c` step now). So dotcc's Zig C/libc interop uses the modern,
standalone-valid path: an **`extern fn` prototype + link libc (`-lc`)**. dotcc
marks such a prototype `FromSystemHeader` and renders the call **by its bare
name**, which routes it to dotcc's own `Libc` runtime — exactly how a C
program's libc call is handled. No `@cImport`, no header harvest.

## Declarations

| Feature | Status | Notes |
|---|---|---|
| `fn name(params) Ret { … }` | ✅ | top-level function definition |
| `pub fn main() void` | ✅ | void-returning main — shell calls it for effect, returns 0 |
| `pub fn main() u8` | ✅ | the `u8` return is the process exit code |
| `pub fn …` | ✅ | `pub` unwrapped (visibility is a no-op in our single-module emit) |
| `export fn …` / `pub export fn …` | ✅ | C-ABI external-linkage modifier (Milestone R, part 4) — unwrapped to the inner function, which lowers like an ordinary one (every non-static function is already export-eligible under `-shared`, so the modifier is a no-op in a console program) |
| `export const`/`var`, `pub const`/`var`, `pub export const`/`var` (data) | ✅ | exported / public DATA globals — `Unwrap` peels the modifier to the inner `const`/`var`, which lowers as an ordinary global (the same path plain top-level `const`/`var` take). The modifier is a no-op in a console program. **Cut:** actually EXPORTING the data symbol under `-shared` (NativeAOT data-symbol export) — the modifier is dropped, not surfaced across the C ABI |
| Parameters `name: Type` | ✅ | names + types ride into the C# signature; faithful signedness |
| Forward references | ✅ | two-pass lowering (Zig has no prototypes) — a call may precede the callee |
| `extern fn f(p: T) Ret;` | ✅ | libc/FFI prototype (no body); routed by bare name, linked with `-lc` |
| `extern fn f(p: T, ...) Ret;` | ✅ | **variadic** extern (e.g. `printf`); `...` must be last + extern-only. A variadic argument must have a fixed-size type — `printf("%d", @as(c_int, 42))`, never a bare `printf("%d", 42)` (rejected, matching real zig — see below) |
| local `const`/`var` (typed or inferred) | ✅ | inside a function body |
| `fn f() !T` (inferred-error return) | ✅ | the `!T` returns an error union (`ErrUnion<T>`); see `try`/`catch`/`error.X` below. V1 erases the error SET |
| `pub fn main() !void` / `!u8` (error-union main) | ✅ | main may return an error union (Milestone N, part 4) — emitted as `ErrUnion<…>`; the process entry maps the result like real zig: an error → exit 1 (the flat error code reported to stderr, stdout stays clean), success → exit 0 (a `!void` payload) or the integer payload value (`!u8`). `try` inside main propagates to that boundary |
| top-level / global `const`/`var` | ✅ | a runtime global → a `public static` field of `DotCcGlobals` (the same path the C front-end's file-scope variables take), surfaced by bare name (a function body reads/writes it unqualified). Typed keeps its annotation; untyped infers from the initializer (`const N = 5;` → `int`). Initializers are lowered in source order, so a global may reference an EARLIER global by bare name. `const`-ness isn't enforced (both lower to a mutable field — observably identical for a correct Zig program). An aggregate (struct), `[N]T` array, and `undefined` global are supported (Milestone K — an array routes through a pinned, program-lifetime backing store). A **fn-pointer global** works — typed (`const h: *const fn (i32) i32 = &fn;`) or INFERRED (`const alias = &fn;`, where `&fn` is a callable `CType.Func` value) — and may FORWARD-reference a function declared later (functions are registered before globals). **Cut:** a forward reference to a later global CONST VALUE (`const a = b + 1; const b = 41;`) — order-independent comptime-const resolution isn't modeled, and C# static-field initializers run in declaration order; declare in dependency order |
| `threadlocal var x: T = 0;` | ✅ | thread storage duration on a container-level `var` → `[ThreadStatic]` on the emitted `DotCcGlobals` field (the C `_Thread_local` twofer — same `Symbol.IsThreadLocal` marker, same backend emit; completion-milestone part 2). Every thread gets its own zero-initialized slot. **V1 constraints (loud errors, matching what .NET can honor):** zero initializer only (a `[ThreadStatic]` initializer runs on the first thread only), scalar only, typed form only, and container level only — a function-local `threadlocal` is rejected exactly as real zig rejects it. Oracle `threadlocal_var` (=42 vs real zig 0.17) |
| `callconv(.c)` / `align(N)` / `linksection(".s")` | ✅ | declaration modifiers (Milestone R, part 5) — `callconv(Expr)` between a fn's `)` and its return type; `align(Expr)` / `linksection(Expr)` (in that order) between a typed `const`/`var`'s Type and `=`. All ACCEPTED + IGNORED on a fn DEFINITION (pure no-ops on the managed target — a C# method/field has no controllable calling convention, alignment, or link section); their value is round-trippability. **On a fn-POINTER TYPE, `callconv(.c)`/`(.C)` is HONORED** (road-to-zig-std S9): it is a genuine call-ABI annotation, so it marks the `CType.Func` native and the backend renders `delegate* unmanaged[Cdecl]<…>` instead of the managed `delegate*<…>` — std's C-ABI fn-ptr typedefs (`*const fn (?*anyopaque) callconv(.c) void`, e.g. `std/c/darwin/dispatch.zig`) parse and lower faithfully. Other conventions on a fn-ptr type stay managed. **Cuts:** `align`/`linksection` on a pointer type / struct field / function; `callconv` on an `extern` prototype; a container-`const` with modifiers |
| `inline fn` | 🚫 | inlining modifier not modeled (`export`/`callconv`/`align`/`linksection` ARE — above) |
| `extern "c" fn …;` | ✅ | the optional library/calling-convention string after `extern` (Milestone R, part 4); accepted + lowered like a plain `extern fn` (routed to dotcc's libc-shaped runtime by bare name) |

## Types

| Feature | Status | Notes |
|---|---|---|
| `i8 i16 i32 i64`, `u8 u16 u32 u64` | ✅ | faithful signedness (i8→`sbyte`, u8→`byte`, …) |
| `i128`/`u128` | ✅ | Milestone ß — → C# `System.Int128`/`System.UInt128` (BCL primitives; `* / << >>` etc. free). A literal past `ulong` materializes via `Parse(...)` (no C# 128-bit literal suffix). Wrapping `+%` works; **saturating `+\|`/`-\|`/`*\|` is a cut** (the exact-128-bit clamp accumulator would itself overflow). The wat target has no 128-bit type (throws). |
| `usize`/`isize` | ✅ | LP64 pointer-width (`ulong`/`long`) |
| `f32`/`f64` | ✅ | → C# `float`/`double` |
| `bool`, `void` | ✅ | |
| `c_char c_short c_ushort c_int c_uint c_long c_ulong c_longlong c_ulonglong` | ✅ | C-ABI types for `extern fn`, LP64-shaped |
| `*T`, `*const T` | ✅ | pointer (pointee `const` rides as a type qualifier) |
| `*anyopaque`, `?*anyopaque` | ✅ | Milestone W, part 1a — `anyopaque` is Zig's opaque type, used behind a pointer as a type-erased context (the C `void*`-callback idiom). → C's `void`, so `*anyopaque` → `void*` and `?*anyopaque` → a nullable `void*` (the pointer niche). A typed `*T` coerces in implicitly (as C does); `@ptrCast(@alignCast(ctx))` recovers the typed pointer |
| `fn (params) RetType` (function pointer) | ✅ | Milestone W, part 1a — a function-pointer type → a **managed** C# `delegate*<P…, Ret>` (the shape the Zig allocator vtable uses; NOT the `unmanaged[Cdecl]` form a dlsym'd native fn-ptr takes). `*const fn (…) R` / `?*const fn (…) R` compose through the pointer / optional prefixes (a pointer-to-function collapses to the bare function-pointer, the C-frontend convention). A bare function name decays to its address (`&fn`); a fn-pointer VALUE is called indirectly (`op(args)`), and so is a fn-pointer FIELD called like a method (`o.f(x)`, `w.vtable.drain(w, …)`: std.Io.Writer's dispatch), each argument result-located against the parameter (oracle `fn_pointer_field_call`). Params may be UNNAMED (`fn (i32, i32) i32`, the common Zig form) or named (`IDENT : Type`, the name ignored). A `!T` error-union return works too (`fn (i32) E!i32` → the Func's Return is a `CType.ErrorUnion` → `delegate*<int, ErrUnion<int>>`). **Cut:** a `callconv` on a fn-pointer type |
| `[*c]T`, `[*c]const T` | ✅ | C pointer (== C's `T*` / `const T*`) — printf's `[*c]const u8` format |
| `[*]T`, `[*]const T` (many-item) | ✅ | many-item pointer (Milestone O, part 2) → a bare `T*`, like `[*c]`; indexing `p[i]` + closed-slicing `p[lo..hi]` work, `.len` is unavailable (no known length). A slice's `.ptr` is a `[*]T`. The type-level distinction from `[*c]` (non-null, no C-conversion) is not modeled — both are `T*` `&arr` (a local or field array) coerces to `[*]T` as its element pointer, zig's `*[N]T` to `[*]T` (oracle `array_address_to_many_pointer`; unit `ZigArrayDecayTests`). |
| `[*:0]T`, `[:0]T` (sentinel, +`const`) | ✅ | sentinel-terminated types (Milestone O, part 3 — the C-string shape; V1 sentinel = 0). `[*:0]T` is a NUL-terminated many-item pointer (C's `char*`) → a bare `T*`, like `[*]`; `[:0]T` is a NUL-terminated slice → `Slice<T>`, like `[]T` (`.len` excludes the sentinel). A string literal coerces to `[:0]const u8` (`.len` = char count) and its `.ptr` is a `[*:0]const u8`. The sentinel is a type-level annotation, not separately enforced — dotcc's string literals are already NUL-terminated, so a manual `while (p[n] != 0)` scan works. **Cut:** the auto-scan `p[0..]` on a sentinel pointer (use a manual scan); sentinels other than `0` |
| `?T` optional | ✅ | `?*T` → bare nullable `T*` (niche); `?T` over a value → C# `Nullable<T>`. `null`/`.?`/`orelse` below |
| `[]T` / `[]const T` slice | ✅ | → the runtime fat pointer `Slice<T>` / `ConstSlice<T>` (`{ T* Ptr; ulong Len; }`, the C++ `std::span` shape — **not** C#'s ref-struct `Span<T>`, so a slice can be a struct field and cross the ABI; `AsSpan()` bridges to the BCL). `.len`/`.ptr`, `s[i]`, `s[lo..hi]`, array/string coercion, and `for` over it all work (rows below). Milestone O completed the family: `[*]T`-backed slices (part 2), sentinel `[:0]T` (part 3), open-ended `s[lo..]` (part 1), by-ref `\|*x\|` (Milestone M), and the non-escaping-stack-slice → `stackalloc` peephole (part 5, below) |
| `[N]T` array (local) | ✅ | `var b: [N]T = …;` → a stackalloc'd C array (zero heap); `b[i]` indexes, `b[lo..hi]` yields a **stack-backed slice**. Size `N` must be an integer literal. **Init:** `undefined` (zeroed) OR an array literal (Milestone K) — `.{…}` at a `[N]T` sink, typed `[N]T{…}` (explicit length), or `[_]T{…}` (length inferred from the element count). An empty literal is rejected (use `undefined`). **Array-by-value return** (`fn() [N]T`) is supported: arrays lower to `T*`, so a naive `return t;` of a stackalloc local would dangle — dotcc copies the N elements into a heap-owned buffer (`ZigAlloc.CopyArrayResult`) so the value outlives the call (Zig arrays are value types). V1: that buffer is leaked (sound values, unfreed; a caller-allocated result pointer would avoid it) |
| `[N:s]T` sentinel array (local **and global**) | ✅ | sentinel-terminated array (Milestone O, part 4; non-zero sentinel in Milestone Z). Reserves **N+1** elements of storage: the trailing slot (index `N`) holds the sentinel `s`, the logical length stays `N` — so a `[N:0]u8` literal is a valid NUL-terminated C string with no hand-written terminator, and `b[N]` reads the sentinel back. A LOCAL lowers to the same `stackalloc` an `[N]T` local uses, grown by one slot; a GLOBAL uses the pinned, program-lifetime backing store grown by one slot (same shape a plain `[N]T` global takes). A **zero** sentinel rides C#'s zero-fill (`undefined` reserves `N+1` zeroed; a literal lays down its `N` elements + a trailing `0`); a **non-zero** sentinel is materialized — appended to a literal, or written for `undefined` (a local writes `b[N] = s;`; a global lays down an explicit `[0×N, s]` element list, since the pinned static store can't post-write). The symbol's type is the N-element `CType.Array`, so indexing/slicing behave like `[N]T`. Note an `undefined` array's sentinel is dotcc-defined (real zig leaves it undefined) |
| tuple `struct { T1, T2, … }` | ✅ | an anonymous **positional** struct → C# `System.ValueTuple<…>` (Milestone G — see the **Tuples** section). Valid as a return / param / var type; a positional literal `.{a, b}` constructs it, `t[N]` (literal `N`) reads `.ItemN+1`, and `const a, const b = e` destructures. **Any arity:** empty `.{}` → the non-generic `System.ValueTuple`; arity > 7 nests via C#'s 8th `TRest` field (an index ≥ 7 reads through `.Rest`). **Runtime subset only:** comptime / type-valued fields and a mixed positional+named literal are rejected |
| `std.mem.Allocator` | ✅ | the allocator fat pointer `{ ptr, vtable }` → the runtime `Allocator` value type (see the **Allocators** section). `std.heap.FixedBufferAllocator` / `std.heap.ArenaAllocator` are the concrete second / third allocators. A **user-constructed** allocator works too (Milestone W, part 1b): `std.mem.Allocator.VTable` → the runtime 4-fn `AllocatorVTable` and `std.mem.Alignment` → the `Alignment` value type, so a hand-written `std.mem.Allocator{ .ptr, .vtable }` + `VTable{…}` literal lowers and dispatches indirectly. Any OTHER `std.*` type errors clearly |
| `std.mem.eql` / `copyForwards` / `span` / `zeroes` | ✅ | curated `std.mem` helpers → the runtime `ZigMem` class (`DotCC.Libc/ZigMem.cs`, auto-spliced like `ZigAlloc`). `eql(T,a,b)` = equal length AND identical contents (byte-wise, = element equality for the scalar element types it's used with); `copyForwards(T,dest,src)` = copy `src.len` elements front-to-back; `span(ptr)` = a NUL-sentinel `[*:0]T` pointer → the `[]const T` slice before the sentinel (byte-wise scan); `zeroes(T)` = an all-zero value → C#'s `default(T)`. Slice args coerce a `&array` (`*[N]T`). Any OTHER `std.mem.*` member errors clearly — `std` is a curated-paths resolver, not a general model. **Cuts:** `span` yields a `[]const T` even from a mutable `[*:0]T`, and assumes sentinel 0 (dotcc erases the sentinel); `zeroes` of an ARRAY/slice type (arrays lower to a pointer, so `default` would be null) |
| std.fmt.Placeholder.parse's shapes (road-to-zig-std, the bufPrint path) | ✅ | `.{ .none = {} }` builds a void variant; `return .{ .named = x };` in a `!Spec` function takes the payload type; a value `if (opt) |c| label: {…} else null` takes its result type (an enum literal at a `?Mode` sink included); an untyped top-level `const default_mode = .right;` is typed by where it is read; `x catch unreachable` in value position renders a C# `throw` expression; a static call through a type another module declares (`std.fmt.Placeholder.parse(…)`, also from inside an imported module) lowers in that module; `s[a..b].*` bound to a `const` stands for the slice, and `&` of it coerces back; a `@compileError` whose message is built from a runtime value (`catch \|err\| @compileError(@errorName(err))`) is the `unreachable` trap, since zig raises it only on a comptime path that takes it. Multi-file oracle `fmt_parse_shapes`; unit `ZigFmtParseShapesTests` |
| std's string helpers' shapes (road-to-zig-std, tasks #52 to #55) | ✅ | what `std.mem.splitScalar` / `tokenizeScalar`, `indexOf`, `startsWith` / `endsWith`, `std.fmt.bufPrint` with `{s}` / `{x}` / `{c}` and `ArrayList.appendSlice` needed from real std: a struct field whose TYPE is a comptime `switch` (SplitIterator's `delimiter: switch (delimiter_type) {…}`, over an enum-literal type argument); a value `if (switch (…) {…}) \|i\| … else …` (SplitIterator.next); a local struct chosen by an if-capture over a comptime optional (`const Scan = if (suggestVectorLength(u8)) \|n\| struct {…} else struct {…};`, the capture a comptime value inside the chosen body); an `and` / `or` whose comptime-known LEFT operand settles an `if` in an unrolled `inline for` or a generic instance (the dead arm may name a type that cannot exist); `@bitCast(s[i..][0..4].*)` read through the slice's data pointer; `std.mem.sliceAsBytes`; a bare `asBytes` / `sliceAsBytes` inside std's own mem.zig; a file's `const mem = @This();` self alias; `&.{ 1, 2 }` at a `[]const T` parameter of the curated list. A tuple's pointer-like element (a string literal, a pointer, a function) rides as `nint` in the C# `ValueTuple` (C# forbids a pointer type argument), and **a string literal element's type drops its NUL**, so `{s}` of `.{ "k", i }` prints one byte, not two. Unit `ZigStringShapesTests`; oracles `string_shapes`, `list_slice_as_bytes`; real-std differential `Dotcc_matches_zig_std_string_pipeline_from_source` (55 == zig). |
| std's small helpers' shapes (road-to-zig-std, tasks #57 to #59) | ✅ | `@TypeOf(a, b, c)` is the operands' peer type (a comptime_int yields to a fixed width; std.math.clamp); an empty array literal `[_]T{}` / `&[0]u8{}` is a zero-length array (at a non-zero extent it is still rejected); `allocator.dupe(T, s)` on the curated allocator; mem.zig's own `Allocator` (`@import("mem/Allocator.zig")`) is the curated `std.mem.Allocator`; an array value at a `![]T` return is the slice; a side-effect-free `stackalloc` hoists out of a ternary arm. For std.fmt.parse_float: a `switch (T)` / `T == f16` over zig floats dotcc does not lower (`f16`, `f80`, `f128`, `c_longdouble`) passes over them, a local `const M = switch (T) { … => u64, … }` is a type alias, a named field value may be a value `if` / `switch` (grammar), `&.{ i.base + 'a' }` at a `comptime cs: []const u8` parameter is a comptime string, `inline for` over a comptime string unrolls per byte, and a value `if (c) return x else y` hoists the return (grammar). Unit `ZigStdHelperShapesTests`; oracle `std_helper_shapes`; real-std differential `Dotcc_matches_zig_std_small_helpers_from_source` (230 == zig). **Not yet:** std.fmt.parseFloat end to end (task #59: `MantissaT` passed as a type argument next), std.fmt.allocPrint (#60) |
| a root file's self alias `const root = @This();` (road-to-zig-std, task #56) | ✅ | `root.helper()`, `root.Point` (a type, and a static call `root.Point.sum(p)`), `root.limit`, and `root.helper` / `&root.helper` as a function value. A std file's self alias (`const mem = @This();`) resolves through its own module; the root has none, so a root namespace file (no top-level fields) that names itself gets a synthetic module over its own lowering, and the binding itself is comptime-only (no global). A module's top-level FUNCTION is also a value through any module path now (`const f = util.helper;`, `&util.helper`); a generic one is rejected by name (it has no single address). Unit `ZigSelfAliasTests`; oracle `root_self_alias` |
| `x orelse label: { …; break :label v; }` | ✅ | a labeled value block as the `orelse` / `catch` fallback (std.fmt.ArgState.nextArg): it runs only on the null / error path and fills the result there, the payload fills it otherwise. Oracle `comptime_struct_var`; unit `ZigComptimeEngineTests` |
| `std.debug.print(fmt, .{args})` (curated — wall-plan W6) | ✅ | the biggest remaining `std` idiom. Like real Zig it writes to **STDERR** (not stdout). The comptime format string is parsed AT LOWERING TIME (no reflection — the AOT rule) and its `{…}` placeholders paired POSITIONALLY with the argument tuple's elements, each translated to the equivalent C `printf` conversion; the whole thing lowers to `fprintf(stderr, "…").Arg(…).Done()` over dotcc's existing printf-builder. Because the runtime builder keys off the actual `.Arg(…)` overload, no length modifier is needed — `{d}`/`{}` → `%d` for **every** integer width (a `{d}` on an `i64` prints the full 64-bit value), `{x}`/`{X}` → `%x`/`%X`, `{c}` → `%c`, `{s}` → `%s`. `{{`/`}}` fold to literal braces; a literal `%` is doubled to `%%`. Curated set: `{}`, `{d}`, `{s}`, `{c}`, `{x}`, `{X}` on integer / string-pointer arguments. Oracle `debug-print` (asserts against real zig's stderr); example `examples/zig-debug-print/`; unit `ZigDebugPrintTests`. **Works under both backends** — `--target=wat` lowers the same `fprintf(stderr, …)` by mapping `stderr` to WASI fd 2 (the void-typed call leaves nothing on the stack); pinned by `WatBackendTests.zig_std_debug_print_lowers_to_a_void_fprintf_on_stderr` and verified running in-browser by the web sandbox. **Cuts (loud):** a float / bool / slice / struct argument (Zig's decimal-float / `true`-`false` / `.len`-bytes formatting can't be matched byte-for-byte by a C conversion); a width / alignment / named specifier (`{d:0>5}`, `{[name]}`) or `{any}`; a non-literal format string; a non-`print` `std.debug` member |
| `*[N]T` → `[]T` coercion (`&array` to a slice) | ✅ | Zig's pointer-to-array → slice coercion: `&arr` (`*[N]T`) passed where a `[]T`/`[]const T` is expected promotes to a fat pointer over the array's `N` elements. `CoerceToSlice` strips the address-of; the array already lowers to its element pointer. (Previously only a bare array / string literal coerced.) |
| `std.ArrayList(T)` (curated — wall-plan W0) | ✅ | The modern **UNMANAGED** array list (zig 0.15+ re-pointed `std.ArrayList` at it; the managed `init(alloc)` API no longer exists in the pinned zig and is rejected BY NAME with the migration hint). → the runtime `ZigList<T>` value type (`{ ptr, len, capacity }`, `DotCC.Libc/ZigList.cs`). The type parses in TYPE position through the ordinary Suffix chain (`Type → ErrUnion → Suffix → callArgs` — **no grammar change**) and resolves to `CType.ZigList(Element)`. Curated surface: `.empty` (→ `default` — exactly zig's decl literal), `append`/`appendSlice` (→ `!void`, `error.OutOfMemory` on exhaustion, so `try` composes; the allocator is an explicit per-call argument), `pop` (→ `?T`), `deinit`, `clearRetainingCapacity`, `items` (→ a mutable `[]T` — subscript / `.len` / `for (list.items) \|x\|` ride the slice lowering), `capacity`. Mutating members lower to INSTANCE methods on the runtime struct via the `ZigListCall` IR node (a C# struct method on an lvalue receiver mutates in place — zig's `*Self` shape). **Fidelity note:** `capacity`'s VALUE is the growth policy's detail (dotcc doubles from 8; real zig's curve is super-linear + version-dependent), so it isn't an oracle-comparable observable. Unmodeled members error naming the curated set. Oracle `arraylist` (byte-identical vs real zig 0.17); example `examples/zig-arraylist/`; unit `ZigArrayListTests` + `ZigListRuntimeTests`. **Cuts:** a pointer-to-list receiver (`(*std.ArrayList(T)).append` auto-deref), `insert`/`orderedRemove`/`swapRemove`/`getLast`/`toOwnedSlice` etc. (loud errors, demand-driven) |
| stack-slice peephole | ✅ | non-escaping stack-slice promotion (Milestone O, part 5 — the Zig analogue of the C `malloc`→`stackalloc` peephole). `const s = try a.alloc(u8, N); …s[i]/s.len…; a.free(s);` where the allocator is the **devirtualized C-heap default** (`page_allocator`/`c_allocator` → `Libc.malloc`), `N` is a compile-time constant ≤ 1024, the element is 1-byte, the decl isn't in a loop, and `s` never escapes (only `s[i]`/`s.len`/`a.free(s)`) → demoted to `byte* __slicebufK = stackalloc byte[N]; Slice<byte> s = new Slice<byte>(__slicebufK, N);`, the `free` dropped. The slice keeps its `Slice<T>` type (no `s[i]`/`s.len` rewrite). Conservative: any unmodeled use, a return / store / `s.ptr` exposure, a non-constant size, no `free`, or an INDIRECT/FBA allocator (`Receiver != null`) keeps it on the heap. **Cuts:** the `catch` form (only `try`), `defer a.free(s)` (only an explicit free), wider-than-byte elements |
| `const P = struct { fields…, methods… };` (+ `pub`) | ✅ | container decl (top-level) → a real C# `unsafe struct` via the SHARED aggregate machinery the C frontend uses. Fields **and** methods (below) in the body; tagged unions are a later D slice. Empty `struct {}` allowed. A `pub`-wrapped container (`pub const P = struct/enum/union {…}`) works too — the forms are grouped under a `ContainerDecl` nonterminal so one `Unwrap` peel covers them all (the modifier is a no-op in single-module emit). An **in-FUNCTION** struct decl is now supported too (next row, wall-plan W2). |
| in-function `const P = struct { … };` (wall-plan W2) | ✅ | a struct declared inside a fn body. The grammar admits a `ContainerDecl` in statement position (conflict-free with the Decl-level form — after `const IDENT =`, the `struct` token selects it). Lowering registers the layout into the module type section ON THE FLY during body lowering (top-level containers pre-register in pass 0; a local one is first seen mid-pass-2) under a **function-mangled** IR name (`<fn>__<P>`) so two bodies' like-named-but-differently-shaped locals never collide, and maps the plain name → that type (shadow-saved, restored at body exit) so it does NOT leak into a sibling function. Emits no runtime decl (a type decl is not code). Self-referential fields (`next: *P`) resolve. Oracle `in-fn-struct`; example `examples/zig-in-fn-struct/`; unit `ZigInFnContainerTests`. **Methods and `const` members** (task #48, std.sort's local `Context`): declared under the function-mangled container (`main__P_get`), their bodies deferred and drained like a reified generic's, carrying the enclosing generic INSTANCE's comptime seeds, a `comptime` function parameter included (`lessThanFn`), so one local struct per instance reads that instance's `T` and comparator (unit `ZigSortTests`, oracle `local_struct_methods`). zig rejects a local struct naming itself inside its own initializer (`self: P`); `@This()` is the spelling. **Cuts (loud):** a local **enum/union** (struct only), and a **nested container** inside a local struct. The reify-a-struct primitive W4's type-returning functions will build on |
| nested container member `pub const Mode = enum {…};` (road-to-zig-std G3) | ✅ | a container declared INSIDE a struct body — any kind (struct / extern / packed struct, enum, union), with its own fields, methods, consts and further nesting. Pass 0 registers each nested container as an ORDINARY container under a parent-mangled name (`Number__Mode`, `A__B__C`), spliced into the pass-0 work list right after its parent, so everything a top-level container can carry a nested one can too; two parents may each nest a `Kind` without colliding. The plain name resolves through the LEXICAL parent chain while anything inside the parent is in scope — a sibling FIELD (a field layout now registers with its container as the current scope, so `mode: Mode` resolves even when `Mode` is declared after it — `std.fmt.Number`'s shape, the wall `std.fmt.bufPrint` hit first), a method, a grandchild naming an uncle — and never leaks outside it. Qualified access works in type and value position: `Number.Mode`, `A.B.C`, `Number.Mode.decimal`, a nested const `A.P.EXTRA`, a static call `A.P.two()`. Oracle `nested_containers`; example `examples/zig-nested-containers/`; unit `ZigNestedContainerTests`. A container nested in an ENUM or UNION body (std.Target.Cpu.Arch's `pub const Family = enum {…}`) and a cross-MODULE qualified nested type (`std.Target.Cpu.Arch`, `tgt.Cpu.Arch.Family`: the module prefix, then the owner's nested containers / type consts) resolve too (oracle `module_nested_enum_types`; real-std differential `Dotcc_matches_zig_std_target_arch_from_source`). A nested container in a W4 REIFIED struct flattens per instance (`Map__u16_u8__Entry`), with the instance's comptime seeds for its fields and deferred method bodies (oracle `reified_nested_containers`; unit `ZigContainerScopeTests`). A container's TYPE const (`const FingerPrint = u7;`, or `if (c) enum {…} else enum {…}`) is evaluated on first use in that container's scope, so a field names it, and it carries the width it spelled into `@typeInfo(FingerPrint).int.bits` (7, not the byte's 8; bare or qualified; oracle `container_type_const_widths`); a field default is lowered in its container's scope (a sibling const `= free`) and may be a value `if` (oracle `comptime_type_arms`). **Lenient:** dotcc accepts declarations BETWEEN fields, which zig rejects |
| `const P = extern struct { … };` | ✅ | layout-controlled struct (Milestone R, part 2) — guaranteed C-ABI layout → C# `[StructLayout(Sequential)]` (natural alignment + tail padding). Identical to a plain struct except the layout is pinned; `@sizeOf` matches Zig's C-ABI size. Fields/methods/consts as for a plain struct |
| `const P = packed struct { … };` | ✅ | layout-controlled struct (Milestone R, part 2) — no inter-field padding → C# `[StructLayout(Sequential, Pack=1)]`. **V1 byte-packs** (Pack=1), so `@sizeOf`/field offsets are self-consistent and match Zig's bit-backing-integer model *only* when fields are byte-multiples summing to an ABI size (e.g. `packed struct { a:u8,b:u8,c:u8,d:u8 }` = 32 bits = 4 bytes on both). **Sub-byte fields are bit-fields (task #51):** an integer field declared narrower than the type it lowers to (`u7`, `u1`, `u3`) is a C bit-field of that width, so a run of them shares one storage unit with masked accessor properties: std.hash_map's `Metadata = packed struct { fingerprint: u7, used: u1 }` is ONE byte and `@bitCast`s to a `u8`, as in zig (unit `ZigHashMapTests`, oracle `hash_map_shapes`). **Cuts:** a run whose fields have DIFFERENT lowered widths starts a new storage unit (MSVC-style) where zig would keep packing into its backing integer, so such a struct's `@sizeOf` / bit positions can differ from zig's; a `bool` field stays a byte; empty `extern/packed struct {}` |
| `packed struct(u16) { … }` with a backing integer, and a returned `return packed struct(MaskInt) { … }` (road-to-zig-std, task #61) | ✅ | the same packed layout as the unbacked form (std.bit_set's Integer; 480 `packed struct(T)` in std). The backing type is parsed and not re-checked (zig rejects fields that do not total it). With it: `MaskInt == u0` folds (dotcc lowers no `u0` / `i0`, so no lowered type is one), prefix wrapping negation `-%x` (std.math.boolMask), an integer widened into a `?usize` return (`return @ctz(mask);`), and a constant `~@as(u8, 0)` narrowed under `unchecked` (C# evaluates `~` promoted to `int`). Unit `ZigPackedStructTests`; oracle `packed_struct_backed`; real-std differential `Dotcc_matches_zig_std_bit_set_from_source` (StaticBitSet / IntegerBitSet, 33 == zig) |
| std.Io.Writer.Allocating's shapes (road-to-zig-std, task #60) | ✅ | a vtable const of the container's own functions (`.drain = Allocating.drain`, a bare sibling `.rebase = growingRebase`); **`&vtable` of a container const is static storage** (it was a copy on the current frame, a dangling pointer once that frame returned: a silent crash); `@fieldParentPtr("field", ptr)` at a `*Parent` result type; the curated allocator's `rawAlloc` / `rawResize` / `rawRemap` / `rawFree`, `@returnAddress()` (0: managed code has none), `Alignment.of(T)`; a value `if (r) |n| … else |e| …` over an error union (grammar); a lazy module's function body resolves `Module.Decl` through the module (not its file struct). Unit `ZigWriterShapesTests`; oracle `writer_shapes`. **Not yet (task #63, the platform floor):** std.fmt.allocPrint, whose vtable pulls in `sendFile` and with it `File.Handle` = `std.posix.fd_t` |
| labeled switch expression `r: switch (x) { 0 => 10, … => { break :r v; } }` (road-to-zig-std, task #59) | ✅ | a labeled value block whose body is the switch (grammar; std.math.shl): a bare value prong is `break :r value`, a block prong breaks explicitly, a switch nested in a prong keeps its own prongs, a comptime subject folds to one prong. With std.fmt.parse_float: a call with only type arguments returning a COMPTIME-ONLY struct (a `comptime_int` field, FloatInfo) binds at compile time (a runtime struct stays a runtime call, as zig rules), a comptime int switch with a `@compileError` prong folds before any prong lowers, and a reified struct's consts register before its fields. Unit `ZigParseFloatShapesTests`; oracles `labeled_switch`, `parse_float_shapes`. **Not yet:** `continue :r x` (a labeled switch as a state machine) |
| **std.fmt.parseFloat RUNS from real std, bit-exact == zig** (road-to-zig-std, task #59) | ✅ | f64 and f32 over the fast path, Eisel-Lemire, the slow big-decimal path, hex floats, subnormals, overflow, inf / nan, underscores and an invalid input (differential `Dotcc_matches_zig_std_parse_float_from_source`, 112 == zig). The last walls, THREE of them silent wrong answers: an untyped literal on the left of `<<` is a comptime_int and widens to the carrier (C#'s `int` shifted `1 << 52` by 52 MOD 32); `opt orelse error.E` is an error union (`ErrUnion.OrError`; the error's code had been the payload); `buf.* = @bitCast(v)` through a `*[N]u8` writes the bytes (`Unsafe.WriteUnaligned`; it had assigned the pointer). Also: `while (true)` renders bare (CS0161), an untyped float literal at an `f32` sink is a `float` literal (CS0664), a type alias chosen by `switch (T)` keeps its declared width, `Gen(T).CONST` as a value, `break :blk switch (x) { 0 => break, … }` as a statement, a comptime-int argument read through the interpreter, and a curated list of a failed container fails its holder. Unit `ZigParseFloatRunTests`; oracle `parse_float_core` |
| a compound assignment as a switch prong body `0 => hits += 1,` (task #64) | ✅ | the assignment prong takes any assignment operator (`+=`, `*=`, `|=`, `-%=`, `<<=`, …; grammar: `AssignOp` where `=` was). Pin in `ZigStdHelperShapesTests`; oracle `compound_assign_prong` |
| `std.debug.print("{s}", .{slice})` with a byte slice (task #65) | ✅ | the curated print passes the slice to the runtime builder, whose `Arg(ConstSlice<byte>)` / `Arg(Slice<byte>)` overloads print exactly `.len` bytes (no NUL is read), as zig's `{s}` does. Pin in `ZigStdHelperShapesTests`; oracle `debug_print_slice` |
| `std.math.pow` / `sqrt` / `divCeil`, `std.mem.lastIndexOf`, `std.meta.eql` from real std (tasks #66 to #70) | ✅ | the float builtins (`@sqrt`, `@sin`, `@cos`, `@tan`, `@exp`, `@exp2`, `@log`, `@log2`, `@log10`, `@floor`, `@ceil`, `@trunc`, `@round`, `@abs` of a float) map to `System.Math` / `System.MathF` at the operand's width, except `@round`, which rounds half away from zero as zig does (`ZigMath.RoundAway`; .NET rounds to even); `@setRuntimeSafety` / `@setFloatMode` lower to nothing; `while (true) : (i -= 1)` renders `for (; ; …)` so C# sees the loop never falls out (CS0161); a comptime-taken `if` that returns through a hoisted `catch unreachable` ends the block, so pow's dead `@compileError` is never analysed; `if (c) unreachable else 1` takes its sink's type; `@typeInfo(T).@"struct".layout` folds to `.auto` / `.@"extern"` / `.@"packed"`, and a packed struct's `==` compares its backing bytes (a generated C# operator). Pins in `ZigStdHelperShapesTests`; oracles `float_builtins`, `struct_layout_packed_eq`; real-std differentials `Dotcc_matches_zig_std_math_pow_sqrt_divceil_lastindexof`, `Dotcc_matches_zig_std_meta_eql` |
| **std.hash.Crc32 (and every `std.hash.crc` algorithm) from real std, bit-exact** (tasks #71, #79) | ✅ | a type-returning generic takes a comptime STRUCT value (`Crc(u32, .{ .polynomial = …, … })`): the value keys the instance by a digest of its contents and its body, consts and methods read its fields. A global or container `const` computed by a labeled block (`const lookup_table = blk: { var t: [256]I = undefined; for (&t, 0..) \|*e, i\| { … } break :blk t; };`) runs in the comptime interpreter, once, into a static (element-pointer stores through `\|*e\|`, and a store to a fixed-width integer wraps to its width, as the runtime conversion does). `@bitReverse` reverses the operand's declared width (a `u3` reverses 3 bits), and a same-module alias of a type-call alias (`Crc32 = Crc32IsoHdlc`) resolves. Pins in `ZigStdHelperShapesTests`; oracles `labeled_block_consts`, `comptime_struct_type_param`, `bit_reverse`; real-std differential `Dotcc_matches_zig_std_hash_crc` (widths 5 to 64, reflected and not) |
| `std.unicode.utf8CountCodepoints` / `utf8Decode` / `utf8Encode` from real std (task #72) | ✅ | `utf8Decode2(bytes[0..2].*)`: a comptime-length slice dereference passes to a `[N]T` parameter as its element pointer. **A silent miscompile fixed:** a returned `if` or switch with an error arm (`else => error.Utf8InvalidStartByte`, `return if (ok) v else error.E;`) was wrapped whole as a success, so the error's code came back as the payload; each arm is now the success or the failure of the error union. Pins in `ZigStdHelperShapesTests`; oracles `error_arm_returns`, `slice_deref_array_arg`; real-std differential `Dotcc_matches_zig_std_unicode`. **Not yet:** `utf8ValidateSlice` (its `comptime first: { … }` table, task #82) |
| `std.debug.print("{}", .{b})` of a bool (task #81) | ✅ | `{}` / `{any}` of a bool prints `true` / `false`; a local bound to a comparison (`const t = x > 2;`) is a zig `bool` (a C# `CBool`), not C's `int`. Pin in `ZigStdHelperShapesTests`; oracle `debug_print_bool` |
| a `switch` choosing a type argument (task #73, part) | ✅ | `id(switch (@typeInfo(T).int.signedness) { .signed => i32, .unsigned => u32 }, v)` and `@sizeOf(switch …)`: a switch in any type position selects its prong's type (std.math.log2's `log2_int(switch (…) {…}, x)`); a `comptime_int` local bound to a call folds in an array extent (a runtime-typed one stays rejected, as zig rejects it). Oracle `switch_type_argument`. **Not yet:** std.sort.pdq itself needs a comptime_int beyond 64 bits (task #83); a switch over a comptime bool, or in a parameter's type (task #84) |
| `std.mem.readVarInt` from real std; comptime type choices (tasks #76, #77 part) | ✅ | `const signedness = @typeInfo(T).int.signedness;` binds a comptime tag that `@Int(signedness, …)`, `==` and `switch` read. `const DT = if (@bitSizeOf(T) <= 64) u64 else u128;` is a type alias of the taken arm, and **a silent wrong answer fixed:** `DT == u64` in a type switch had compared a missing declared width against 64 and taken `else`. `&array_global` is the array's storage pointer (it had been CS0306), and `.len` reads through a pointer to an array. Pins in `ZigStdHelperShapesTests`; oracles `signedness_tag_binding`, `type_if_alias`; real-std differential `Dotcc_matches_zig_std_mem_read_var_int`. bufPrint `{d}` / `{e}` of a float RUNS from real std since tasks #85 and #96 |
| `std.base64` from real std; array fields in struct literals; more curated ArrayList members (tasks #74, #75, #78) | ✅ | A struct literal that sets an array field (inline storage a C# object initializer cannot set) is a hoisted temp with each array field copied in after it; at module scope, and for a lazy module's top-level const, the global gets a synthesized `__init_NAME()` initializer (evaluated once, as zig evaluates a top-level const). `@splat(v)` fills a `[N]T` (nested for `[M][N]T`), `"…".*` is the string's array value, an index takes a cast builtin's `usize` result type, and a lazy module's fn-pointer type alias naming a later container is deferred to its first use. Curated ArrayList gains `insert`, `orderedRemove`, `swapRemove`, `getLast` (`?T`, as in 0.17-dev), `ensureTotalCapacity`, `ensureUnusedCapacity`, `appendAssumeCapacity`, `shrinkRetainingCapacity`. Pins in `ZigStdHelperShapesTests` / `ZigFrontendTests`; oracles `array_list_index_members`, `array_field_literal`, `global_array_field_init`; real-std differential `Dotcc_matches_zig_std_base64` |
| `catch \|e\| return switch (e) {…}`; `comptime label: { … }`; `std.unicode.utf8ValidateSlice` (tasks #80, #82) | ✅ | Grammar: a capture arm may return a switch expression, and `comptime` may prefix a labeled block in value position. A `comptime` block runs in the comptime interpreter, like a const's labeled block (task #79), into a static table per site and function instance. `a ++ b` joins two array values of known length element by element (a named array or a nested join; a call's result must be bound first), and a shift amount takes a cast builtin's type (`1 << @intCast(i)`). Pins in `ZigStdHelperShapesTests`; oracles `catch_return_switch`, `comptime_labeled_block`; real-std differential `Dotcc_matches_zig_std_unicode_validate` |
| a switch over a comptime bool (task #84) | ✅ | `switch (wide) { true => u32, false => u8 }` with a comptime-known subject selects its prong at lowering time, as a type alias (`const W = switch …`) or a value; a runtime bool subject stays a runtime switch. Pin in `ZigStdHelperShapesTests`; oracle `comptime_bool_switch`. **Not yet:** a switch as a PARAMETER's type (`x: switch (c) {…}`, which no std file uses) |
| `inline` switch prongs; type-switch captures; std.math.gcd, std.hash.XxHash32/64 from real std (tasks #86, #87) | ✅ | `inline 0, 1, 2 => \|n\| { … }` in a runtime switch instantiates the prong once per case value (a range expands, up to 256), `n` a comptime constant, so `inline for (0..n)` unrolls; `inline else` is a loud cut. `else => \|U\| U` over a TYPE subject binds the capture to the subject type (a type alias or a type-returning body). A compound shift's `@intCast` count takes C#'s int, and a comptime_int prints. **A silent miscompile fixed:** a string literal passed as an `anytype` was typed with its stored NUL, so `x.len` was one too many (XxHash32 of "hello" read 6 bytes). Pin in `ZigStdHelperShapesTests`; oracle `inline_prongs_type_capture`; real-std differentials `Dotcc_matches_zig_std_math_gcd`, `Dotcc_matches_zig_std_hash_xxhash` |
| std.bit_set.ArrayBitSet / StaticBitSet from real std (task #88) | ✅ | `return extern struct { … }` reifies with the C layout. Value loops whose `else` RETURNS (`const w = for (xs) |x| { … break x; } else return null;`, also with a `|*x|` capture and for `while`), and a value `while (c) : (i += 1) { … } else v`. A type body binds `const info: std.builtin.Type = @typeInfo(T);` for its folds, and an `if` condition that calls (`!std.math.isPowerOfTwo(@bitSizeOf(T))`) runs through the comptime interpreter. `null` / `undefined` are union variant names (std.builtin.Type). A const whose labeled block builds a struct with a non-zero array field (`full`) is built by a synthesized initializer that copies the evaluated array in. **Two silent miscompiles fixed:** `~@as(u64, 0) >> n` folded with a sign-extending shift (all ones), and such a struct lost its array field (the copy-in was hoisted before the block's own locals and the interpreter skipped it). Pin in `ZigStdHelperShapesTests`; oracle `extern_struct_value_loops`; real-std differential `Dotcc_matches_zig_std_bit_set` |
| std.crypto.hash.Md5 and sha2.Sha224 / Sha256 from real std (task #90) | ✅ | `std.crypto.hash.Md5` reaches `pub const Md5 = @import("crypto/md5.zig").Md5;` inside a nested namespace struct (an inline `@import` as a qualified type base, resolved on demand), and `std.crypto.hash.sha2` is a MODULE bound as a const of that struct. Inline `asm` PARSES (`asm [volatile] ("template" : outputs : inputs : clobbers)`) and is a loud cut when lowered; std guards it behind comptime target checks, and an else-less `=> if (c) { … }` prong whose condition folds false is never lowered (dotcc's target reports no `sha`, as zig does on this host). Also: a `comptime_int` / `type` parameter is comptime without the keyword, `align(N)` on a field is accepted (not applied: C# places the field), `Alias{ … }` for an array alias, a comptime ARRAY argument to a type-returning generic (keyed by a digest, read from a const global too), and `inline for` over a comptime array VALUE binds each element as a literal. Pin in `ZigStdHelperShapesTests`; oracle `asm_dead_prong_comptime_array_param`; real-std differential `Dotcc_matches_zig_std_crypto_hash` |
| Comptime operands a type cannot hold are rejected (task #91) | ✅ | As zig: a literal shift amount that does not fit `Log2Int` of the shifted operand (`~@as(u64, 0) >> 88`: "type 'u6' cannot represent integer value '88'"), and an integer literal outside its typed peer's range (`@intFromBool(b) * 10`, `x_u8 + 300`, `x_i8 - 128`). A typed amount (std.math.rotl's `1 +% ~ar`) is not checked: it fits by construction, and dotcc folds it at its carrier width. Only a width the source spells is checked (a declared type, `@as`, `@intFromBool`'s `u1`), so a `comptime_int` operand, which is unbounded, is never rejected. A compile error, not an unsupported feature. Pins in `ZigStdHelperShapesTests` |
| std.enums.values from real std; comptime slices and type-body statements (task #89, part) | ✅ | A `comptime x: []const T` parameter takes a comptime slice (an integer `@typeInfo` member list such as `field_values`, or any comptime array or slice), keyed by digest; a non-byte comptime slice splices as a pinned static. An inline function's `comptime { …; return &final; }` block in a slice-returning function runs in the interpreter and returns that static. A type body may bind a comptime `var` (a member list slice `field_values[0..n].*` becomes a comptime array) and run expression statements through the interpreter (`@setEvalBranchQuota` is a floor below dotcc's budget, so it is skipped); an inline `struct { fn f … }.f` is a comptime function argument; `{}` is the interpreter's void value; `@enumFromInt` of a 128-bit operand casts through the tag type; `@typeInfo(E).@"enum".mode`. **Silent miscompiles fixed:** the comptime-return block had been lowered at runtime, returning a slice over the frame's `stackalloc`; the interpreter skipped a `ZigMem` array copy (`const final = result;` stayed zeros; any unevaluable call statement now aborts); a non-exhaustive enum's `_` was lowered as a member; interpreter shifts by 128 or more had masked the count. Pins in `ZigStdHelperShapesTests`; oracle `comptime_block_static_slice`; real-std differential `Dotcc_matches_zig_std_enums_values` |
| comptime_int past 64 bits; std.mem.sortUnstable and std.enums.EnumIndexer from real std (tasks #83, #89) | ✅ | Binary arithmetic whose operands are all comptime_int (literals, comptime_int locals, folded calls) is evaluated in 128 bits and, when the result leaves the operands' carrier, stays a comptime_int (`maxInt(u64) + 1` had wrapped to 0, so pdqsort's `log2_int(maxInt(usize) + 1)` bound was wrong). `@inComptime()` is true in the interpreter: a type-returning generic keeps both arms of an `if (@inComptime())` and the interpreter takes the comptime one. `@addWithOverflow` and friends, tuple indexing and a pinned-array literal evaluate at comptime; a comptime_int parameter of a type-returning function is a comptime value; a type body's value and aggregate locals seed the reified struct's members; an untyped enum's `tag_type` is zig's inferred `u{log2(n)}` (the old loud cut); a comptime-const switch label folds to the subject's type; `break f();` of a void call leaves a plain loop; another module's container const reads through a type alias. Oracle `comptime_int_wide_and_comptime_generic_body`; real-std differentials `Dotcc_matches_zig_std_mem_sort_unstable` and `Dotcc_matches_zig_std_enums_enum_indexer` (dense and sparse) |
| `@Struct` reification; std.EnumSet and std.EnumArray from real std (task #93) | ✅ | A type-returning function's `return @Struct(layout, null, names, types, attrs)` reifies one struct per instance, named after it (std.enums.EnumFieldStruct): the names are a spelled `&.{ "a", "b" }`, a `@typeInfo` member list or any call the interpreter evaluates to one (`std.meta.fieldNames(E)`); the types are `&@splat(T)` or a spelled list; each field's `.default_value_ptr` is null or a type-body `const p: ?*const anyopaque = if (default) \|d\| @ptrCast(&d) else null;`, and a literal omitting the field is filled from it. A duplicate name and a list of the wrong length are rejected as zig does; a packed `@Struct`, a `comptime` field and a `@Struct` outside a type-returning function are loud cuts. `field_names` in value position is a pinned static array of string slices (`fieldNames(E)` returns it; a plain `for` walks it, as in zig). `@tagName` of a comptime-known enum value is its member name (a runtime one is a cut), and a local `const tag = @tagName(key);` names a field for `@field`. A generic method's comptime struct parameter may be spelled in its return type (std.bit_set's `iterator(self, comptime options) Iterator(options)`). A slice, array or tuple passed to a scalar parameter is rejected (zig: "expected type"). **Silent miscompiles fixed:** a struct literal in one module of a struct another module declares dropped every omitted field default (`const s: m.S = .{ .b = 1 };` left `a` zero); an enum-typed comptime fold was spliced without its cast (`E key = 0UL;`, which C# rejects). Pins in `ZigStdHelperShapesTests`; oracle `reify_struct_and_comptime_tag_name`; real-std differential `Dotcc_matches_zig_std_enums_enum_set` |
| By-ref `if (opt) \|*v\|`; std.EnumMap from real std (task #94) | ✅ | `v` points INTO the optional, with no copy and no write-back: a value optional's payload through `ZigMem.OptionalPayload` (the BCL's `Nullable.GetValueRefOrDefaultRef`, so no layout is assumed), an optional pointer's own slot (`**T`). A repeatable lvalue condition (a variable or a field of one) is used as is; any other is a temporary the capture points into. An error union's by-ref capture and a comptime optional's are loud cuts. `@as(?T, null)` is a comptime null argument (at a `??T` parameter zig keeps a non-null outer around a null payload; reading it as no default is observably the same, since an omitted optional field is null). Pins in `ZigStdHelperShapesTests`, `SliceRuntimeTests`; oracle `if_capture_by_ref`; real-std differential `Dotcc_matches_zig_std_enums_enum_map` |
| Stores to a `const` rejected (task #95) | ✅ | zig's "cannot assign to constant" for a plain, compound or saturating store to a `const` local or global, a parameter, a field or array element of one, and anything reached through a pointer or slice to const. `&y` of such storage is a `*const T`, and so is a by-ref capture `if (c) \|*v\|` of a const optional. A `const` that HOLDS a pointer still writes its pointee (`const p: *[2]u32 = &b; p[1] = 3;`). dotcc had accepted and run `const y: u8 = 1; y = 2;`. Pins in `ZigStdHelperShapesTests` |
| A pointer to a container type as a comptime namespace (task #85) | ✅ | `comptime tables: anytype` fed `&Backend64_TablesFull` (std.fmt.float.binaryToDecimal), or a const bound to a comptime `if` / `switch` of such pointers (`const tables = switch (DT) { u64 => &A, u128 => &B, else => unreachable };`), binds as a comptime TYPE seed: one instance per container, no runtime slot, and `tables.T` / `tables.bound` / `tables.scale(x)` resolve through the container. `comptime x: anytype` is an `anytype` (it had fallen to a comptime value of type `anytype`). `x == if (c) a else b` / `!=` parse with the `if` as the right operand, its arms at the operand level (a full-expression else arm would make a trailing `catch` ambiguous). A type body's `comptime check(…);` statement evaluates to void. Pins in `ZigStdHelperShapesTests`; oracle `comptime_type_pointer_namespace` |
| std.fmt float formatting from real std; `if (c) i += 1 else i -= 1;` (task #96) | ✅ | `bufPrint` of `{d}` / `{e}` for f64 and f32 runs from std source (render, binaryToDecimal, formatScientific, formatDecimal) and prints what zig prints. An `if` statement's then-arm may be an assignment of any operator ended by the `else` (an AssignArm, the ReturnArm's twin). A nested array const (`[326][2]u64`) is one flat pinned table, row after row, as it is indexed; a lazy module's top-level array literal is memoized as that one static. A `catch` fallback is result-located at the payload type (`catch "ERR"` for a slice). **Bad emits fixed:** a nested array global had rows emitted as `stackalloc` pointers inside the static initializer, and a lazy module's table was rebuilt as a stack array on every call. Pins in `ZigStdHelperShapesTests`; oracle `if_assign_arm_and_flat_table`; real-std differential `Dotcc_matches_zig_std_fmt_float_formatting` |
| A runtime call of a comptime-returning function rejected (task #92) | ✅ | zig's "function called at runtime cannot return value at comptime": a plain (non-inline) function whose body returns from a `comptime { }` block may only be called at compile time. dotcc records `inline fn` (the keyword had been erased where a declaration is unwrapped), each such function, and the runtime call edges between function bodies (a call under `comptime`, in a comptime block, a type body, an array extent or a container-level initializer is not one); once every module has lowered, the graph is walked from `main` (or every top-level function of a program without one), so a function only a comptime call reaches, or one nothing calls, stays legal, as zig analyzes a function only once it is referenced. Pins in `ZigStdHelperShapesTests`; oracle `comptime_return_called_at_compile_time` |
| Narrow complements and arbitrary-width wrapping (task #97) | ✅ | `~x` of a u8 / u16 keeps its type (zig has no integer promotion), and a complement or `+%` / `-%` / `*%` of an arbitrary-width unsigned (`u3`, `u5`, carried in a byte) is masked to its declared width. **Silent miscompiles fixed:** `~@as(u8, 1)` had been C#'s `int` -2 (so `~x == 254` was false), and std.math.rotl(u8, 0x81, 1) returned 2 (zig 3): its `x >> 1 +% ~ar` over a `u3` became `x >> -1`, which C# masks to 31. Pins in `ZigStdHelperShapesTests`; oracle `narrow_complement_and_wrap`; real-std differential `Dotcc_matches_zig_std_math_rotate` |
| Signed `/` and `%` need an explicit rounding (task #98) | ✅ | zig's "signed integers must use @divTrunc, @divFloor, or @divExact" (for `/` and `/=`) and "signed integers and floats must use @rem or @mod" (for `%` and `%=`), unless both operands are comptime-known; unsigned operands, float `/`, comptime_int and comptime contexts are unaffected. `@mod` / `@rem` of a float now lower (ZigMath float overloads; they had reached the integer-only generic, CS0315). Pins in `ZigStdHelperShapesTests`; oracle `division_builtins_and_unsigned` |
| A comptime function argument to a type-returning generic; tuple `.len` (task #99) | ✅ (std.StaticStringMap: #100) | `comptime eql: fn (a: []const u8, b: []const u8) bool` (std.StaticStringMapWithEql) keys one reified struct per function and is aliased while the type body evaluates and in every reified method body. A tuple's `.len` is its element count. Pins in `ZigStdHelperShapesTests`; oracle `type_returning_comptime_fn_arg` |
| std.PriorityQueue from real std (task #103) | ✅ | a comptime function argument stays aliased for the whole reification, so a nested container that passes it along (`Iterator`'s `queue: *PriorityQueue(T, Context, compareFn)`) resolves; a value `switch` takes a block prong that never completes (`error.OutOfMemory => { return; }`); the signed `/` / `%` check (#98) reads the ZIG peer type, so `(i * 37) % 101` over a `u16` is not mistaken for `int`. push / pushSlice / pop / peek / initContext run == zig. Pins in `ZigStdHelperShapesTests`; oracle `comptime_fn_arg_nested_container` |
| A capture-`while` continue expression that reads the capture (task #105) | ✅ | `while (it) |n| : (it = n.next)` (std.SinglyLinkedList's walk): the capture is declared in the `for` init, so the post and the body both see it, and assigned each turn; a labeled `continue` still runs the post. std.SinglyLinkedList with `@fieldParentPtr` runs == zig. Pin in `ZigStdHelperShapesTests`; oracle `while_capture_cont_reads_capture` |
| A comptime `bool` argument asked as a question (task #106) | ✅ (std.AutoArrayHashMap: std.MultiArrayList, #108) | `ArrayHashMap(K, V, AutoContext(K), !autoEqlIsCheap(K))`: a `comptime x: bool` argument to a type-returning generic folds through the comptime-condition folder (a call switching over `@typeInfo(K)`, `!`, `and` / `or`), keys the instance, and is never lowered as a runtime call. Pin in `ZigStdHelperShapesTests`; oracle `comptime_bool_question_arg` |
| `Allocator.dupeSentinel` / `allocSentinel` (task #107) | ✅ | `Error![:s]T` on every allocator (FBA, arena, the devirtualized C heap, a custom vtable): `ZigAlloc.DupeSentinel` / `AllocSentinel` allocate one element more and store the sentinel past the end; the `len`-long slice before it is returned, since dotcc's slice carries no sentinel. `free` of one returns `len` elements, not zig's `len + 1` (see [deferred.md](plans/deferred.md#runtime-fidelity)). Pin in `ZigStdHelperShapesTests` |
| Inline `union(enum) { … }` / `union { … }` types (task #104) | ✅ | as a parameter, field or local annotation type (`fn f(x: union(enum) { a: u8, b: u16 })`), reified per occurrence under an anonymous name like the inline `struct { … }` / `enum { … }` types, so switches, capture prongs and `.{ .tag = v }` literals work as on a named union. Fields only. As a call argument see #110. Pin in `ZigStdHelperShapesTests`; oracle `inline_union_types` |
| Tagged-union switch prongs and tag comparisons (task #109) | ✅ | a union `switch` takes an assignment prong (`.reset => acc.* = 1`) and its capture twin (`.add => |n| acc.* += n`, grammar `prongCaptureAssign`); `u == .tag` / `.tag == u` / `u != .tag` compare the union's tag; a capture or block `switch` / `if` after a compound operator (`bonus += switch (c) { .add => |n| n, … }`) fills a temp as after `=`. Pin in `ZigStdHelperShapesTests`; oracle `union_switch_assign_and_tag_compare` |
| Inline container types as call arguments (task #110) | ✅ | `@as(struct { a: u8 }, .{ .a = 4 })`, `@as(enum { x, y }, .y)`, `@as(union(enum) { … }, …)`, `@TypeOf(@as(union { … }, …))`, a struct type passed to an `anytype` parameter: `Arg` admits the four inline container forms (reusing AType's actions; `enum` / `union` are not in FIRST(Expr), and the closure idiom `struct {…}.f` continues with `.`, which no Arg is followed by). Pin in `ZigStdHelperShapesTests`; oracle `inline_container_type_args` |
| In-function `enum` / `union` declarations (task #111) | ✅ | `const Suit = enum(u8) { … };`, `const Card = union(enum) { … };` and `const Raw = union { … };` inside a function body register under `<fn>__<Name>` through the top-level registration, the plain name shadowed for the rest of the body, as a local struct (W2) is; two functions' same-named locals never collide. Fields only (a method needs a container-level declaration). Pins in `ZigStdHelperShapesTests`; oracle `local_enum_and_union_decls` |
| A container type as a `switch` prong value (task #108, step 1) | ✅ | `.int => struct { lo: T, hi: T }`, `.@"union" => |u| struct { … }`, `.@"enum" => enum { … }` (std.MultiArrayList's `Elem`): grammar `ProngType` under the plain / capture expr prong actions, so a comptime type switch reifies the selected prong's type and only parses the others. Pin in `ZigStdHelperShapesTests`; oracle `container_type_prong_value` |
| The general multi-object `for` (task #108, step 2) | ✅ | `for (a, &b, c) |x, *y, z|`, `for (a, b, 0..) |x, y, i|`, four or more objects, bounded range objects (`5..8`), zig fmt's trailing comma: one production (`ForObjs` / `ForCaps`, symbols 186 to 189) in place of nine fixed pair / triple / indexed shapes. `(s, N..)` still walks the slice with its index; every other shape walks all objects in lockstep, a range capture being its start plus the index. `inline for` over comptime lists takes any number of lists and `0..`. A capture-count mismatch and a by-reference range index are rejected. Pins in `ZigStdHelperShapesTests`; oracle `multi_object_for_shapes` |
| A generic instance's value consts in its nested containers (task #108, step 3) | ✅ | a nested container's field sized by a sibling value const of the instance (std.MultiArrayList's `Slice` with `ptrs: [field_names.len][*]u8`): the instance registers its value consts before its nested containers' bodies. Pin in `ZigStdHelperShapesTests`; oracle `nested_container_reads_instance_consts` |
| `for … else` statements; a capture prong with a capture-`if` body (task #108, step 4) | ✅ | `for (xs, 0..) |x, i| { … break; } else { … }` (single or multi-object, std.meta.FieldEnum): the else runs only when the loop ends without a `break`. The loop's own exit sets a flag guarding the else after the loop, so a `break` inside the else still reaches an outer loop; with no break in the body the else follows unguarded. `.@"union" => |u| if (u.tag_type) |E| { … }` parses (a named cut if a comptime switch selects it). Pins in `ZigStdHelperShapesTests`; oracle `for_else_statements` |
| `@Enum` as a type-returning function's result; saturating ops over comptime operands (task #108, steps 5 and 6) | ✅ (FieldEnum's `&std.simd.iota(…)` values: a `@Vector(3, u8)`, see #108) | `fn E(…) type { return @Enum(TagInt, .exhaustive / .nonexhaustive, names, &values); }` registers the enum under the instance's name (as `@Struct`, #93): names as `@Struct` reads them, values spelled or evaluated by the interpreter; duplicate names or values and a count mismatch are rejected. `a -| b` / `+|` / `*|` over comptime operands fold, clamped to the peer type (std.meta.FieldEnum's `field_names.len -| 1`). Pins in `ZigStdHelperShapesTests`; oracle `enum_builtin_and_saturating_fold` |
| A typed declaration's comptime integer out of range is rejected (task #112) | ✅ | `const s: u8 = 300;`, `-2`, `3 -| 5` (comptime_int arithmetic, so -2), `var s: u3 = 8;`: the untyped comptime initializer is evaluated before any narrowing and checked against the declared width, with zig's "type 'u8' cannot represent integer value '300'". dotcc had narrowed or wrapped it silently. Pins in `ZigStdHelperShapesTests` |
| A struct returned by a function's `comptime { … }` block (task #100, step 1) | ✅ (StaticStringMap.initComptime: #100) | `inline fn build(comptime n) T { comptime { …; self.vals = &fin; self.meta = &.{ … }; return self; } }`: the block is evaluated by the interpreter (an early `return` is its result) and the struct spliced with each pointer field's target (an array, a struct, an element) pinned for the program's life. **Silent miscompile fixed:** as runtime code the struct pointed into the function's dead stack frame. Two pointers to one comptime array get separate pinned copies (see deferred.md). Pin in `ZigStdHelperShapesTests`; oracle `comptime_block_struct_pinned` |
| A comptime block run statement by statement; a `for` over a tuple (task #100, step 2) | ✅ (StaticStringMap.initComptime: #100) | A function's struct- or slice-returning `comptime { … }` block is lowered and interpreted ONE STATEMENT AT A TIME, each statement's locals published as comptime values, so a later statement folds an earlier result (`var bins: [self.peak + 1]u8`). A tuple literal evaluates at comptime; `t.@"0"` reads a tuple element by position; a `for` over a tuple unrolls where zig allows it (a comptime context) and is rejected like zig elsewhere ("tuple field index must be comptime-known"). Pins in `ZigStdHelperShapesTests`; oracle `comptime_block_session` |
| A `comptime x: anytype` tuple argument; a tuple iterated by a comptime-only helper (task #100, step 3) | ✅ (real std.StaticStringMap(u8).initComptime, `get` / `has` / `getLongestPrefix` == zig) | `initComptime(.{ .{ "one", 1 }, .{ "three", 3 } })`: the tuple's comptime value keys the instance by digest and the body reads it as a comptime aggregate (no runtime slot). A function iterating a tuple is accepted while only comptime code calls it; a runtime call reaching it is rejected like zig, once the call graph is known (as task #92's comptime returns are). `return .{ … }` at an optional struct result is the payload. The curated `Allocator.free` also takes a `[]const T`. Cuts: an enum-literal tuple element (`.{ "if", .kw_if }`, #113) and a `void` value type (`StaticStringMap(void)`, #114). Pins in `ZigStdHelperShapesTests`; oracle `comptime_anytype_tuple_arg` |
| `void` as data (task #114) | ✅ (real std.StaticStringMap(void), a string set, == zig) | The element of a slice, many-pointer, array, optional or tuple, and a `*T` with `T = void` (std.mem.swap(void, …)), is the runtime's empty `Unit` struct: C# has no `void` element, generic argument or storage. The void value `{}` stored there is `default(Unit)`, and `{}` parses as a list element (`[3]void{ {}, {}, {} }`). `*anyopaque` stays an opaque `void*`. Pins in `ZigStdHelperShapesTests`; oracle `void_as_data` |
| An enum literal with no result type (task #113) | ✅ (real std.StaticStringMap(Kw), enum-valued, == zig) | zig types a bare `.member` as the comptime-only `@EnumLiteral()`. dotcc makes each literal a singleton type carrying its name (`CType.EnumLiteral`), so it coerces statically wherever it meets an enum, an optional enum or a tagged union, whatever expression carried it there (a tuple element `kv.@"1"` read in a comptime-called helper). `const lit = .blue;` emits nothing; a `var` of one is rejected like zig; the comptime interpreter keeps the name, so two tuples that differ only in a literal key two instances; a function whose parameter carries the type runs only at comptime (a runtime call is rejected, its runtime copy dropped). Cut: the `@EnumLiteral()` builtin as a TYPE spelling (std.Options' fields). Pins in `ZigStdHelperShapesTests`; oracle `enum_literal_no_result_type` |
| `comptime f() catch unreachable` as a comptime value (task #117) | ✅ | std.Random.int's `const ceil_bytes = comptime std.math.divCeil(u16, bits, 8) catch unreachable;` then `@Int(.unsigned, ceil_bytes * 8)`: zig's `comptime` takes the whole expression; dotcc's binds tighter, but its fold already evaluated the call, so the `catch` of a success is the payload, at the payload's type. An array extent or `@Int` width inlines such an unfolded const through arithmetic. Pin in `ZigStdHelperShapesTests`; oracle `comptime_catch_unreachable_width` |
| `.len` / `.ptr` through a single pointer to a slice (task #118) | ✅ (real std.StringHashMap put / iterator / contains == zig) | zig auto-dereferences a single pointer for field access: `e.key_ptr.len` with `key_ptr: *[]const u8` reads (and `p.len = 1` writes) the slice's fields, `p->Len`. Pin in `ZigStdHelperShapesTests`; oracle `slice_fields_through_pointer` |
| A comptime block building a table from `field_names` (task #116) | ✅ (real std.meta.stringToEnum == zig) | In a `comptime label: { … }` block (now a comptime context) a plain `for` over a comptime member list unrolls like `inline for`, so its captures are comptime; `@field(T, name)` with `T` an enum TYPE is the member; the interpreter zeroes a tuple (`var kvs_array: [n]struct { []const u8, T } = undefined;`) and splices each tuple element at its own type; a const bound to such a block keeps its comptime value; and a `comptime x: anytype` parameter also takes a comptime SLICE (of tuples) as its instance key. Pin in `ZigStdHelperShapesTests`; oracle `comptime_block_field_names_table` |
| Format guards over a comptime string (task #121) | ✅ (real std `{any}` of a struct and an integer == zig) | std.Io.Writer.printValue's guards settle at compile time, so their `invalidFmtError` / `@compileError` arms are never analysed: `fmt[0] == 'b' and ...` and `fmt.len != 0` over a comptime string; `const is_any = comptime std.mem.eql(u8, fmt, ANY);` (a top-level string const is readable at comptime, and the interpreter evaluates the curated `std.mem.eql`), read back as a comptime bool. Also `@typeInfo(T).@"struct".is_tuple`, `@intFromBool` of a settled bool, and a struct field read carries its declared width (`@typeInfo(@TypeOf(r.wide)).int.bits` for `wide: u21`). `{any}` of a STRUCT runs too (task #122): a lazily prepared module records its function declarations before it registers containers, so std.Io.Limit's `unlimited = math.maxInt(usize)` lowers while math.zig is still being prepared. Pin in `ZigStdHelperShapesTests`; oracle `comptime_format_guards`; real-std differential `Dotcc_matches_zig_std_fmt_any` |
| `@typeInfo(P).pointer.size` of a raw pointer (task #119) | ✅ (real std.Random.DefaultPrng == zig) | `*T`, `[*]T` and `[*c]T` lower to one C pointer, so the size class rides the SPELLING, as a declared integer width does: `&x` and `*T` are `.one` (a string literal too), `[*]T`, a sentinel pointer and a slice's `.ptr` `.many`, `[*c]T` `.c`; through a typed local, a parameter, an alias (`const Ptr = @TypeOf(pointer);`) and an `anytype` argument, whose class keys its own instance (a pointer no spelling classifies keys apart, so it never shares an answer). std.Random.init's `assert(@typeInfo(Ptr).pointer.size == .one)` folds, so DefaultPrng, Pcg and Sfc64 run from real std (`int`, `intRangeAtMost`, `uintLessThan`, `boolean`, `shuffle`, `float`, `bytes`). A pointer from a call or a field is still the loud cut. Pins in `ZigStdHelperShapesTests`; oracle `pointer_size_class`; real-std differentials `Dotcc_matches_zig_std_random_api` / `Dotcc_matches_zig_std_random_two_generators` |
| An in-function struct's methods close over the body's type aliases (task #124) | ✅ | std.Random.init's local `gen.fill` casts to `const Ptr = @TypeOf(pointer);`. The method body lowers after every instance of the enclosing generic, so the body's aliases are seeds of the struct's methods (with their declared width and pointer size class). Before, every instance's method read the LAST instance's binding: two same-layout structs dispatched to the wrong `get` without any error. Pin in `ZigStdHelperShapesTests`; oracle `local_struct_closes_over_alias` |
| An enum member valued by the root unit's own function (task #123) | ✅ | `green = maxOf(u8) - 5` in a single file: the root unit registers its containers (pass 0) before pass 1 declares every signature, so a call from a member's value declares that function early (the lazy-module path of task #122, for the root), and pass 1 reuses the declaration. A member's value is comptime, lowered as `comptime EXPR` is, so the call runs in the interpreter. A container const valued by one (`const default_size = base() * 2;`) works the same way. Pin in `ZigStdHelperShapesTests`; oracle `enum_member_valued_by_own_fn` |
| `std.mem.bytesAsValue` / `bytesToValue` (task #126) | ✅ | Curated like `asBytes`: their return type is reified through `CopyPtrAttrs` / `@Pointer(size, attrs, child, null)`, which dotcc does not model. A pointer cast of the bytes' data pointer to `*T` (const when the bytes are), from a pointer to an array, a byte slice or a string literal; `bytesToValue` reads through it (unaligned, as zig's `@ptrCast` checks neither length nor alignment). An array VALUE is rejected, as zig rejects it. Pins in `ZigStdHelperShapesTests`; real-std differential `Dotcc_matches_zig_std_bytes_as_value` |
| `std.fmt.allocPrint` from real std (tasks #60 / #63) | ✅ (real std == zig) | `std.Io.Writer.Allocating` puts `Allocating.sendFile` in its vtable, so its body is lowered though allocPrint never calls it, and that body needs `std.Io.File`, whose `handle: std.posix.fd_t` is the platform floor dotcc does not model. A function whose body fails to lower and which takes a single-item pointer to a WITHDRAWN container (one dotcc could not lower, see task #8) is compiled as a runtime trap naming itself: no dotcc program can construct such an argument, so no valid call reaches it, and only a function-value reference (a vtable slot) keeps it alive. A direct runtime call to it from `main` is still a compile error (the comptime-return call-graph check). Modeling `std.posix.fd_t` / `File.Handle` for the host is backlogged as [GitHub issue #126](https://github.com/sebgod/dotcc/issues/126). Also a static call on `std.ArrayList(T)` other than the removed managed `init` (`growCapacity`, as `Allocating` calls it) goes to real std's `array_list.Aligned(T, null)`. Pins in `ZigCuratedStdVsNavigationTests`; real-std differentials `Dotcc_matches_zig_std_alloc_print` / `Dotcc_matches_zig_std_array_list_grow_capacity` |
| Toward std.MultiArrayList (task #108, steps 7 to 11) | 🟡 (the `field_attrs` walk is next) | (7) A `@Vector` shape .NET vectors cannot hold (`@Vector(3, u8)`, std.meta.FieldEnum's `&std.simd.iota(u8, 3)`) is an array `[N]T` while evaluating at compile time (a type-returning body in any module, a `comptime` expression or block); at runtime it stays the loud cut, and a generic instance whose signature needed one is refused if a runtime call reaches it (the runtime array fallback is [GitHub issue #127](https://github.com/sebgod/dotcc/issues/127)). (8) A reified container's method whose SIGNATURE does not lower fails when it is called, not when the type is reified, as zig analyses a declaration only when referenced (std.MultiArrayList's debugger-only `dbHelper(…, entry: *Entry)`). (9) `@FieldType(T, name)`. (10) `inline for` over a list-valued const of an enclosing container. (11) `@field(T, name)` with `T` a type const of an enclosing container. Pins in `ZigStdHelperShapesTests`; oracle `comptime_vector_and_field_type` |
| Declared width of a range capture and of arithmetic (task #132) | ✅ | Real std's `{d}` asks `@typeInfo(T).int.bits` of every integer it prints (printIntAny). A `for (lo..hi) |i|` capture (and a `(list, 0..)` index) is a usize; an arithmetic or bitwise result takes its operands' width when they agree or one is an integer literal; a shift keeps its left operand's. Operands of different widths stay unknown and loud (zig's peer type widens to the larger only when the signedness allows, which this does not track). Pins in `ZigStdHelperShapesTests`; oracle `declared_width_of_captures_and_arithmetic`; real-std differential `Dotcc_matches_zig_std_fmt_computed_ints` |
| Zig string escapes decoded by zig's rules (task #134) | ✅ | Zig's `\xNN` is exactly two hex digits; the shared (C) string decoder's `\x` takes every hex digit that follows, so `"ab\x00cd"` had decoded as `a b 0xCD` (a silent miscompile). Each zig `\xNN` and each UTF-8 byte of a `\u{NNNN}` reaches the decoder as a three-digit octal escape, unambiguous whatever follows; a `\x` with fewer than two hex digits is rejected, as zig rejects it. Pin in `ZigStdHelperShapesTests`; oracle `zig_hex_escapes_two_digits` |
| `std.mem.sliceTo` (task #127) | ✅ | Curated like `asBytes`: the source's return type is reified through `@Pointer(.slice, attrs, Elem, sentinel)`. An array pointer (mutability kept) or a string literal is scanned within its length, as is a slice; a many-item or C pointer is scanned up to `end`, its sentinel. The result's own sentinel is erased, as dotcc erases sentinels. Pin in `ZigStdHelperShapesTests`; real-std differential `Dotcc_matches_zig_std_slice_to` |
| A value-position `inline for … else` (task #131) | ✅ | `return inline for (field_names, field_values) |f_name, f_value| { if (…) break f_name; } else null;` (std.enums.tagName): two grammar productions (`inlineForElseExpr`, `inlineForMultiElseExpr`) under the value loops; the lowering unrolls over the comptime lists as the statement `inline for` does, and each copy's `break v` fills the result and skips the `else`. A string literal reaching a slice or optional-slice sink through a comptime binding (the captured name) is coerced like a spelled one. Pin in `ZigStdHelperShapesTests`; oracle `inline_for_value_loop`; real-std differential `Dotcc_matches_zig_std_enums_tag_name` |
| `@alignCast` passes its result type through (task #129) | ✅ (real std.fmt.count == zig) | `@alignCast` only raises an alignment requirement, the identity in dotcc, so a result type it is given is its operand's: `const d: *Discarding = @alignCast(@fieldParentPtr("writer", w));` (std.Io.Writer.Discarding, behind std.fmt.count) names the parent. Pin in `ZigStdHelperShapesTests`; oracle `align_cast_field_parent_ptr`; real-std differential `Dotcc_matches_zig_std_fmt_count` |
| std.DynamicBitSet from real std (task #130) | ✅ (real std == zig) | bit_set.zig's `Dynamic` now parses and runs: a labeled block STATEMENT (`if (…) realloc: { … break :realloc; }`, grammar `labeledBlockStmt`; `continue :lbl` and `break :lbl v` on one are refused as zig refuses them), the statement `while (c) body else elsebody` (grammar `stmtWhileElse`, the for-else's flag lowering), a decl literal behind `try` (`.unmanaged = try .initEmpty(…)`), an array container `var` as a pinned global (lowered on demand in a lazy module), an untyped sibling const read at its reader's result type, `a[1..2]` with comptime bounds (zig's `*[1]T`) at a many-pointer, and `x catch unreachable;` over a `!void`. Pins in `ZigStdHelperShapesTests`; oracles `labeled_block_statement`, `while_else_statement`, `decl_literal_through_try`, `container_array_var`, `catch_unreachable_void`; real-std differential `Dotcc_matches_zig_std_dynamic_bit_set` |
| `std.fmt.comptimePrint` (task #128) | ✅ (real std == zig, byte for byte) | Formatted while lowering, since its `*const [count(fmt, args):0]u8` return type needs zig's formatter run before the call can bind: the call becomes the string literal it produces. Integers (`{}` `{d}` `{x}` `{X}` `{b}` `{o}` `{c}` `{u}`, a signed typed one showing `+` under a width), bools, strings (`{s}`), positional (`{1s}`) and named (`{[a]d}`) arguments, `{{`/`}}`, and `[fill][alignment][width][.precision]` (a byte width, right-aligned by default, the precision ignored as zig ignores it for these). A runtime argument, too few or unused arguments, `{}` of a string and a non-ASCII fill are refused as zig refuses them; a float, an enum's `{t}` and other specifiers are loud cuts. Pins in `ZigStdHelperShapesTests`; real-std differential `Dotcc_matches_zig_std_fmt_comptime_print` |
| std.MultiArrayList from real std (task #108) | ✅ (real std == zig) | append, set, get, items, slice, swapRemove, orderedRemove, pop over a user struct. It took: `@typeInfo(T).@"struct".field_attrs` (a list whose `.@"align"` folds to the spelled `align(N)` or null; a defaulted field's `default_value_ptr` stays a loud cut); an anonymous struct type for an untyped `.{ .f = v }` (the unannotated comptime `sizes` block); `std.mem.Alignment` in the comptime interpreter and `mem.Alignment.fromByteUnits(n)` spelled through the type; `inline for` over arrays (by value or `*`) in lockstep with comptime lists; an enum `const` folded as a comptime argument; a `comptime_int` block const kept a literal; an `and`/`or` settled by any operand skipping its dead side; the exact `@bitSizeOf` of a struct of uniform scalars; and a static's fixed-buffer array field decaying through Unsafe.AsPointer. Pins in `ZigStdHelperShapesTests`; oracles `anon_struct_literal`, `comptime_int_block_const`, `inline_for_arrays_and_lists`; real-std differential `Dotcc_matches_zig_std_multi_array_list` |
| std.AutoArrayHashMapUnmanaged / StringArrayHashMapUnmanaged from real std (task #135) | ✅ (real std == zig) | put, get, getOrPut, contains, keys, values, swapRemove, orderedRemove, past the linear-scan limit. It took: a reified instance's nested struct registered on first demand (a type const `MultiArrayList(Data)` reads its fields first) and its type consts resolvable on demand, from any module; `IndexHeader.alloc(…)` as a static call, not an allocator method; a lazy module's labeled-block const evaluated once into a static; `@sizeOf`/`@bitSizeOf` of `void` as 0; a comptime bool seed folding a value `if`; `void == void` as true; zero-size stores and loads (a `void` field, a `void`-as-data element through a pointer zig leaves `undefined`) as no-ops; an exhaustive returning enum switch given an unreachable default for C#. Pins in `ZigStdHelperShapesTests`; oracle `void_fields_and_static_alloc`; real-std differentials `Dotcc_matches_zig_std_auto_array_hash_map`, `Dotcc_matches_zig_std_string_array_hash_map` |
| Unsigned `%` / `/` over an element, field or deref read (task #136) | ✅ | `(out[i] & 0xff) % 10`, `(e[1] - '0') % 10`, `p.n % 10`, `b.n / 7` with unsigned operands are accepted; the read keeps its declared zig type instead of the C#-promoted `int`, so a signed read is still refused like zig. Pins in `ZigStdHelperShapesTests`; oracle `unsigned_div_mod_reads` |
| std.mem.bytesAsSlice (task #137) | ✅ (== zig) | Curated like `bytesAsValue`: `[]T` over a pointer to a byte array or a byte slice, `bytes.len / @sizeOf(T)` long, const when the bytes are, empty for a zero-size `T`. A side-effecting slice operand (a call) is evaluated once here and in `sliceAsBytes`. Pins in `ZigStdHelperShapesTests`; real-std differentials `Dotcc_matches_zig_std_bytes_as_slice`, `Dotcc_matches_zig_std_byte_views_evaluate_once` |
| A `u64` comptime argument past `i64` (task #139) | ✅ | `Fnv1a(u64, 0x100000001b3, 0xcbf29ce484222325)` (std.hash.Fnv1a_64 from real std == zig): the interpreter reads the argument, the instance is keyed by its unsigned value, and every read in the body spells it back unsigned. Pin in `ZigStdHelperShapesTests`; oracle `wide_u64_comptime_args`; real-std differential `Dotcc_matches_zig_std_fnv1a_64` |
| Curated std.ArrayList `insertSlice` (task #138) | ✅ (== zig) | `list.insertSlice(gpa, i, items)` moves the tail up by `items.len` and copies the items in, growing through the allocator; `error.OutOfMemory` on exhaustion. The slice must not alias the list, as in zig. Pin in `ZigStdHelperShapesTests`; real-std differential `Dotcc_matches_zig_std_array_list_insert_slice` |
| Curated std.ArrayList grows like zig (task #145) | ✅ (== zig) | `growCapacity(n) = n +| (n/2 + cache_line / @sizeOf(T))` (cache_line 128 on x86_64 / aarch64), `ensureTotalCapacity` only past capacity, the precise step remapping in place before allocating afresh, and `insert` / `insertSlice` copying head and tail around a fresh block as zig's `addManyAt` does. Capacity is observable through a bounded allocator's out-of-memory point, so the numbers match zig's (33, 83, 135, 336, 20, 76 in the differential). Runtime tests in `ZigListRuntimeTests`; real-std differentials `Dotcc_matches_zig_std_array_list_capacity`, `Dotcc_matches_zig_std_array_list_fba_oom` |
| Else-less `if` switch prong over an expression (task #141) | ✅ | `.one => if (c) @compileError("…"),` and `1 => if (c) f(x),`: a comptime subject keeps the prong's expression or nothing (an untaken `@compileError` never fires), a runtime subject gets an else-less `if`. Unblocks parsing std.mem.ReverseIterator. Pins in `ZigStdHelperShapesTests`; oracle `else_less_if_prongs` |
| `catch \|e\| return if (c) a else b` (task #146) | ✅ | The condition, then one of two returns; a comptime-known condition keeps one. The arms are operator expressions (a `catch` / `orelse` inside an arm needs parentheses). Pin in `ZigStdHelperShapesTests`; oracle `catch_return_if` |
| std.crypto.blake3 shapes (task #140, part 1) | ✅ | A field extent reads a const of a struct declared later in the file (pass 0a2 records every struct's consts before any field layout); `@intCast` / `@truncate` slice bounds and indexes lower at their `usize` result location; `p[lo..]` over a many-item pointer is the advanced pointer (a single-item one is refused, as zig does); `const t: [N]T = p.*;` copies through a pointer to an array; a lazy module's `const a = b;` value alias is followed. Blake3 itself still stops at its SIMD path (`math.rotr` over a `@Vector`, task #148). Pins in `ZigStdHelperShapesTests`; oracle `blake3_shapes` |
| std.math.sign from real std (task #143) | ✅ (real std == zig) | `@abs` of a compile-time-known integer folds to its magnitude (a comptime_int stays one), so IntFittingRange's capture switch over `@max(@abs(from) - 1, to)` folds; `@intFromFloat` of an integer and `@floatFromInt` of a float are refused as zig refuses them. Pins in `ZigStdHelperShapesTests`; oracle `comptime_abs_capture_switch`; real-std differential `Dotcc_matches_zig_std_math_sign` |
| std.unicode.utf8ToUtf16Le from real std (task #144) | ✅ (real std == zig) | `std.simd.suggestVectorLength(u16) orelse break :vectorized` over the value dotcc folds binds a comptime `const` that sizes a `@Vector`; `dest[i..][0..N].* = v` stores a vector's lanes and `.{ a, b }` lowers at the viewed array; an integer vector widens lane by lane at a wider vector sink; `@byteSwap` of a vector swaps each lane. Pin in `ZigStdHelperShapesTests`; oracle `vector_store_and_widen`; real-std differential `Dotcc_matches_zig_std_utf8_to_utf16le` |
| std.meta.Tag / activeTag from real std (task #142) | ✅ (real std == zig) | A tagged union's `@typeInfo(U).@"union".tag_type` is its tag enum; `opt orelse fallback` in a type position takes the fallback for an untagged union (whose `@typeInfo` is now `.@"union"`), so its `@compileError` fires; a `@compileError` in any type position fires; a tagged union at its tag enum's sink is its active tag. Pins in `ZigStdHelperShapesTests`; oracle `union_tag_type`; real-std differential `Dotcc_matches_zig_std_meta_active_tag` |
| std.mem.reverseIterator from real std (task #147) | ✅ (real std == zig) | A switch that yields a `@typeInfo` payload (`.pointer => |p| p`) binds it as a const; `switch (ptr.size)` folds in a type body, with an else-less `if … @compileError` prong; `@Pointer(size, attrs, child, sentinel)` reifies a pointer or slice type (const from `ptr.attrs` or a literal, the sentinel erased); a pointer to an array is `.one` when no spelling says otherwise; `.ptr` of a `*[N]T` is its element pointer. Pins in `ZigStdHelperShapesTests`; oracle `ptr_len_through_array_pointer`; differentials `Dotcc_matches_zig_reverse_iterator_shape` (local-only: `@Pointer` / `attrs` are 0.17-dev), `Dotcc_matches_zig_std_mem_reverse_iterator` |
| std.math.rotr / rotl over a vector from real std (task #148) | ✅ (real std == zig) | `@typeInfo(T).vector.child` and a `@Vector(N, T)` type argument carry the lane type's declared width (`.int.bits` of a `u32` lane is 32); a vector shifted by a `@splat(n)` amount shifts every lane by the one scalar count, as zig's `Log2Int` result type fills it. A `@splat` operand of any other operator, or beside a scalar shift, is refused as zig refuses it ("@splat must have a known result type"); a per-lane count vector is not lowered yet. Pins in `ZigStdHelperShapesTests`; oracle `vector_rotate_by_splat`; real-std differential `Dotcc_matches_zig_std_math_rotr_vector`. Blake3 itself next stops at `?[N]T` (task #151) |
| A user function returning `?comptime_int` (task #149) | ✅ | A call to it is evaluated at compile time, as zig evaluates any call with a comptime-only result: `pick(T) orelse 0` is the payload or the fallback, and `const n = pick(T) orelse break :blk;` binds a comptime-known `n` that can size a `@Vector(n, u8)`, or, when the call is null, jumps at compile time so the rest of the block (which zig never analyses) is not lowered. Vector comparisons, `@select`, `@reduce` and lane reads now cover 64-bit and 512-bit vectors too. Pins in `ZigStdHelperShapesTests`; oracles `comptime_optional_int_orelse_break`, `vector_widths_select_reduce` |
| A pointer type argument's size class inside a generic (task #150) | ✅ | `*T`, `[*]T` and `[*c]T` lower to one C pointer, so the class the argument SPELLED rides with the type seed: `@typeInfo(T).pointer.size` folds in a generic function body, in a type-returning generic's body and in its struct's consts, and `const s = …pointer.size;` binds the tag. `*T` and `[*]T` key distinct instances (a `_many` / `_c` suffix; a single-item pointer keeps its name). `std.mem.ReverseIterator([*]const u8)` now reaches its own `@compileError("bad size")`, as zig does. Pins in `ZigStdHelperShapesTests`; oracle `pointer_size_class_in_generics` |
| Optional arrays `?[N]T` (task #151) | ✅ | An array lowers to a pointer and `T*?` is no C# type, so `?[N]T` is a generated value type per element and length: the elements in a `fixed` buffer beside a has-value flag, with `Nullable`'s `HasValue` / `Value` surface (`Value` is the element pointer), a copying conversion from `T*` (a null one is none, so `= null` needs no rewrite) and `== null`. Fields with a `null` default, locals, parameters, returns, `.?`, capture `if`s and `orelse` work. Cuts: an element C# cannot hold in a `fixed` buffer (a struct), and `orelse` over a call (it would read the optional twice). Pins in `ZigStdHelperShapesTests`; oracles `optional_array_field_and_local`, `optional_array_return_and_orelse`, `optional_array_orelse_copies` (an `orelse` result is copied into its own storage, as any array value is). Blake3 next stops at `[][N]T` (task #152) |
| Multi-dimensional arrays and slices of arrays `[][N]T` (task #152) | ✅ | A `[M][N]T` is one flat run of `T`, as it is indexed (`g[i][j]` is `(g + i * N)[j]`): a nested literal splices its rows into that run (a row naming an array is read element by element), and copying a sub-array copies its whole flat run; an `undefined` one zero-fills the run, and assigning a whole row (`m[i] = row`) copies its elements (task #154, oracle `multi_dimensional_undefined_and_row_assign`). A slice of arrays is a slice of the flat element whose `.len` counts rows (a pointer is no C# type argument): slicing advances whole rows and a `|*r|` capture is the row pointer. Cut: a literal row that is a call. Pins in `ZigStdHelperShapesTests`; oracles `multi_dimensional_array_literal`, `slice_of_arrays`, `three_dimensional_array`. Blake3 next stops at `[][*]const u8` (task #153) |
| Slices of pointers `[]*T` / `[][*]T` (task #153) | ✅ | C# forbids a pointer type argument, so a slice of pointers is the runtime's `PtrSlice<T>` / `ConstPtrSlice<T>` over the pointees: the same `{ ptr, len }` fat pointer with `.Ptr` a `T**`, so indexing, slicing, iteration and `&arr` spell as for any slice. A pointer to a pointer, `*anyopaque` or a function-pointer element is not covered. Runtime test in `SliceRuntimeTests`; pin in `ZigStdHelperShapesTests`; oracle `slice_of_pointers` |
| Vector lane stores `v[i] = x` (task #155) | ✅ | A .NET vector is immutable, so a store to one lane assigns back the vector with that lane replaced (`v = ZigVec.With(v, i, x)`), into a variable, field, array element (`vecs[i][j] = …`) or dereference; `v[i] op= x` combines with the lane first. As in zig, the index must be comptime-known (an `inline for` capture is): a runtime one is refused with zig's "vector index not comptime known" (a runtime-index READ is fine). A bool vector's lane store is not lowered yet. Pins in `ZigStdHelperShapesTests`; oracle `vector_lane_store` |
| Packed `bool` fields and std.crypto.hash.Blake3 from real std (task #156) | ✅ (real std == zig) | A `bool` is ONE bit of a `packed struct`, as C's `_Bool b : 1` (it was a whole CBool field, so `packed struct(u8)` of bools was 8 bytes and its `@bitCast` threw); the IR layout model packs bit-fields into storage units as the backend does, so `@sizeOf` agrees; a `@bitCast` of an integer operand C# widened (`u8 | u8` is an `int`) is narrowed back to the destination's size; `@intFromBool` of a runtime helper's C# `bool` picks 1 or 0. With tasks #148 and #151..#155, **std.crypto.hash.Blake3 runs from real std == zig** (plain, keyed, incremental). Pin in `ZigStdHelperShapesTests`; oracle `packed_struct_bool_bits`; real-std differential `Dotcc_matches_zig_std_crypto_blake3` |
| Reject mixed-signedness peer arithmetic (task #158) | ✅ | zig peer-resolves an arithmetic or bitwise operator's operands, and a signed and an unsigned runtime integer combine only when the signed type holds every unsigned value: `i32 + u8` is an `i32`, but `i16 + usize` and `i16 - u16` are "incompatible types", and so are they in dotcc. Comparisons (exact in zig), shifts and comptime-known operands are not affected. The check only fires where both zig types are certain (parameters, elements, `@as`, 64-bit fields), so an inferred local, whose lowered C# type may differ from its zig one, is let through. Pins in `ZigStdHelperShapesTests`; oracle `mixed_signedness_allowed_peers` |
| std.crypto.auth.siphash from real std (task #159) | ✅ (real std == zig) | A method named like an allocator's (`create`, `alloc`, `free`, …) on a type a type-returning call builds (`SipHash64(2, 4).create(&out, msg, &key)`) is that type's function, not an allocator call on a value; `const blob = b[off..][0..8].*;` binds an ARRAY copy (`[8]u8` with its own storage) where the slice's length is comptime-known, so it passes to an array parameter and a `var` bound so no longer writes through to the source. Pins in `ZigStdHelperShapesTests`; oracles `type_call_create_method`, `slice_deref_array_copy`; real-std differential `Dotcc_matches_zig_std_crypto_siphash` |
| std.crypto.hash.sha2.Sha512 from real std (task #160) | ✅ (real std == zig) | A `catch` over an error union carrying a `comptime_int` (`pub const digest_length = std.math.divCeil(comptime_int, digest_bits, 8) catch unreachable;`) is evaluated while lowering, as `comptime f() catch unreachable` is: a `comptime_int` has no runtime form. The const is then the literal it folds to, so it sizes an array parameter (`out: *[digest_length]u8`) and is no longer a runtime catch hoisted into a type. Pin in `ZigStdHelperShapesTests`; oracle `comptime_int_catch_container_const`; real-std differential `Dotcc_matches_zig_std_crypto_sha512` |
| Type-body enums and mode-selected structs, `volatile` pointees (task #161) | ✅ | A type-returning body may declare its own `const Op = enum {…};` (or union) and choose a struct by a comptime condition (`const Tracker = if (mode == .Debug) struct {…} else struct {…};`, std.crypto.keccak_p's State): both are registered per instance, and the chosen struct's methods are declared with the instance's seeds. A module-level `const mode = @import("builtin").mode;` folds in conditions and emits no runtime global (a bare enum literal is comptime-only). A container nested in an instance sees the instance's seeds (sha3's `Options = struct { delim: u8 = default_delim }`). `volatile` on a pointee (`[]volatile T`, `*volatile T`) parses and lowers like the plain pointer; anywhere else it is rejected. std.crypto.hash.sha3 is next blocked on a comptime value seed's declared width (task #163). Pins in `ZigStdHelperShapesTests`; oracles `type_body_enum_and_selected_struct`, `nested_container_default_reads_seed`, `volatile_pointees` |
| Comptime value seeds keep their declared type (task #163) | ✅ | A `comptime f: u11` seed carries its declared width, and an `anytype` argument is zig's type rather than C's promoted `int`: `f / 25` is a `u11` (an integer literal adopts its peer's type, a shift keeps its left operand's) and a bare seed is its parameter's type, so `@typeInfo(@TypeOf(x)).int` answers zig's bits and signedness. A type argument chosen by a switch over a comptime tag (`switch (int_info.signedness) { .signed => @Int(…), .unsigned => T }` in std.math.log2) keeps the selected prong's width. std.crypto.hash.sha3 is next blocked on KeccakF's `block_bytes` (task #164). Pins in `ZigStdHelperShapesTests`; oracles `comptime_seed_anytype_types`, `log2_over_a_seeded_u11` |
| std.crypto.hash.sha3 from real std (task #164) | ✅ (real std == zig) | An instance reified while ANOTHER container's const lowers (keccak_p's State lays out `buf: [rate]u8` with `rate = KeccakF(f).block_bytes - capacity / 8`, which reifies KeccakF) resolves its own members in itself: its `init(bytes: [block_bytes]u8)` had looked `block_bytes` up in State and failed. With #161 and #163 this runs Sha3_256 from real std. Pin in `ZigStdHelperShapesTests`; oracle `reify_inside_other_container_const`; real-std differential `Dotcc_matches_zig_std_crypto_sha3` |
| A global array literal whose address is taken (task #115) | ✅ | `const direct: []const u8 = &[_]u8{ 1, 2, 3 };` and a global struct literal's `.vals = &[_]u8{ 7, 9 }` pin the array for the program's life (`Libc.GlobalArrayFrom`); a `stackalloc` in a static initializer did not build (C# CS1503). Pin in `ZigStdHelperShapesTests`; oracle `global_array_literal_address` |
| `@clz` / `@ctz` of an arbitrary-width integer (task #101) | ✅ | counted within the declared width, not the carrier's: `@clz` subtracts the carrier's extra bits and `@ctz` of zero is the declared width. **Silent miscompile fixed:** `@clz(@as(u3, 1))` had been 7 (zig 2). Still lenient: the count is an `int`, where zig types it `Log2IntCeil(T)` (task #102). Pin in `ZigStdHelperShapesTests`; oracle `clz_ctz_arbitrary_width` |
| The `@clz` / `@ctz` / `@popCount` count type (task #102) | ✅ | an unsigned `std.math.Log2IntCeil(T)` (a `u3`'s count is a `u2`, a `u64`'s a `u7`), so a literal peer it cannot hold is rejected with zig's message (`@clz(s) * 100` over a `u3`: "type 'u2' cannot represent integer value '100'") and one it can (`@clz(s) + 1`) is accepted. Pins in `ZigStdHelperShapesTests`; oracle `clz_count_is_log2_int_ceil` |
| container **method** `fn m(self: P, …) …` | ✅ | a `fn`/`pub fn` in a struct, **enum, or union** body lowers to a free function `P_m` with the receiver as its first parameter. `p.m(a)` (UFCS) auto-refs/derefs the receiver to the declared `self` form (`&p` for a `*P` receiver, `p.*` for the reverse); `P.m(p, a)` and a no-receiver `P.assoc(a)` (associated function) call it directly. An enum receiver dispatches the same way; `self == .member` (enum equality with a result-located literal) is supported. **Deferred:** generic methods |
| more std syntax: member `comptime { … }` blocks, BLOCK continue expressions, `orelse return <identifier>` (road-to-zig-std S9/G5) | ✅ | measured on `hash_map.zig`'s `Custom` (behind `std.AutoHashMap`): a `comptime { … }` block as a struct / enum / union MEMBER (126 in std), analysis-only and dropped like the top-level form; `while (c) : ({ a += 1; b += 1; }) body` (17), lowered to the C# for-update list `for (; c; a += 1, b += 1)` (each statement of the block must be one assignment or call); and `x orelse return err` / `catch return err`, which failed at `err` although `orelse return 0` worked: a struct field default may be followed directly by the next member, so an identifier can follow an expression, and LALR.CC's group-order precedence let the VOID form's reduce win that state. The void forms now rank last, so the value form takes the identifier. Oracle `hash_map_parse_bricks`; unit `ZigStdParseBricksTests` |
| `std.array_list.Aligned(T, null)` and the typed `builtin.cpu` from real std (road-to-zig-std, target-identity T3) | ✅ | With `DOTCC_ZIG_LIB_DIR`, `builtin.cpu` is a typed `std.Target.Cpu` for this host: the x86-64 level it reaches (`std.Target.x86.cpu.x86_64_v3`, …) and the features .NET's `System.Runtime.Intrinsics` report, as a `std.Target.x86.featureSet(&.{ … })` (aarch64: `generic` and the `Arm` intrinsics, unverified on an arm64 host). std's own code answers comptime questions over it: `comptime std.atomic.cacheLineForCpu(builtin.cpu)`, `comptime builtin.cpu.has(.x86, .sse2)`, `std.simd.suggestVectorLength(u8)` (T4, through a comptime STRUCT generic parameter; its value is this host's, as zig's native detection gives). The list then runs from source (`.empty`, `append` through `page_allocator`, `deinit`). Real-std differentials `Dotcc_matches_zig_std_builtin_cpu_features_from_source`, `Dotcc_matches_zig_std_array_list_from_source`; unit `ZigSyntheticBuiltinTargetTests`. **Cuts:** `builtin.cpu` read as a runtime value (see docs/plans/deferred.md), and `comptime a or b` covers only `a` (parenthesize) |
| TYPE const members of a reified struct (road-to-zig-std G4/G5) | ✅ | `pub const Slice = if (alignment) |a| ([]align(…) T) else []T;` (`array_list.Aligned`) and `pub const Unmanaged = HashMapUnmanaged(K, V, …);` (`HashMap`): a member const whose RHS is a type (a type former, a type-returning call, a type name, or an `if` whose then-arm is one) is evaluated when the struct is reified, with the instantiation's comptime seeds live, into an alias scoped to that instance, so a field typed by it resolves (member order does not matter, as in zig). A VALUE const member keeps its lazy lowering. Oracle `reified_type_consts`; unit `ZigReifiedTypeConstTests`. **Cut:** a VALUE const or field default that reads a comptime parameter (`pub const max = if (cap) |n| n else 0;`, `n: u8 = n`), which lowers at its use site after the seeds are gone |
| std's everyday syntax: `align(E)` pointer / slice types, general sentinels, trailing call commas, `inline fn` (road-to-zig-std S9) | ✅ | the parse gaps that kept `std.array_list.Aligned` (what `std.ArrayList` is) from parsing, each measured on the pin: `*align(E) [const] T`, `[*]align(E) [const] T`, `[]align(E) [const] T` (548 of std's 561 `align` types; alignment is not tracked, since C# pointers carry none, as on a declaration); a GENERAL sentinel `[:s]T`, `[:s]align(E) T`, `[*:s]T` (77) and sentinel slicing `a[lo .. hi :s]` / `a[lo .. :s]` (73), the sentinel erased in the type as `[:0]`'s is (zig checks `a[hi] == s` only in a safe mode, and dotcc reports `.ReleaseFast`); a trailing comma in a call's arguments (zig fmt's layout for every multi-line call); `inline fn` / `pub inline fn` at top level and in struct / enum / union bodies (379), an optimizer hint lowered as an ordinary function. Oracle `std_parse_bricks`; unit `ZigStdParseBricksTests`. **Cut:** a general sentinel on an ARRAY type (`[N:s]T` keeps its `0`-only form), and zig's comptime propagation through an `inline` call (the comptime-call engine's job) |
| the void value `{}` / `void` parameters and storage (road-to-zig-std S9) | ✅ | `{}` as a call argument (108 in std, the `context: void` of `std.sort`) and at a right-hand side (`x = {};`, `return {};`, `const v = {};`). zig's void is zero-sized with no runtime value, and C# has no void parameter, local or `default(void)`, so the emit ERASES it, driven by the type: a void parameter leaves the C# signature, every call's arguments and `delegate*` types (arity still lines up), a void local and a store into one emit nothing, `_ = ctx;` emits nothing, and `return {};` is `return;` (or the `!void` success value). A void parameter reached through `anytype` erases the same way. Oracle `void_value`; unit `ZigVoidValueTests`. **Cut (loud):** a SIDE-EFFECTING argument to a void parameter (`f(g(), …)` with `g` returning void), which erasure would silently drop |
| anonymous `enum { a, b }` type (road-to-zig-std S9) | ✅ | an enum TYPE spelled inline in a type slot, std's `parseIntWithSign(…, comptime sign: enum { pos, neg })`. Reified once per source site (`__AnonEnum<n>`, module-qualified in an import), so every instance of a generic whose parameter spells it shares one type; a bare `.pos` argument to such a `comptime` parameter resolves against it. Oracle `comptime_if_anon_enum`; unit `ZigComptimeIfTests`. **Cut:** a method or `const` member (use a named `const E = enum {…};`) |
| decl literal `.init(…)` / `.name` (zig 0.14+; road-to-zig-std G3) | ✅ | a `.name(args)` / `.name()` CALL or a bare `.name` VALUE at a struct/union result location names a declaration of the RESULT type — `const w: Writer = .fixed(buf);` IS `Writer.fixed(buf)`, `const z: Counter = .zero;` IS `Counter.zero` — the same lookup an enum literal's tag gets, at every sink (a typed `var`/`const`, a parameter, a `return`). The call resolves through the shared method registry, so a container ANOTHER module declares works (`const p: geom.Point = .init(40, 2);`, lowered in `geom`). A decl-literal call with no result type is rejected (zig rejects it too). Oracle `decl_literals` / `import_decl_literal`; unit `ZigDeclLiteralTests`. A decl-literal VALUE of a container another module declares (`var list: std.array_list.Aligned(u8, null) = .empty;`) is lowered by that module (`ContainerConstOwners`), and every container const is evaluated in its container's scope (`pub const empty: Self = …`); oracle `cross_module_decl_literal`, unit `ZigCrossModuleDeclLiteralTests`. `&.{}` / `&[_]T{}` at a slice sink is the empty slice, and a bare call to a sibling container function (`self.* = init(a);`) is `Self.init(a)`. |
| namespaced value `const NAME = …;` | ✅ | a container-level `const` (a comptime value) read as `Type.NAME` in any of struct/enum/union. dotcc **inlines** the (lazily re-lowered) RHS at each use site — `const max: u8 = 42;` → `Cfg.max`, `const default = Color.blue;` → `Color.default`. A const RHS may reference a *sibling* const by **bare (unqualified) name** (Milestone R, part 6 — `const doubled = base * 2;`); a dependency cycle errors cleanly |
| namespaced mutable `var NAME = …;` | ✅ | a container-level `var` (Milestone R, part 6) — a namespaced mutable global, lowered to a real `public static` field of `DotCcGlobals` under a mangled `Container_NAME`. `Type.NAME` reads/writes it (an lvalue: `Cfg.counter = …` / `+= …`). The init may reference a sibling const by bare name. **V1: scalar only** (an array/aggregate container var is rejected) |
| receiver type `self: @This()` | ✅ | `@This()` resolves to the enclosing container type (so `self: @This()` / `self: *@This()` name the receiver without repeating the name); explicit `self: P` / `self: *P` also work |
| self-type alias `const Self = @This();` | ✅ | a container-level `const` aliasing the container's own type inside its methods (the ubiquitous Zig idiom; any alias name). Resolves as a param/return/local type (`self: Self`, `… ) Self`), the base of a static call (`Self.init(…)`), and a typed literal (`Self{…}`) — all to the container, scoped per-container (two containers may each declare `const Self = @This();`). A **non-`@This()`** container `const` is a namespaced value constant (supported — see the row above), distinct from this self-type alias |
| type-as-value alias `const T = <type>;` (wall-plan W1) | ✅ | Zig's "types are values": a `const` binds a NAME to a TYPE, resolved in any type position (`var x: T = …;`, a param/return/field). The RHS already parses via the `CurlySuffix → Type` value path (no grammar change); recognized in lowering and recorded as a function-flat type alias with **no runtime decl**. A type prefix composes over the alias for free (`const P = *T;` → `int*`, `?T`, `[]T`, `[N]T`) — including alias-of-alias. A curated `std` type (`const List = std.ArrayList(i32);`, `const A = std.mem.Allocator;`) can be aliased too. Works at top level (source order — a forward struct ref is a V1 cut) and locally in a body (the monomorphization-shaped `const T = @TypeOf(a);`). Oracle `type-value`; example `examples/zig-type-values/`; unit `ZigTypeValueTests`. **Cut:** `type` as a runtime `var`/param type is rejected loudly (comptime-only — a `comptime` param is W3) |
| `@TypeOf(expr)` (wall-plan W1) | ✅ | the operand's synthesized `CType`, UNEVALUATED (the operand lowers into a throwaway hoist buffer, so no side effect reaches the body). Usable in a type position (`var y: @TypeOf(x) = …;`, a param/return type) and as a `const` type alias (`const T = @TypeOf(x);`) |
| generic fn — `comptime` VALUE param (wall-plan W3a) | ✅ | `fn addN(comptime N: i32, x: i32) i32` — a `comptime`-value parameter makes the function a TEMPLATE (no runtime storage for the param). A call **monomorphizes**: the comptime arg is `ConstEval`-folded, and a SPECIALIZED body is emitted per resolved value under a mangled name (`addN__10`, `addN__100`) — memoized so a repeat call reuses it, the value baked into the body as a literal. Bodies lower from a re-entrancy-safe worklist drained after pass 2. A comptime-known `if` inside an instance folds to its taken branch (Zig comptime-if), so a RECURSIVE generic (`fn fib(comptime n: u32) …`) prunes its base case and terminates. A comptime **OPTIONAL** value param `comptime opt: ?T` (road-to-zig-std S4b) is also supported: the arg is a comptime `null` or a comptime-known payload (mangled `__optnull` / `__opt<v>`), and a captured `if (opt) \|x\| … else …` on it FOLDS at instantiation — `null` → the else, a payload → the then with `x` bound to the literal (in both value and statement position, no runtime optional test). This is the user-generic analog of `std.ArrayList`'s `Aligned(T, alignment)` — the parameter shape its `Slice` selection uses. Oracle `comptime-param` / `comptime_optional_fold`; examples `examples/zig-comptime-param/`, `examples/zig-comptime-optional/`; unit `ZigComptimeParamTests`. A named container-level integer `const` is a comptime argument like a literal (`addN(N, 3)` — the const carries its folded value, as a C23 `constexpr` does). A comptime **STRING** param `comptime fmt: []const u8` (road-to-zig-std G3 — every std formatting entry point's shape) takes a comptime string argument (a literal, a comptime const, a `++`/`**` fold), keys the instance by a digest of its bytes (`lenOf__s<fnv32>`; a collision fails loudly), and binds the name as a comptime string in the body (unit `ZigCrossModuleGenericTests`). A generic METHOD (`fn get(self: S, x: anytype)`, `fn addN(self: S, comptime N: i32)`, hash_map's `fetchRemoveAdapted(self, key: anytype, ctx: anytype)`) is the same template under its container: the receiver fills parameter 0, the instance (`S_addN__10`) is declared under its owner with the owner's comptime seeds live (a `comptime sentinel: T` typed by a reified owner's `T`, spelled in the return type), lowered inside it, and reached from any module (oracle `generic_methods`; unit `ZigGenericMethodTests`). **Cuts (loud):** a value-dependent type in a FREE function (`fn f(comptime n: usize, a: [n]u8)` — a VALUE-only signature lowers at template time); a comptime arg that isn't `ConstEval`-able (a call needs explicit `comptime f()`); a comptime-if in EXPRESSION position (a recursive generic written that way hits the instantiation cap) |
| generic fn — `comptime` TYPE param (wall-plan W3b) | ✅ | `fn maxOf(comptime T: type, a: T, b: T) T` — a `comptime T: type` parameter makes the later parameter/return types DEPEND on the resolved type, so — unlike a VALUE param (W3a) — the SIGNATURE cannot be lowered at template time. Each call resolves the type argument to a concrete type, seeds `T` (shadow-saved so a nested/sibling `T` doesn't collide), and lowers a SPECIALIZED signature + body under a mangled name keyed by the RESOLVED type: `maxOf(i32,…)` → `int maxOf__i32(int,int)`, `maxOf(f64,…)` → `double maxOf__f64(double,double)`. An alias for a type keys the SAME instance (`const I = i32; maxOf(I,…)` ≡ `maxOf(i32,…)`). `T` resolves inside the body too (`@sizeOf(T)`, a local `var x: T`, a cast). Reuses the W3a worklist + memoization. Oracle `comptime-type-param`; example `examples/zig-comptime-type-param/`; unit `ZigComptimeTypeParamTests`. A generic METHOD instantiates as in the W3a row. **Cuts (loud):** a comptime `if (T == i32)` type-comparison in the body (needs interpreter type values); a type-FORMER type argument (`maxOf([]u8, …)` — doesn't parse in argument position yet) |
| std.hash's auto-hash forms (road-to-zig-std G4) | ✅ | An `if` statement whose then arm is a bare `return x` before `else` (`if (c) return a else return b;`, with a capture and an `else \|e\|`); `inline` prongs and `\|val, tag\|` captures (parsed; a comptime-selected switch takes one prong); `.undefined` / `.null` enum literals; a no-`else` capture `if` as a prong body (parsed; a loud cut if comptime-selected); a comptime TYPE question (`comptime isSlice(T)`, or a plain `hasUniqueRepresentation(T)`, a generic of types only with one `return`) folding, also through another module, so the branch it guards is never analysed; `const H = switch (@typeInfo(T)) { … }` choosing a type; `@call(modifier, f, .{ … })` as `f(…)`; `@divExact` (and `@divTrunc`/`@divFloor`/`@rem`/`@mod` folding over constants, so a quotient sizes an array); `&arr` of a local or member array as its `*[N]T` (the array expression itself), sliced open-ended with length N; a plain value at an error-union argument as its success variant; the curated `std.mem.asBytes(ptr)` (a byte slice over the item, where zig says `*[N]u8`); and a module re-export (`pub const builtin = lang;`, so `std.builtin.Endian` is lang.zig's). Oracle `hash_path_forms`; unit `ZigHashPathTests` |
| container const bound to a FUNCTION value, called as a method (road-to-zig-std G4) | ✅ | hash_map's `AutoContext(K)`: `pub const hash = getAutoHashFn(K, @This());`, a closure-idiom function value, then `ctx.hash(key)` / `Context.hash(ctx, key)`. The const is resolved by the module holding it, in the container's scope with its comptime seeds live (memoized), and called with the receiver as parameter 0. A closure-idiom body may open with `comptime { … }` guards, which run: an `if` over a comptime-known condition, a `@compileError` in the taken branch (raised, as zig does, only when the function is referenced), and `assert(c)` (checked when `c` folds). Oracle `container_fn_consts_and_hints`; unit `ZigFnConstAndHintTests` |
| type-RETURNING function (wall-plan W4) | ✅ | `fn Pair(comptime T: type) type { return struct { a: T, b: T }; }` — a COMPTIME type constructor (emits no runtime code). Each use in a TYPE position (`const p: Pair(i32)`, a param/field, a typed literal `Pair(i32){…}`) or a type alias (`const P = Pair(i32);`) REIFIES a fresh struct per resolved type argument (`Pair__i32`, `Pair__f64`), memoized so the same argument reuses one struct — keyed by the RESOLVED type (`const I=i32; Pair(I)` ≡ `Pair(i32)`). A field typed `T` gets the concrete type; `?*@This()` becomes a self-pointer (the mapping is installed before the fields lower, so a self-referential / recursive type resolves). Reuses the W2 on-the-fly struct registration (mangled name, shadow-saved type-param seed). No grammar change beyond `return struct {…}` (a `struct` is not an `RhsExpr`, so `return struct` is a conflict-free 1-token shift). **A comptime VALUE / OPTIONAL parameter is now supported (road-to-zig-std S4b pt2 / S4c):** `fn Store(comptime T: type, comptime cap: ?u8) type` keys its instance by the resolved value too (`Store__u8_3`, `Store__u8_optnull`), and the body may be MULTI-statement — zero or more leading `const NAME = <type>;` type-alias locals (each a scoped alias) before the final `return struct {…}`. A leading alias RHS may be a captured-`if` that FOLDS to a type: `const Slice = if (cap) \|n\| [n]T else []T;` → `[n]T` for a payload `cap` (with `n` bound), `[]T` for `null` — exactly `std.ArrayList`'s `Aligned(T, alignment)` `Slice` selection. **The reified struct carries `const` members and METHODS, not only fields (road-to-zig-std G4 — lifts the fields-only V1 cut):** a `const Self = @This();` registers against the mangled container (so a `*Self` receiver is a pointer to *that* instantiation), a value `const` inlines via `Self.NAME`, and each method is declared under the mangled container — lowering to the same `Container_method` free function an ordinary container's method does, with its BODY deferred to a top-level drain (a reification is triggered from an arbitrary type position, so lowering a body inline would clobber the in-flight per-function state — the same re-entrancy rule as W3a's monomorphization worklist). The drain re-applies the reification's comptime seeds, so the method body's `T` matches its signature's. A receiverless method is reachable **both** through the alias the instantiation is bound to (`const S = Stack(u8,8); S.init()` — a type ALIAS now also serves as a static-call / const base) **and directly off the generic call** (`Stack(u8,8).init()` / `std.ArrayList(u8).init(…)`, the idiomatic spelling) — that's the `ArrayList.init`/`.empty` constructor idiom either way; an ordinary call base (`getBox().get()`) still lowers as an instance method. Sibling method calls resolve within the instantiation, and a reified type may be a FIELD of / returned by another reified type (its methods lower on demand through the same worklist). Oracle `type-returning-fn` / `type_returning_optional_fold` / `generic-container-methods`; examples `examples/zig-type-returning-fn/`, `examples/zig-comptime-type-fold/`, `examples/zig-generic-container/`; unit `ZigTypeReturningFnTests`. **The body is EVALUATED at comptime, not pattern-matched (the W4 lift — road-to-zig-std G4 blocker 2):** statements are walked in order — a `const` binds a TYPE alias or a comptime VALUE (`const bits: u16 = @typeInfo(T).int.bits;`, folded into a comptime symbol), an `if` / captured `if` / STATEMENT `switch` whose condition folds contributes only its taken arm (`else => {}` falls through), a reached `@compileError` raises, a bare `_ = param;` is the unused-param no-op, and the FIRST `return` reached is the result. `return struct {…}` reifies as above; **any other `return <type-expr>` IS the result** — a DELEGATING type-returning call, local or module-qualified (`pub fn ArrayList(comptime T: type) type { return array_list.Aligned(T, null); }` — what `std.ArrayList` is), a folded `return switch (@typeInfo(T)) { .pointer => \|info\| info.child, … }` / `return if (…) A else B`, `@Int(…)`, a bare `T`. A delegating instance reifies NOTHING: it is memoized (under its own mangled name) to the type its body returned, so `List(u8)`, `Aligned(u8, null)` and an early-returning `Aligned(u8, 1)` are one C# struct — exactly zig's type identity. The returned type's declared integer WIDTH rides the call site too (`fn U(comptime n: u16) type { return @Int(.unsigned, n); }` → `@bitSizeOf(U(21))` is 21). Conditions fold over a `comptime` value seed (`n > 255`), a tag / `builtin` query, and a TYPE comparison `T == u8` / `T == comptime_int` (width-aware: `u21 != u32` although both lower to `uint`). A body that depends on its own instance is a loud dependency-loop error. **Real std compiles through it from source:** `std.meta.Child`, `std.meta.Elem`, `std.math.Log2Int` / `Log2IntCeil`, oracle-identical. Oracle `type_body_delegation` + the real-std `Dotcc_matches_zig_std_type_functions_from_source` differential; example `examples/zig-type-body/`; unit `ZigTypeBodyTests`. A `type`-returning METHOD (hash_map's `fn FieldIterator(comptime T: type) type`, `Aligned`'s `SentinelSlice`) is the same template under its container: reached bare inside the container, through a type const (`pub const KeyIterator = FieldIterator(K);`), through the container (`Self.FieldIterator(K)`, `Box(u16).Elem()`), and evaluated in its OWNER's scope with the owner's seeds live, so the struct it returns sees the owner's nested types (`Table__u16_u8_FieldIterator__u16`; oracle `type_returning_methods`; unit `ZigTypeReturningMethodTests`). A container's type const is also named qualified (`Shapes.Bytes`, and through a type call: `Outer(u16, null).Managed`). A reified struct may pass the comptime optional it was instantiated with on to another type call (array_list's `AlignedManaged(T, alignment)`; oracle `comptime_optional_forwarding`). **Cuts (loud):** a generic (`comptime`/`anytype`) METHOD (the standing W3/W5 free-functions-only rule; hash_map's `fetchRemoveAdapted`, array_list's `toOwnedSliceSentinel`); a type-body statement with no comptime meaning (a `var`, a call; `assert(…)` over comptime operands IS evaluated, a false one loud, since std.math.IntFittingRange opens with one); a condition or `switch` subject that does not fold; a block / `return` prong in a VALUE-position type `switch`; a runtime / `anytype` parameter |
| generic fn — `anytype` param (wall-plan W5) | ✅ | `fn add(a: anytype, b: anytype) @TypeOf(a)` — an `anytype` parameter has NO written type: its type is INFERRED from the actual argument (`@TypeOf(arg)`), then keys a specialization like a comptime TYPE param (W3b) — but the argument is ALSO passed at runtime (a HYBRID: monomorphization key AND runtime slot, unlike a comptime TYPE arg which is a compile-time-only type spelling). Each call infers each anytype param's type, mangles by the inferred types (`add__i32_i32` / `add__f64_f64`), and lowers a SPECIALIZED signature + body — the `@TypeOf(param)` return type follows suit, resolving through the inferred seed. The body binds the param as an ordinary runtime symbol of the inferred type, so **duck-typed** use lowers against the concrete type (`p.x` on a struct, `s.len` on a slice, arithmetic); a use the inferred type doesn't support fails PER INSTANTIATION (real-Zig / C++-template behavior). Reuses the W3 worklist + memoization; **no grammar change** (`anytype` lexes as a bare identifier and parses as a `Type`, like `type`). Oracle `anytype-param`; example `examples/zig-anytype/`; unit `ZigAnytypeParamTests`. An `anytype` METHOD instantiates as in the W3a row. **Cuts (loud):** an `anytype` parameter on an `extern` prototype (no C-ABI slot). A `comptime x: anytype` (redundant) is not classified specially |
| `const C = enum(T) { … };` / `enum { … }` | ✅ | container decl → C# `enum C : T` (default underlying `int`); members auto-increment or take an explicit constant value. `@intFromEnum` decays to the underlying int. Methods (above) and a `const Self = @This();` alias are allowed in the body An `enum(u64)` / `enum(usize)` member above `long.MaxValue` keeps its 64-bit pattern and prints unsigned (`std.Io.Limit`'s `unlimited = std.math.maxInt(usize)`; oracle `comptime_value_forms`). |
| `const U = union(enum) { a: T, b, … };` | ✅ | tagged union → the faithful C tagged-union shape: an outer struct `{ U_Tag __tag; U_Payload __payload; }` whose `__payload` is a NESTED `[StructLayout(Explicit)]` union overlaying every payload variant at offset 0 (the shared C-union machinery) — so payloads share storage, matching Zig's memory model. A void variant (`b`) is tag-only; an all-void union has no `__payload`. Construct with `.{ .a = v }` / `U{ .a = v }` (payload) or `.b` at a union sink (void). Direct `u.a` reads `u.__payload.a` (unchecked, like Zig release mode). Methods (above) and a `const Self = @This();` alias are allowed in the body |
| `const U = union(SomeEnum) { a: T, b, … };` | ✅ | explicit-tag tagged union (Milestone R, part 1) — the discriminant is an EXISTING named enum rather than a synthesized `U_Tag`. Reuses the whole tagged-union lowering 1:1 (outer `{ __tag, __payload }`, switch on `__tag`, payload capture); only the tag enum source differs, so the `__tag` field is typed by the named enum and each variant's tag VALUE is that enum member's value (a non-zero / out-of-order enum drives the discriminant). Each variant must name a member of the tag enum (an extra enum member with no variant is tolerated — a V1 leniency) |
| `const U = union { a: T, b: U, … };` | ✅ | UNTAGGED union (Milestone R, part 3) — no discriminant. Lowers directly to the bare overlapping-payload struct (`[StructLayout(Explicit)]`, every variant at `[FieldOffset(0)]`) — NOT a tagged `ZigUnionInfo`, so it has no `__tag`/`__payload`. Construction (`U{ .a = v }` / `.{ .a = v }`) and access (`u.a`) route through the ordinary struct-init / member paths. Each variant must carry a payload type (a void variant needs a tagged `union(enum)`). **Cut:** Zig's safe-mode active-field tracking / type-pun checks are NOT modeled — same-field read/write is faithful, reading a non-active field (punning) is unmodeled; a `switch` on an untagged union is rejected (Zig forbids it too) |
| `E!T` / `!T` error-union type | ✅ | → runtime `ErrUnion<Payload>` (`ErrUnion<Unit>` for `!void`). V1 erases the error SET `E` — `anyerror!T` / named `E!T` lower identically (payload only) |
| arbitrary-width `iN`/`uN` (`u4`, `u7`, `u12`, `u21`, `i7`, …) | ✅ | any 1..128-bit width → the smallest STANDARD C# width holding N bits (`u4`/`u7`→`byte`, `i7`→`sbyte`, `u12`→`ushort`, `u21`→`uint`, `u65`..`u128`→`UInt128`). `@sizeOf` matches Zig (both round up to whole bytes). **Leniency:** the wider storage means arithmetic does NOT wrap at exactly N bits — the same documented no-overflow-trap stance dotcc takes for plain `+`. Load-bearing for `std.unicode` (`u21` codepoints). N > 128 (needs BigInteger) is a loud cut. The DECLARED width is not lost, though it does not live on the type: it rides each type BINDING (a spelling, an alias, a `comptime T: type` seed) so `@typeInfo(T).int.bits` reports 21 for a `u21`, and it joins the monomorphization key so `f(u21)` and `f(u32)` — the same lowered `uint` — are distinct instances (road-to-zig-std S5b). A VALUE carries its width too (road-to-zig-std G3): a typed local, a parameter, a capture over a spelled element type, a `.len` (`usize`), a call's declared return, `@as(T, e)`, through `-`/`~`/`try`; so `@typeInfo(@TypeOf(x)).int.bits` over an `anytype` argument is answered (`std.math.cast`'s `maxInt(@TypeOf(x))`), and a width other than the lowered type's own keys its own instance (`bitsOf__u32w21`). A value with no spelled width anywhere still refuses (unit `ZigValueWidthTests`). Sub-byte bit-PACKING inside a `packed struct` stays byte-packed (see packed-struct row) |
| `comptime_int`/`comptime_float` | 🟡 | **A `comptime_int` local or top-level const** (`const init_capacity: comptime_int = @max(1, …);`) folds to its literal and emits no declaration; a lazy std module's one is read across modules (`std.atomic.cache_line`, a non-curated std VALUE, is lowered by the module that declares it). **A `comptime_int` RETURN type is supported (the comptime-call engine V1, road-to-zig-std G3):** `fn maxInt(comptime T: type) comptime_int` (real `std.math.maxInt` / `minInt` compile from source). The result has no runtime type, so every call is a deferred comptime fold: the instance lowers with a 128-bit carrier, the shared interpreter runs its body once every module has drained (the callee may live in a lazy module) and splices the literal, and the instance itself is dropped from the emitted program. A comptime-only call may nest in another (`maxInt(T) - minInt(T)`). Oracle `comptime_int_calls` + the real-std `Dotcc_matches_zig_std_math_max_min_int_from_source` differential; unit `ZigComptimeCallTests`. A body of comptime `const` bindings and a `return` is EVALUATED when the call is instantiated, so a position that needs the value during lowering has it (an enum member of `maxInt(u64)`); any other body stays a deferred fold. **Leniency:** a folded value that does not fit its sink truncates (`const m: u8 = maxInt(u16);`), as a plain out-of-range literal already does; zig rejects both. **`comptime_int` as a TYPE (target T5):** `CType.ComptimeInt`, an `__int128` carrier that compares unequal to `i128`, so a `comptime to: comptime_int` parameter, a capture of one and arithmetic over those keep it; an untyped literal or such a value bound to an `anytype` makes that parameter a comptime VALUE, one instance per value as in zig (`kind(5)` → `kind__ci5`), `@typeInfo(@TypeOf(x))` answers `.comptime_int`, and a `@TypeOf(x)` result is comptime-only, so the call folds (`add(3, 4)` → 7). This changed what a bare literal to an `anytype` means: it was inferred as `i32`, which zig never did (oracle `inline_for_break_comptime_int`). **Cuts:** `comptime_int` as a field type, a non-generic `comptime_int` function, a comptime_int argument beyond 64 bits, `comptime_float` anywhere, a call whose body the immediate path cannot evaluate, in a position that needs its value DURING lowering, and a ROOT unit's enum member calling a function of the same unit (containers register before functions are declared; a lazy module declares on demand) |
| `[N:s]T` non-zero sentinel array (local + global) | ✅ | a non-zero sentinel `[N:s]T` array (Milestone Z) materializes `s` in the trailing slot (literal-appended / written for `undefined`); a GLOBAL `[N:s]T` gets the same N+1 reservation in its pinned store. `[*:s]T`/`[:s]T` pointer & slice sentinels are type-level only (erased; the value is unused without the cut auto-scan) |

## Statements

| Feature | Status | Notes |
|---|---|---|
| `if (c) … else …` | ✅ | condition wrapped in `Cond.B(…)` for C-truthy semantics |
| `if (opt) \|x\| {…} else {…}` (optional capture) | ✅ | bind a value/pointer optional's payload in the then-branch (Milestone M, part 1). A value optional `?T` → hoist the condition to a single-eval temp, `if (Cond.B(__cap.HasValue)) { var x = __cap.Value; … } else { … }`; a niche optional pointer `?*T` (a bare `T*`) → a non-null test with `x` bound to the unwrapped pointer (the same value). `\|_\|` tests without binding; the `else` is optional. **Deferred:** the by-ref `\|*x\|` form (part 4) |
| `const v = if (opt) \|x\| thenE else elseE;` (VALUE-position optional capture) | ✅ | the expression sibling of the statement form (road-to-zig-std S4a). A captured `if` can't be a bare ternary (it binds `x`), so it HOISTS (ANF): a result temp is declared before the enclosing statement and assigned by a real `if` whose then-branch binds `x = opt.Value` (or the unwrapped pointer). `else` is mandatory (a value is yielded on both paths). Optional + optional-pointer conditions; the parse gate for `std.ArrayList`'s `const Slice = if (alignment) \|a\| …align(a)… else []T;`. Oracle `if_capture_expr`; example `examples/zig-if-capture-expr`. **Cuts:** an error-union value `if`; the by-ref `\|*x\|` form; comptime folding to a TYPE (arrives with the S4 TypeVal engine) |
| `if (eu) \|x\| {…} else \|e\| {…}` (error-union capture) | ✅ | bind an error union's success payload to `x` in the then-branch, the error to `e` in the else-branch (Milestone M, part 3). → a value inspection of the runtime `ErrUnion<T>`: `if (Cond.B(__cap.IsErr)) { var e = __cap.Code; … } else { var x = __cap.Value; … }` — NOT a propagating `try`, so the error is handled here and never reaches the function-boundary catch. `else \|_\|` discards the error. **V1:** `e` is the erased `ushort` error code; OPERATING on it (`e == error.X`, `@errorName`, propagation) awaits the error-set milestone. dotcc also leniently accepts a plain `else` on an error union (real zig requires `\|e\|`/`\|_\|`) |
| `while (c) …` | ✅ | (scalar/optional-less condition) |
| `while (opt) \|x\| …` (capture-while) | ✅ | optional payload capture-while (Milestone M, part 2). The condition is re-evaluated each iteration (it commonly advances an iterator); while it's non-null, `x` is bound and the body runs, else the loop exits. → `while (true) { var __cap = cond; if (Cond.B(__cap.HasValue)) { var x = __cap.Value; … } else break; }` (a niche optional pointer tests non-null / binds the pointer itself). A real loop, so `break`/`continue` (incl. the labeled forms) compose; `\|_\|` iterates without binding. The completions are supported: a while-`else` clause (`… else elsebody` runs on natural exit — a `break` skips it), a `: (cont)` continue-expression (lowered to the C `for` post, so `continue` runs it), and the error-union capture-while `while (eu) \|x\| … else \|e\| …` (bind the success payload each turn; on error bind `e`, run the else-branch, exit — mirrors the `if`-capture error arm). **Deferred:** a cont + else combination together (rare) |
| `while (c) : (cont) …` | ✅ | the continue-expression → the C IR `for`-post, so `continue` runs `cont` (faithful to Zig). An assignment cont accepts the **full operator set** (an `AssignOp` grammar nonterminal): the canonical `: (i += 1)`, plain `: (i = i + 1)`, and every compound / wrapping / saturating op (`+= … ^=`, `+%= …`, `+\|= …`) — each lowering exactly as the matching `x op= e;` statement (see **Assignment**). A bare-expr cont (`: (step())`) also parses. Applies to the capture-while `: (cont)` form too |
| `break;` / `continue;` | ✅ | unlabeled — reuse the C IR loop-control nodes. A `break` inside a `switch` prong exits the enclosing LOOP, as in zig: the prong lowers into a C# `switch`, where a bare `break` exits only the switch, so there it is a `goto` past the loop (this was a silent miscompile until road-to-zig-std G3; oracle `switch_break_exits_loop`). A prong body may be a bare jump too: `v => break`, `v => break :l`, `v => break :blk v`, `v => continue [:l]`, optionally after a `|x|` capture (about 330 in std, including std.Io.Writer.print's `'{', '}' => break,`), in a statement switch, a tagged-union switch and a `catch \|e\| switch (e)` (oracle `switch_jump_prongs`; unit `ZigSwitchJumpTests`). `unreachable` as a statement or a statement prong (`0 => unreachable,`) is the loud throw C23's `unreachable()` lowers to (zig's safe builds panic); **cut:** `unreachable` in VALUE position (`x orelse unreachable`) |
| `break :blk v;` (labeled break with value) | ✅ | yields `v` from the enclosing labeled value-block (see **labeled block as a value** in Expressions) |
| `lbl: while/for (…) …` (labeled loop) + `break :lbl;` / `continue :lbl;` | ✅ | a `label:` may prefix any while/for loop; `break :lbl` / `continue :lbl` exit / next-iterate it — including an **outer** loop. C# has no labeled break/continue, so they lower to a `goto`: `break :lbl` → a label just AFTER the loop, `continue :lbl` → a label at the END of the loop body (so the natural iteration step still runs). Labels are emitted only when referenced. The labeled-while/for VALUE form (`break :lbl v` yielding from a loop used as an expression) is supported via the value-position loop (see **`while/for … else`** in Expressions, Milestone Y part 2) |
| `switch (x) { v => {…}, a, b => {…}, lo...hi => {…}, else => {…} }` | ✅ | as a STATEMENT → the C IR Switch. Single / multi-value / inclusive-**range** (`lo...hi`) / `else` (→ default) prongs; NO fall-through (each prong gets an appended `break`). Switching on an **enum** works (subject + `.member` labels decay to the underlying int). A range lowers to a C# relational-pattern case `case >= lo and <= hi:` (Zig requires comptime-known bounds = C#'s constant requirement). Prong bodies are braced **blocks** OR a bare expression (`v => expr`, an expression statement) |
| `switch (u) { .a => \|x\| {…}, .b => \|*y\| {…} }` | ✅ | switch on a **tagged union** → dispatch on the `__tag`; a `\|x\|` payload capture binds the matched variant's payload **by value**, and a by-reference `\|*x\|` capture (Milestone M, part 4) binds a `*T` into the payload field (`T* x = &u.__payload.v;`) so `x.* = …` writes through to the (mutable) union. A **multi-variant** capture prong `.a, .b => \|x\|` (Milestone Z) is allowed when every listed variant shares the same payload type — `x` binds the FIRST variant's field, which aliases the rest (all at offset 0 in the explicit-layout payload union); differing payload types → a clear error. An exhaustive union switch with no `else` makes its last prong the C# `default` (so the function provably returns) |
| `return e;` / `return;` | ✅ | |
| `x = e;` assignment | ✅ | |
| `x op= e;` compound assignment | ✅ | all ten: `+= -= *= /= %= <<= >>= &= \|= ^=`, plus the wrapping `+%= -%= *%=` and saturating `+\|= -\|= *\|=` (Milestone P). → the shared `Assign` IR node with a non-null `CompoundOp` → a NATIVE C# `x op= e`, so the target lvalue is evaluated exactly **once** (`arr[next()] += 1` calls `next()` a single time — not a `x = x op e` desugar). A native compound op already truncates back to the LHS width (unchecked), so `+%=` is observably identical to `+=`. The SATURATING compound forms have no native op → they desugar to `x = ZigMath.Sat…(x, e)` (the lvalue read twice; a side-effecting target — `slot().* +\|= 1` — is a clear deferred error rather than a silent double-eval). Zig has **no** `++`/`--` (the idiom is `i += 1`) |
| destructure `a, b = e;` / `const a, const b = e;` | ✅ | bind a tuple's elements to new locals OR assign to existing lvalues (Milestone G + S), ≥2 binders. Binder kinds: a fresh `const`/`var` (optionally typed `const a: T`), an existing lvalue, or `_` (discard). A tuple-**literal** RHS lowers **element-wise in source order, no temp** — matching Zig's sequential semantics, where an existing-lvalue write is visible to a later element's read (so `a, b = .{ b, a }` is **not** a swap: `a←b`, then `b←` the new `a`). A non-literal tuple RHS single-evals into `__tupN`, then per-element `.ItemN` reads. A brace-less sequence keeps new binders in the enclosing scope |
| `_ = e;` discard | ✅ | Zig's mandatory discard of a non-void result |
| block `{ … }` | ✅ | |
| `defer Stmt;` | ✅ | scope-exit cleanup — runs on EVERY exit from the enclosing block (fall-through, `return`, `break`, `continue`, a propagating error), in LIFO declaration order. → C# `try { rest } finally { cleanup }`. The deferred `Stmt` is an `expr;`, a `_ = expr;` discard, or a braced block. See the **Defer** section |
| `errdefer Stmt;` | ✅ | error-exit cleanup — runs only when the block exits via a propagating error, LIFO-interleaved with `defer`. → C# `try { rest } catch (ZigErrorReturn) { cleanup; throw; }`. A function with an `errdefer` routes its `return error.X` through a throw so it reaches the catch. **NOT pursued:** `errdefer \|e\| …` payload capture — current Zig (0.17) has REMOVED that syntax (`errdefer \|e\|` is a parse error: "expected block or expression, found '\|'"), so dotcc rejects it too (round-trippable) |
| `for (a..b) \|i\| …` (range for) | ✅ | → C `for (usize i = a; i < b; i++)`; the `\|i\|` capture is the usize loop index (`\|_\|` discards). The body is a Stmt or block |
| `for (s) \|x\|` / `for (s, 0..) \|x, i\|` (for-over-slice) | ✅ | iterate a slice's elements (`x` = a per-iteration copy); the index form also binds the usize index. → C `for (usize __i=0; __i<s.Len; __i++) { var x = s.Ptr[__i]; [var i = __i + 0;] … }` (the slice is hoisted to a temp unless a bare var) |
| `for (s) \|*e\|` / `for (s, 0..) \|*e, i\|` (by-reference for-slice) | ✅ | BY-REFERENCE element capture (Milestone M, part 4): `e` is a `T*` into the slice element (`T* e = &s.Ptr[__i];`), so `e.* = …` writes through to the element. The combined by-ref + index form `for (s, 0..) \|*e, i\|` (Milestone Z) binds both the element pointer and the usize index. A NON-ZERO index start `for (s, 5..) \|x, i\|` works too — the index binds `__i + N` |
| `for (a, b) \|x, y\|` / `for (a, b, c) \|x, y, z\|` (multi-object for) | ✅ | a RUNTIME lockstep walk (road-to-zig-std G5, hash_map's `for (metadata, keys, values) \|m, k, v\|`): one index over the FIRST object's length, each capture a per-iteration copy of its element; an object is a slice, an array or a pointer to one (read once), a `_` capture binds nothing, and a trailing comma may follow the last object (zig fmt's one-per-line layout). zig asserts equal lengths; dotcc does not check (its ReleaseFast stance). The `inline for` over comptime member lists keeps its unrolled form (oracle `multi_object_for`; unit `ZigMultiObjectForTests`). **Cuts:** four or more objects, by-reference `\|*x\|` and index `0..` objects in the multi-object forms |
| open-ended `s[lo..]` | ✅ | open-ended slicing (Milestone O, part 1) — the high bound is the source LENGTH, so → `{ s.ptr + lo, sourceLen - lo }` where `sourceLen` is a slice's `.len` or an array's element count. Shares the `s[lo..hi]` machinery; only the high bound differs. A bare pointer carries no length, so open-ending one is rejected (as Zig does). (by-ref on an optional/error-union `if`/`while` stays a grammar-level cut) |

## Expressions

| Feature | Status | Notes |
|---|---|---|
| integer / float / string literals | ✅ | decimal **+ `0x`/`0o`/`0b` radix + `_` separators** (see Lexer); float incl. hex `0x1.8p3` **+ exponent-only `1e10`**; string reuses C escape decoding (`\n \t \\ \" \xNN`) **+ `\u{…}` (incl. non-BMP) + multiline `\\`**. A string literal is `*const [N:0]u8`: its `.len` is N — directly and through an unannotated `const s = "…";` binding (it used to count the NUL; oracle `string_literal_len`) |
| `true` / `false` | ✅ | boolean literals — a `bool` value (→ C# `true`/`false`, stored in the normalising `CBool`) |
| char literal `'x'` | ✅ | Zig's `comptime_int` = the codepoint → an integer literal (`'A'` → 65). Escapes `\n \t \r \\ \' \xNN` + octal decode via the shared string-escape machinery, plus `\u{NNNN}` (decoded Zig-side). **Deferred:** a `\u{…}` codepoint > 0xFFFF (lowered as a single int — surrogate handling deferred) |
| identifiers, `(grouped)` | ✅ | |
| `or` `and` (short-circuit) | ✅ | |
| comparison `== != < > <= >=` | ✅ | non-associative (`a < b < c` is a parse error, like Zig) |
| bitwise `& ^ \|`, shift `<< >>` | ✅ | |
| arithmetic `+ - * / %` | ✅ | usual-arithmetic result typing (fixes i64 truncation) |
| wrapping `+% -% *%` | ✅ | two's-complement WRAP at the operand width (Milestone P, part 1). Zig has no integer promotion, so the result type is the peer-resolved operand type; the emitted C# runs unchecked, where a narrowing cast truncates — a sub-`int` width (`u8`/`u16`/…) gets a `(byte)`/`(short)` truncating cast back, `int`-and-wider wrap natively. The wrap is at the OPERAND width even when widened (`(250 +% 10)` is 4, not 260). dotcc does **not** model Zig's safe-mode trap on plain `+`, so `+%` and `+` are observably identical here |
| saturating `+\| -\| *\|` | ✅ | CLAMP to the operand type's range (Milestone P, part 2). No native C# operator, so each routes through the spliced `ZigMath.Sat{Add,Sub,Mul}<T>` runtime (`DotCC.Libc/ZigMath.cs`): widen both operands to a 128-bit accumulator, do the EXACT op, clamp to `[T.min, T.max]`, truncate back — exception-free and correct for every width (incl. the signed `MinValue * -1` edge). The peer type is the operand type (a literal yields to its concrete peer); the clamp is at the OPERAND width even when widened. Two comptime-literal operands are a Zig error if the exact result doesn't fit the sink (`100 *\| 100` at `u8`) — not modeled (no comptime fit-check), but never round-trippable code |
| prefix `-` `~` `!` | ✅ | |
| `if (c) a else b` (if-**expression**) | ✅ | → C# ternary |
| `switch (x) { v => e, … }` (switch-**expression**) | ✅ | a switch in value position (a typed binding / return / any `RhsExpr`) → C#'s native switch EXPRESSION (`x switch { v => e, a or b => e, _ => e }`). Each prong yields a value (a bare-expr body); `else` → the `_` default; arm values lower at the result sink; an enum subject + `.member` labels decay to the underlying int. Same structural trick as the if-expression (a `RhsExpr`, not a Primary). An inclusive **range** arm `lo...hi => e` lowers to a relational pattern `>= lo and <= hi => e`. A **block-bodied prong** `v => blk: { …; break :blk v; }` (the idiomatic multi-statement arm) is supported at a full `const`/`var`/`return`/assignment RHS (Milestone Y, part 1): the prong body became `RhsExpr`, and the whole switch lowers as a STATEMENT switch filling a result temp (not a C# switch-expression). In a SUB-expression it must be PARENTHESIZED (`x + (switch (k) { … })`) — the `( RhsExpr )` Primary makes it reachable, and the ANF pass hoists the temp-fill. A plain block prong that never completes (`error.OutOfMemory => { return; }`, std.PriorityQueue) is `noreturn`, so it stands where a value is wanted like `=> return` (task #103); a block that can complete stays a loud cut. `comptime switch (…) {…}` / `comptime if (…) a else b` in value position (std.Io.Writer.print's `const arg_pos = comptime switch (placeholder.arg) {…}`, road-to-zig-std G3) lower as the inner form: a comptime-TAG subject selects its arm at lowering time, and any other subject (a comptime value, where zig would fold too) lowers to a C# switch over the literal, the same value. A **tagged-union** subject in value position (`const n: u8 = switch (spec) { .none => 1, .number => \|v\| v * 4 };`) lowers as the statement union switch filling the result temp, a `\|v\|` payload capture included (multi-file oracle `fmt_parse_shapes`). **Deferred:** a `\|x\|` capture in a switch that must stay a C# expression (a sub-expression over a non-union subject) |
| `blk: { …; break :blk v; }` (labeled block as a value) | ✅ | a block in value position that runs statements and YIELDS a value via `break :blk v`. → the roadmap's temp-fill: a result temp (`__blkN`), each `break :blk v` rewritten to `temp = v; goto __blkN_end;` (braced, so a conditional break stays conditional), an end label, then the surrounding statement reads the temp. The result type is the sink (an annotated decl / function return / lvalue) or the first `break` value's type. Same structural trick as the if/switch-expression (a `RhsExpr`, not a Primary). Also usable as a **value-position `if`/`switch` branch** at a `const`/`var`/`return`/assignment RHS (Milestone Y, part 1 — the `if`/`switch` then lowers as a STATEMENT filling a result temp `__vcf`, each branch's block copied into it). In a SUB-expression it must be PARENTHESIZED (`x + (blk: { … })`) — the `( RhsExpr )` Primary makes it reachable and the ANF pass hoists it. **Deferred** (clear errors): a global initializer (needs a comptime value), and an error-union (`!T`) function return |
| `while/for (…) … else d` (loop as a value) | ✅ | a `while`/`for` with an `else` clause in value position (Milestone Y, part 2) → a STATEMENT filling a result temp `__lv`: a `break v` (unlabeled, innermost) / `break :lbl v` (matching a `lbl:`-labeled value loop) assigns `__lv` and `goto`s the end label (skipping the `else`), and the `else` value is assigned on natural completion. The end label is emitted only when a `break` targets it (an else-only loop avoids a CS0164 unreferenced-label warning). The forms: `while (cond) {…} else d` and `for (slice) \|x\| {…} else d`, each optionally labeled (`lbl: … else d`). In a SUB-expression it must be PARENTHESIZED (`x + (for (s) \|v\| {…} else d)`) — the `( RhsExpr )` Primary makes it reachable and the ANF pass hoists it. **Deferred** (clear errors): a for-RANGE / indexed `\|x,i\|` / capture / continue-expr (`: (cont)`) value loop, a brace-less value-loop body |
| comptime STRUCT CONSTANTS (road-to-zig-std S3a) | ✅ | a <code>const c: Cpu = .{ .arch = .aarch64 };</code> whose fields can be READ at lowering time, so a question about one settles before any code is emitted. The comptime domain S5 left out: it folds types exactly and scalar values, but an aggregate VALUE had none. What it unlocks is the platform conditional — `if (builtin.cpu.arch == .x86_64)`, `switch (builtin.os.tag)` — which **must** fold, because the untaken arm in real std is the inline asm / syscall / per-arch intrinsic the branch exists to avoid, and lowering both would sink the module. A folded `if` lowers only the taken arm; `and`/`or`/`!` over two comptime questions fold too (both sides must settle — a half-comptime condition still has a runtime half to evaluate). Recorded as a SIDE EFFECT: the ordinary runtime decl still emits, since a struct constant is a perfectly good runtime value. **Scope (deliberate):** only a MODULE-QUALIFIED read folds (`builtin.cpu.arch`, not a same-file `cpu.arch`) — every name-keyed comptime map here is guarded on the name not resolving to a runtime symbol, and a constant that emits its declaration always has one, so the same guard would refuse it; reaching through an import has no such ambiguity. Recording is also kept PURE of module resolution, because it runs during module PREPARATION and resolving there would defeat S2's laziness |
| `comptime EXPR` (value-comptime) | ✅ | Milestone T — forces compile-time evaluation of a side-effect-free **value** and splices the result back as a literal. Folds the full expression subset (arithmetic/bitwise/relational/logical/ternary, `@sizeOf`, enum constants), AND interprets a CALL to a user function — `comptime fib(10)` runs the recursive callee (call frames + recursion), `comptime fact(5)` runs a `while` loop with local mutation. Computed in 128-bit; an eval-step budget is the non-termination backstop (Zig's `@setEvalBranchQuota`). Splices scalars (int/float/bool) AND **aggregates by value** — a comptime function returning a **struct** (`var c: T = undefined; c.f = …; return c;` → a field map → `new T { … }`) or an **array lookup table** (`var t: [N]u32 = undefined; t[i] = …;` → a vector → `stackalloc u32[]{ … }`; the fill loop may read prior elements, e.g. a comptime Fibonacci table). Use an aggregate comptime at a LOCAL `const x = comptime f();` (inferred or annotated type) — real zig rejects the keyword on a container const as already-comptime, and a comptime ARRAY at a global is a clear error (use a local, or a runtime `const X = f();` with no keyword, which works via the sound array-by-value return). A pointer to a comptime aggregate is the aggregate itself (the comptime-engine segment E1), so the callee may mutate a struct through a method's `self: *S` and an array through a `*[N]T` parameter (`fill(&buf, v)`); oracle `comptime_pointer_to_aggregate`, unit `ZigComptimeEngineTests`. A comptime value needed DURING lowering is computed then (the comptime engine's E2): an array extent may call a function (`[lenFor(u8)]u8`, a generic, or one declared later), whose body lowers on demand, once; `comptime f()` resolves at once when it can; `comptime_int` works as a local, parameter and return type; and `if (comptime f()) \|x\|` over a `?comptime_int` (`return null` included) folds to the taken branch with `x` a literal (oracle `comptime_values_during_lowering`). A `comptime var s: S = .{…}` of a STRUCT or ARRAY type lives across statements: each `comptime s.method(…)` mutates it through `self: *@This()`, and a runtime read sees the value it has at that point (`std.fmt.ArgState`'s shape; oracle `comptime_struct_var`). A `const p = comptime f(…)` of an aggregate is held the same way: a switch over a comptime UNION value (`switch (p.arg) { .number => |n| n, … }`) selects its prong at lowering time with the payload bound, and a comptime byte-slice field (`.spec = s[1..]`) splices back as a string (oracle `comptime_union_values`). A comptime that produces a **type** is the wall (below) |
| `comptime { … }` (block statement) | ✅ | Milestone T, part 3 — a `comptime { … }` block runs at COMPILE TIME: dotcc executes its compile-time-value statements at lowering time (a `comptime var`/`const` decl, an assignment to a comptime var, a comptime `while` loop) and emits NO runtime code. Its only effect is on comptime values — an enclosing `comptime var` mutated inside keeps its computed value, and later references substitute it as a literal. **Deferred / firewall:** a store to a runtime `var` inside the block (no runtime effect — a clear error, as in real zig), `@compileError` assertions, and a block producing a **type** (the wall) |
| `comptime if (c) then [else e]` (statement) | ✅ | road-to-zig-std S9, the parse gap behind std's `findScalarPos` (`comptime if (block_x_len < 4) break;`). The condition MUST fold (a tag question or anything `ConstEval` settles, comptime vars and captures included), else it is an error, as in zig. Only the taken arm lowers: a block or assignment arm runs in the comptime evaluator (so `w = 20;` updates a `comptime var` and emits nothing), and a control-flow arm (`break`, `return`) lowers as the statement it is. Both zig's `comptime if (c) {…} else {…};` and the `;`-less block form are accepted. Oracle `comptime_if_anon_enum`; unit `ZigComptimeIfTests` |
| `inline for (lo..hi) \|i\|` / `inline for (arr) \|x\|` / `inline for` over a COMPTIME LIST / `inline while` (loop unroll) | ✅ | Milestone T, part 3 — a comptime-counted loop is UNROLLED: the body is replicated once per iteration, each copy binding the capture/counter to that iteration's value, so no runtime loop survives. **Counted range** `inline for (lo..hi) \|i\|` binds `i` to each constant index; **over a fixed array** `inline for (arr) \|x\|` binds `x` to each element (`arr[k]`) of a comptime-length `[N]T`; **`inline while (c) : (i = …)`** advances a `comptime var` counter, folding the condition and continue-expression each round. **Over a COMPTIME LIST (road-to-zig-std S6)** — a `@typeInfo` member list (`field_names` / `field_types` / `field_values`) or a `[_]type{…}` literal — the capture binds COMPTIME rather than to an emitted `const`, because a list element may be a TYPE (which has no runtime slot at all) or a comptime STRING (which `@field(v, name)` / `@hasField(T, name)` must read at lowering time). That is what lets one generic body reach every field of a struct it has never seen, with nothing left in the emitted C# but the field accesses. Four operand shapes, all measured against the pinned std rather than guessed: a single list, two lists walked in PARALLEL (`inline for (names, types) \|name, F\|` — the commonest at 17 uses), a list alongside its indices (`, 0..`), and a `[_]type{…}` literal (21 uses). The captured name is restored to whatever it shadowed after each copy, so nesting and reuse are safe. Oracle `inline_for_comptime_lists` (it iterates `[_]type{…}` literals so it is valid under CI's zig 0.16.0 too — the unroll is the same code path whatever the operand); example `examples/zig-inline-for-lists/`. Because each copy is plain straight-line IR, the same construct works whether the enclosing function runs at runtime or is itself `comptime`-called (the interpreter walks the unrolled copies — e.g. to fold a lookup table). **Deferred (clear errors):** `inline for` over a slice (length not comptime-known); the indexed `\|x, i\|` form over a RUNTIME array and the by-ref `\|*x\|` form; a RUNTIME parallel `for (a, b) \|x, y\|` (the grammar accepts it — only the comptime form lowers); parallel comptime lists of unequal length, or an index capture that does not start at 0; an anonymous `.{…}` / `&.{…}` type list (needs sink inference); a bare `inline while (c) body` without a continue-expression; a non-constant bound / runtime counter; an unroll past a 4096-iteration cap. **`break` / `continue` in the body (target T5, std.mem.findScalarPos):** unrolling removes the loop, so a `break` jumps past the last copy and a `continue` to the end of its own copy; a copy that ALWAYS breaks (a folded `comptime if (w < 4) break;`) ends the unroll, as zig analyses no later iteration, and a counted range's index is a comptime constant in every copy (`const w = 32 / (1 << j);` folds). A jump inside a nested loop or `switch` binds to that construct (oracle `inline_for_break_comptime_int`) **The comptime format SCAN** (road-to-zig-std G3, std.Io.Writer.print): a bare `inline while (c) body` unrolls, the body's assignments to a `comptime var` running at lowering time (`i += 1;`); a compound continue-expression `: (i += 1)` steps (it had been read as `= 1`); a `break` / `continue` that is comptime control (the trailing jump of a copy, e.g. the taken prong of a `switch` over a comptime value, which selects its prong while unrolling) ends or continues the unrolling; and a comptime string indexed by a comptime value is its byte (`fmt[i]`). A runtime-conditional jump in an unrolled body stays a loud cut (oracle `comptime_format_scan`; unit `ZigComptimeScanTests`). A `comptime var` may hold a comptime STRING
(`comptime var literal: []const u8 = "";`), grown at lowering time by `literal = literal ++ fmt[a..b];` over comptime slices
of a comptime string, and read like any comptime string (oracle `comptime_string_var`). **Cut:** a comptime STRUCT var
(`comptime var arg_state: std.fmt.ArgState = .{ … }`) and its `*@This()` methods called at comptime. |
| function call `f(args)` | ✅ | intra-Zig + forward-ref + libc-by-bare-name (incl. variadic `printf`) |
| prefix `&` (address-of) | ✅ | `&x` → `*T`; a var/param operand is marked address-taken |
| postfix `p.*` (deref), `a[i]` (index) | ✅ | pointer deref / subscript → the C `Unary(Deref)` / `Index` IR. On a slice, `s[i]` indexes through `.Ptr` (`s.Ptr[i]`) |
| `t[N]` (tuple index) | ✅ | a **literal** `N` into a tuple → `.ItemN+1` (Milestone G); a runtime index is rejected (a tuple field is statically named, not addressed) |
| `s[lo..hi]` (slicing) | ✅ | → a sub-slice fat pointer `{ s.ptr + lo, (ulong)(hi - lo) }` (base may be a slice, pointer, or array) |
| `s.len` / `s.ptr` (slice fields) | ✅ | the fat pointer's length (`ulong`) and data pointer (`T*`) |
| `@as(T, expr)` | ✅ | explicit-type cast → the C `Cast` IR (`@as(c_int, 42)` for a variadic arg) |
| `null` literal | ✅ | optional none / null pointer (renders C# `null`) |
| `undefined` | ✅ | uninitialized storage. An array local takes the stackalloc path; a scalar → `default(T)` (a zeroed over-approximation — a correct program writes before reading) |
| postfix `.?` (optional unwrap) | ✅ | value optional → `.Value` (panics on none); optional pointer → identity (V1: no null-check) |
| `a orelse b` (value RHS) | ✅ | value optional → C# `??` (single-eval, lazy `b`); pointer → `a != null ? a : b` (simple LHS) |
| `a orelse return [v]` / `a catch return [v]` (control-flow fallback) | ✅ | the unwrapped payload, or — on none / error — an EARLY `return` from the current function (Milestone N, part 6). Lowered structurally at a `const`/`var` initializer or a statement: hoist the operand, `if (none / error) { return …; }`, then bind the payload. The `return` wraps correctly in a `!T` fn (incl. `return error.X`). Both a value (`return v`) and void (`return`) form. (`a catch \|e\| return e` is just `try a` — use `try`.) Also works in a SUB-expression (`100 + (a orelse return v)`) via the ANF statement-hoist. **Deferred:** `break`/`continue` control-flow fallbacks |
| statement-shaped fallbacks: `a catch \|err\| switch (err) {…}`, `a catch {…}` / `a orelse {…}`, `a catch \|e\| return e`, `a orelse break [:l [v]]` / `continue [:l]` (road-to-zig-std) | ✅ | ~1,000 sites in the pinned std (`catch \|err\| switch` alone is 498; `catch {` 150, `orelse {` 105, `orelse break` 102), each of which was a PARSE error that made the resilient parse drop the whole enclosing function — `std.fmt.bufPrint` among them. Grammar: `FallbackArm` (break / continue, optionally labeled, a labeled break optionally valued; a Block; a SwitchExpr) after `catch` / `orelse`, and `CaptureArm` (+ `return v`) after `catch \|e\|` — every arm opens with a token outside FIRST(BoolOr), so the value-vs-arm choice is one lookahead; `break :l` wins the `:` through a `rightmost` group. Lowering reuses the `catch return` machinery: one arm abstraction through the declaration initializer, the expression statement / `_ =` discard, and the ANF sub-expression hoist, each arm lowering exactly like the statement it spells. A `switch` arm FILLS a result (prongs yield a value, or jump — `=> return v`, and the capture prong `else => \|e\| return e`, which binds a plain subject's value). **`return e` of a runtime error value** (a capture) is now an ERROR return in a `!T` function — it used to wrap the code as the success payload. A block that can fall through where the payload is needed is rejected (zig: a `void` block is not a `T`); a discarded `_ = a catch {};` may fall through. Oracle `fallback_arms` / `fallback_switch_capture_prong`; unit `ZigFallbackArmTests`. **Cuts:** `catch \|e\| return;` (void, capture unused — zig rejects it too); an unlabeled `break v`; the no-return proof is syntactic (a block whose LAST statement jumps), so an `if` whose arms both jump still counts as falling through |
| prefix `try` | ✅ | unwrap an error union's payload, or PROPAGATE its error out of the enclosing `!T` fn (exception-based early-return, modeled on the setjmp lowering). `try e;` as a statement works too |
| `e catch fallback` | ✅ | the payload on success, else the fallback. A simple, side-effect-free fallback keeps the eager `ErrUnion.Catch(a, b)`; a SIDE-EFFECTING fallback (Milestone N, part 3) lowers LAZILY — the union is hoisted to a single-eval temp and `b` runs only on error via a ternary `__cE.IsErr ? b : __cE.Value`. A side-effecting fallback in a SUB-expression (`x + (a catch b())`) is hoisted to a `__anf` temp before the enclosing statement (the ANF pass) — subject to the eval-order guard (rejected if a side effect was evaluated earlier in the same statement) |
| `e catch \|err\| fallback` (catch capture) | ✅ | binds the error to `err` for the fallback `b` (Milestone N, part 3), lowered lazily — hoist the union, bind `ushort err = __cE.Code;`, then `__cE.IsErr ? b : __cE.Value` so `b` (which may use `err`, e.g. `err == error.Bad`) runs only on error. As a `const`/`var` initializer or, in a SUB-expression, hoisted via the ANF pass (same eval-order guard). **Deferred:** the control-flow fallback `catch \|e\| return e` (`catch return` clusters with `orelse return`, above) |
| `error.Foo` (bare error value) | ✅ | a first-class error VALUE (Milestone N, part 1) — usable outside `return error.Foo;`: bound to a `const`/`var`, captured (`else \|e\|` / future `catch \|e\|`), and compared. V1 erases the named set into one flat global code space, so an `error.Foo` lowers to its stable `ushort` code, typed `CType.ErrorSet`. (Explicit `error{A,B}` set decls / named `E!T` distinct from `anyerror!T` are still deferred) |
| `e == error.Foo` / `e != error.Foo` (error-value equality) | ✅ | error-value comparison (Milestone N, part 1) — equal codes mean equal errors, so `==`/`!=` lower to the ordinary integer comparison of the flat codes. Works on a bound error too (`else \|e\|` / a `const`), which un-erases the Milestone M part-3 cut (a USED named `\|e\|` is now valid in both compilers) |
| `switch (e) { error.Foo => …, else => … }` (error switch) | ✅ | switch on an error value (Milestone N, part 2) — an error value IS its flat `ushort` code, so this lowers to an ORDINARY integer `switch` on the code (each `error.Foo` prong → a `case <code>:`, `else` → `default:`). Rode in on part 1's representation — no new lowering. The error is commonly captured from `else \|e\|` first; an `anyerror!T` (open set) requires the `else` |
| `const E = error{ A, B };` (error-set declaration) | ✅ | an explicit named error set (Milestone N, part 5). dotcc erases the set into the flat global code space, so the decl is COMPTIME — it registers the member names (each a stable code) and emits NO runtime decl. `E` serves as the (erased) set in an `E!T` return type (same `ErrUnion<T>` as `anyerror!T`; an inline `error{A}!T` works the same way) AND as a plain VALUE type — `fn f(e: E)`, `var x: E`, a non-`!T` return `fn worst() E` (Milestone X, part 3b) — which lowers to the flat `ushort` code (the error value itself). `E.member` access (Milestone X, part 2) and `@errorName` (part 1) are supported; set MEMBERSHIP is checked (part 3a — a `return` of a foreign error / an undeclared `E.member` is rejected). **Deferred:** distinct per-set code spaces (membership stays a single flat space, checked but not type-distinct) |
| postfix `.field` | ✅ | struct field access → the shared `Member` IR (field type from the aggregate table). Zig has no `->`, so `p.field` on a `*T` auto-derefs (emits C# `->`). `EnumName.member` resolves here too → an `EnumConstRef` |
| `.{ .f = v, … }` (anonymous struct literal) | ✅ | result-located → `new T { f = v }` from the sink type (a typed decl, return, assignment, call arg, or field). Empty `.{}` zero-inits. A field with a declared default (`f: T = expr`) that the literal omits is materialized from that default. **An ARRAY field is inline storage** — a C# `fixed` buffer, which C# cannot assign inside an object initializer at all (CS1666) — so `.items = undefined` DROPS the member (C#'s zero-init stands; `undefined` asks for no particular contents) and any other value is a **loud cut**: build the value first and store the elements (`var v: T = undefined; v.items[0] = …;`) **`&.{ … }` at a `*const S` sink** is result-located at `S`, and a comptime-known one (constants, function names, nested such literals) lives in STATIC storage, one address for every evaluation as in zig: `std.Io.Writer.fixed`'s `.vtable = &.{ .drain = fixedDrain, … }` (oracle `vtable_literal`; unit `ZigPrintPathTests`). **Cut (loud):** a `&.{ … }` with a runtime field (a stack temporary in zig). |
| `T{ .f = v, … }` (typed struct literal) | ✅ | Zig's `CurlySuffixExpr <- TypeExpr InitList?` — the type is named, so NO sink is needed; valid in any position, incl. sink-less ones like `(T{…}).field`. A dedicated `CurlySuffix` grammar level (above `Type`) makes it conflict-free against `fn f() RetType {` (the return type stays a raw `Type`, no init list) — no rewriter. `&T{…}` (address of a temporary) materializes a block-local temp and takes its address (the same shared-backend path as C's `&(T){…}`) |
| `.{ a, b, … }` (positional tuple literal) | ✅ | result-located → `new System.ValueTuple<…>(a, b)` (Milestone G); element types come from a tuple sink, or are inferred from the elements (`const t = .{a, b};`). Shares the `.{…}` surface with the named struct literal — a literal that MIXES positional + named is rejected |
| `.enumLiteral` | ✅ | a bare `.member` resolves against its sink (typed decl / return / assignment / call arg / switch subject) → an `EnumConstRef` (`EnumName.member`). Untyped (no sink) is rejected, as Zig requires |
| `@intFromEnum(e)` | ✅ | the enum's integer value → decay to the underlying type (the C enum→int decay) |
| `@intCast` / `@truncate` / `@floatFromInt` / `@intFromFloat` / `@floatCast` / `@enumFromInt` / `@ptrCast` (x) | ✅ | **result-location** casts (Milestone J) — single-arg, target inferred from the SINK (typed binding / return / assignment / call arg / nested `@as`), not a type arg → the C `Cast` IR. Used with no result location they're a clear error, as Zig requires. The cast follows Zig's NON-safe-mode semantics (no overflow trap — same stance as plain `+`) |
| `@bitCast(x)` | ✅ | same-size **bit** reinterpret (e.g. `f32`↔`u32`) → `System.Runtime.CompilerServices.Unsafe.BitCast<TFrom, TTo>` (AOT-clean, size-checked). Result-located like the casts above |
| `@alignCast(p)` | ✅ | identity in dotcc's managed model (alignment is unobservable); the enclosing `@ptrCast`/sink does the real conversion. Needs no sink, so its idiomatic `@ptrCast(@alignCast(p))` lowers to one cast |
| `@sizeOf(T)` | ✅ | the byte size as `usize` → the C `sizeof` IR (folded for a user aggregate via the layout model, else C#'s `sizeof(T)`) |
| `@memcpy(dest, src)` / `@memmove(dest, src)` / `@memset(dest, value)` | ✅ | the mem builtins over slices → the runtime `ZigMem.CopyForwards` / `ZigMem.Move` (overlap-safe: it copies backward when dest is past source) / `ZigMem.Set`. Element type inferred from the dest operand (a `[]T` slice, a `[N]T` array, or `&array`); `@memset`'s value lowers at the element sink. Both are void → rendered as bare statements. `@memcpy` reuses the forward-copy helper (correct for its non-overlapping, equal-length contract) |
| `@alignOf(T)` / `@offsetOf(T, "f")` | ✅ | Milestone T, part 4 — both are comptime values computed from dotcc's layout model (C-ABI / natural alignment). `@alignOf(T)` folds straight to a literal (the ABI alignment; a struct = the max field alignment). `@offsetOf(T, "field")` reuses the C `offsetof` IR — it folds in a comptime-required position (an array bound `[@offsetOf(T,"m")]u8`) and renders the .NET blittable-layout offset at a runtime use. Use an `extern struct` to pin the C field layout when an exact offset must match real zig (a plain Zig struct may reorder fields) |
| `@bitSizeOf(T)` | ✅ | the width in BITS (road-to-zig-std S7; 431 uses in real std, and the operand of most `@Int` calls). Answered from the type's DECLARED SPELLING — the same S5b machinery `@typeInfo(T).int.bits` uses, and for the same reason: dotcc widens `u21` to a 32-bit `uint`, so the lowered type would report 32 where zig says 21. The width therefore travels with a `comptime T: type` binding and an alias too. Falls back to the lowered type only for the kinds whose width is exactly recoverable: `bool` is 1 (zig's packed-field width, not the byte dotcc stores it in), `void` 0, a pointer / `usize` 64, an enum its tag's. **Cut (loud):** an AGGREGATE — zig's answer is its byte size in bits, and dotcc byte-packs its own layout, so the error points at `@sizeOf(T) * 8` as the deliberate opt-in to that approximation |
| `@Int(signedness, bits)` | ✅ | the type CONSTRUCTOR (road-to-zig-std S7; 207 uses, by far the largest of the family that replaced the old monolithic `@Type(info)` — which this zig does not have, so dotcc does not build it). `@Int(.unsigned, 18)` IS `u18`, so it goes through the one arbitrary-width rule the front end has. Signedness comes from the enum literal `.unsigned` / `.signed` (190 of the 207) or a folded `@typeInfo(T).int.signedness` (the rest); the width from any comptime-foldable integer — `@bitSizeOf(T)` (the commonest by far), `@typeInfo(T).int.bits`, a `comptime` parameter, a literal, or arithmetic over those. The constructed width RIDES the binding like a spelling does, so `@typeInfo(@Int(.unsigned, 21)).int.bits` answers 21 and the two halves round-trip. Oracle `reify_int_and_bitsizeof`; example `examples/zig-reify/`. **Cuts (loud):** a width outside 1..128 (needs BigInteger); a non-comptime width; `@Int` in a VALUE position (it builds a type); and the AGGREGATE constructors `@Struct` / `@Union` / `@Enum` / `@Pointer` / `@Fn` / `@Tuple` (≤14 uses each), which take comptime aggregate arguments — field-name and field-type arrays, an attributes struct — that dotcc has no comptime-aggregate engine for. A `fn X(comptime T: type) type { return @Int(…); }` works since the W4 lift, and its width rides the call (`@bitSizeOf(X(…))` is the constructed width) |
| `@compileError(msg)` / `@compileLog(…)` / `@setEvalBranchQuota(n)` | ✅ | the compile-time diagnostics (road-to-zig-std S7). **`@compileError` fires where it is REACHED, not where it is written** — zig's own rule, not a dotcc leniency: the reference says the error is raised "when semantically analyzed", and `if`/`switch` on compile-time constants is one of the several ways code avoids being checked. Lowering IS dotcc's semantic analysis, and the folds already installed are exactly those ways: S5a lowers only the taken `switch` prong (**231 of the 595 uses in real std are an `else =>` guard**), W3a folds a comptime `if` in a generic instance, S2 never lowers an unreferenced module decl. The author's message is carried through, including a `"…" ++ @typeName(T)` naming the offending type. **One shape needs a poison and gets one:** a top-level `pub const NAME = @compileError("use X instead");` — a DEPRECATION TOMBSTONE, 25 in the pin (std/meta.zig, std/os/windows.zig) — records the message, emits nothing, and raises only when something NAMES it, in a value or a type position; firing at the declaration would make those modules unimportable. `@compileLog` prints and then fails the build, as it does in zig. `@setEvalBranchQuota` RAISES the comptime eval budget and never lowers it (zig's rule); the units differ harmlessly in that direction — zig counts backward branches, dotcc counts eval steps, a finer unit, and dotcc's default is already far above the 2,000–100,000 std asks for. Oracle `compile_error_guards_fold_away` |
| `@min`/`@max`/`@rem`/`@divTrunc`/`@mod`/`@divFloor`/`@popCount`/`@clz`/`@ctz` | ✅ | math/bit builtins (road-to-zig-std B3) → `ZigMath.<helper><T>` (`[MethodImpl(AggressiveInlining)]`) over the peer-resolved operand type; operands evaluated once. `@min`/`@max` are Zig-variadic → V1 binary (a wider call is a clear arity error). `@rem`/`@divTrunc` = C#'s `%`/`/` (truncated); `@mod`/`@divFloor` follow the DIVISOR's sign / round toward −∞. `@popCount` is width-agnostic → `int`; `@clz`/`@ctz` count within the type's bit width (an arbitrary `uN` counts within its declared width since task #101: `@clz(@as(u5, 5))` is 2) → `int`. That width is the operand's ZIG type — zig has no integer promotion, so `@clz(bits - 1)` on a `u16` counts in 16 bits (dotcc narrows the C-promoted operand back); a comptime-known operand folds to its literal, which is what lets `std.math.Log2Int` size a type with one. `@byteSwap` → `ZigMath.ByteSwap<T>` (write-LE/reverse/read-LE, same type; exact for whole-byte widths). `@abs` → `ZigMath.Abs128` cast to the operand's UNSIGNED peer (`@abs(iN)` → `uN`, so `@abs(i8 -128)` = `u8 128` with no signed-abs overflow); float `@abs` not yet. Oracle `math_builtins`/`bit_ptr_builtins`/`byteswap_abs_builtins`. |
| `@addWithOverflow`/`@subWithOverflow`/`@mulWithOverflow`/`@shlWithOverflow` | ✅ | overflow-detecting arithmetic (road-to-zig-std B3) → `ZigMath.<op>WithOverflow<T>` returning Zig's `struct { T, u1 }` as a C# `(T, byte)` ValueTuple (`CType.Tuple([T, u8])`), so the result **destructures** (`const r, const o = @addWithOverflow(a, b);`) or **indexes** (`t[0]`/`t[1]`) through the existing tuple path. The wrapped result + 0/1 flag are computed EXACTLY in a 128-bit accumulator; add/sub/mul are peer-typed, `@shlWithOverflow`'s 2nd arg is a shift count. **Cut:** a 128-bit operand (no wider accumulator to detect its overflow) is a clear error. This is the `struct { T, u1 }` `addOrOom` uses in `std.ArrayList`. Oracle `overflow_builtins` |
| `@intFromPtr(p)` | ✅ | the pointer's address as `usize` → an unchecked `(ulong)ptr` cast (LP64). The value is a runtime address, so a program must derive something stable from it (a pointer difference, an alignment remainder, a null check) — as in real zig. Oracle `bit_ptr_builtins` (a deterministic `&a[1] − &a[0]` element-size difference) |
| `@Vector(N, T)` (SIMD) | ✅ | road-to-zig-std's target-identity segment T5 (the maintainer's choice): a numeric vector lowers to .NET's `System.Runtime.Intrinsics.Vector64/128/256/512<T>` chosen by total width, so element-wise work is the JIT's SIMD code (with a software fallback on a CPU without it); a BOOL vector, what a comparison yields, is a lane bitmask in a `ulong`. `@splat(x)` at a vector sink, an array / pointer to one / slice at a vector sink (loaded from its first element; `slice[i..][0..N].*` is the std idiom), a positional list literal `.{ 1, 5, 9, 5 }`, element-wise `+ - * & \| ^` (a scalar operand is splatted), `== != < > <= >=` as masks, a bool vector's `and` / `or` / `!=` as mask bit operations, `@reduce` (`.Or` / `.And` / `.Xor` over a mask, `.Add` / `.Min` / `.Max` over numbers), `@select(T, mask, a, b)`, a lane read `v[i]`, and `@typeInfo(V).vector.len` / `.child`. The operations route through `DotCC.Libc.ZigVec`. Real `std.mem.indexOfScalar` runs from source through its SIMD path, == zig (real-std differential `Dotcc_matches_zig_std_mem_index_of_scalar_simd_from_source`). Oracles `simd_vectors` / `simd_masks`; unit `ZigVectorTests`. **Cuts (loud):** a width with no .NET vector type (`@Vector(3, u8)`), `/` `%` and shifts on vectors, `@shuffle`, a runtime (non-comptime) bool-vector literal, more than 64 lanes in a bool vector |
| `@typeInfo(T)` | ✅ | comptime reflection (road-to-zig-std S5) — the `std.builtin.Type` value is **synthesized from the resolved `CType`**, never by compiling `std/lang.zig` for its layout, and every use folds at LOWERING time (no `TypeVal` in the comptime interpreter — the value/type firewall stands). `switch (@typeInfo(T))` selects its prong at lowering time and lowers ONLY that arm — necessarily so, since every other arm is written for a different kind and would not lower for this type; the quoted tags `.@"struct"`/`.@"enum"`/`.@"union"`/`.@"fn"` dispatch like any other. A `\|i\|` prong capture binds the payload, and `const info = @typeInfo(T);` binds a comptime-only name (no decl emitted). Payload fields: `signedness` (`== .signed` folds to a bool literal), `child` (a TYPE — resolves in type positions), `is_const`, `len`, and `bits`. **`bits` needs the DECLARED width**, which the lowered type cannot carry: dotcc widens an arbitrary-width `uN`/`iN` to the smallest standard width (`u21` → a 32-bit `uint`). The declared width therefore rides the type BINDING — seeded, shadowed and restored in lockstep with it — so `@typeInfo(T).int.bits` answers 21 whether `u21` is spelled directly, bound to an alias (`const Cp = u21;`), or passed as a `comptime T: type`. It also joins the instance KEY, so `bitsOf(u21)` and `bitsOf(u32)` are distinct specializations instead of one shared answer (every standard spelling declares exactly its lowered width, so no existing mangled name changed). Still a loud cut where no spelling survives at all: an `anytype` param or `@TypeOf(expr)`, whose type is inferred from a VALUE. **Member LISTS (S5c):** this zig exposes PARALLEL ARRAYS rather than a slice of field structs — `field_names` / `field_types` / `field_values` (+ `tag_type`) all fold, together with `.len` (the commonest reflection op in real std, 167 uses), a bounds-checked comptime INDEX, and a `const` binding. Lists are built from dotcc's own registries (declared field order / enum member order), never by compiling `std/lang.zig`, and none reaches the IR. **`inline for` walks them (S6)** — see the loop-unroll row. **Cuts (all loud):** `decl_names` (dotcc's const/method registries are NAME-keyed, so declaration ORDER isn't available — `@hasDecl` works, since membership needs none); `tag_type` of an INFERRED enum tag (zig infers the smallest unsigned int holding the largest member, dotcc defaults to `int`, so answering would disagree on width — spell it, `enum(u8) {…}`, and it works); `field_attrs`; `pointer.size` of a raw pointer NO spelling classifies (dotcc collapses `*T`/`[*]T`/`[*c]T` to one C pointer, so the class rides the source spelling instead, task #119: a SLICE answers `.slice`, and `&x`, a typed local, a parameter, an alias or an `anytype` argument answer theirs; a call result or a struct field read does not), reading an inactive tag, `@typeInfo(T)` as a runtime value, a `\|*x\|` by-ref prong capture. Oracles `typeinfo_kind`/`typeinfo_signedness`/`typeinfo_bits`/`typeinfo_child`; example `examples/zig-typeinfo/` |
| `@hasField(T,"n")` / `@hasDecl(T,"n")` / `@field(x,"n")` | ✅ | comptime MEMBERSHIP + comptime-named field access (road-to-zig-std S5c; 73 + 220 uses in real std). Both queries fold to a boolean literal from the same registries the member lists read — fields/enum members for `@hasField`, container `const`s + methods for `@hasDecl` — and need no declaration ORDER, which is exactly why they work where `decl_names` does not. `@field` re-enters the shared member path once the comptime-string name folds, so the emit is indistinguishable from `x.n` (a pointer receiver auto-derefs the same way). The member name must be a comptime string literal |
| wrapping ops `+% -% *%` (+ `op%=`) | ✅ | two's-complement wrap (Milestone P, part 1) — see the operators table above |
| saturating ops `+\| -\| *\|` (+ `op\|=`) | ✅ | clamp-to-range (Milestone P, part 2) — see the operators table above |

## Lexer

| Feature | Status |
|---|---|
| decimal integers, floats, `"…"` strings, char literals `'x'` (`\n \t \\ \' \xNN`), `//` line comments, `@name` builtins | ✅ |
| hex/octal/binary integers `0x1F`/`0o17`/`0b1010` + `_` digit separators `1_000_000` | ✅ | radix + `_` decoded in `DecodeZigInt` (Zig's `0o` octal / `_` separator, UNLIKE C's bare-`0` / `'`); the literal's carrier type is the narrowest of int/uint/long/ulong holding it |
| hex float `0x1.8p3` + underscored float `1_000.5` + exponent-only float `1e10`/`4E2` | ✅ | hex float has no C# syntax → converted to a round-trippable decimal via the shared `EmitHelpers.LowerHexFloat`; an exponent-only decimal (no fraction dot) is a distinct FLOAT lexer rule and passes through as a C# double verbatim |
| multiline `\\` strings | ✅ | a run of `\\`-prefixed lines folded into one literal, lines joined by `\n`; escapes are NOT processed (raw content), matching Zig |
| `\u{NNNN}` unicode escapes (string + char, incl. codepoints > 0xFFFF), escaped-quote `\"` in a string | ✅ | in a STRING, `\u{…}` expands to its UTF-8 bytes Zig-side via `char.ConvertFromUtf32` (the shared decoder is untouched) — a non-BMP codepoint like U+1F600 becomes its 4 UTF-8 bytes, no special surrogate handling; in a CHAR, the value is the codepoint itself as an int (a `comptime_int`, exactly as Zig models it), so non-BMP needs no surrogate either; `\"` is an escaped quote (the old `"[^"]*"` rule truncated there) |
| `0X`/`0O`/`0B` (uppercase radix prefix) | 🚫 | **not valid Zig** — real zig rejects it (`error: base prefix must be lowercase`), so it's deliberately not lexed (adding it would accept programs the reference compiler rejects). The `DecodeZigInt`/`LowerZigFloat` decoders already tolerate an uppercase prefix defensively, but the lexer never produces one |

## Out of scope (the dialect line)

Two tiers, since the wall-breaking plan (`fable-wall.md`, 2026-07-05):

**The planned arc W0–W6 is COMPLETE — later work only *widens the curated `std` set*, the rest are loud errors:** the
comptime-**type** foundation SHIPPED — type-as-value aliases (`const T = i32;`, with
`*T`/`?T`/`[]T` composing) and `@TypeOf` (**wall-plan W1**), in-function struct
decls (**W2**, the reify-a-struct primitive), **generic functions via `comptime`
VALUE params** (**W3a** — call-site monomorphization: one specialized, memoized body
per resolved value, with comptime-if folding so recursive generics terminate),
**generic functions via `comptime` TYPE params** (**W3b** — the core generic case: the
signature DEPENDS on `T`, so it is lowered per-instantiation, keyed by the resolved type
so an alias for `i32` shares the `__i32` instance), **type-RETURNING functions**
(**W4** — a comptime type constructor `fn Pair(comptime T: type) type { return struct {…}; }`
that reifies a fresh struct per resolved type argument via the W2 primitive), **generic
functions via `anytype` params** (**W5** — the parameter type is INFERRED from the argument
and keys a specialization, with duck-typed body use failing per-instantiation), and
**`std.debug.print`** (**W6** — the comptime format parsed at lowering time, its `{…}`
placeholders paired positionally with the tuple, lowered to a stderr `fprintf`) — all in the
Types table. Today `@import("std")` resolves
only the curated paths: the allocator paths (`std.mem.Allocator` +
`std.heap.page_allocator`/`c_allocator`/`FixedBufferAllocator`/`ArenaAllocator`,
with `alloc`/`free`/`create`/`destroy`/`realloc` — see the Allocators section),
the `std.mem` slice helpers, `std.ArrayList(T)`, `std.debug.print`, and the `std.testing` assertions
(`expect`/`expectEqual`/`expectError`/`expectEqualStrings`/`expectEqualSlices` — see **Test runner** below).
**Plus, when a real std source tree is configured** (`DOTCC_ZIG_LIB_DIR` — road-to-zig-std S1/S2/G1),
a NON-curated path navigates the actual upstream files and lowers LAZILY (only the decls a program
references), which is how real `std.ascii` compiles from source today. A curated path always wins over
navigation — upstream re-exports its allocators as whole files, so `std.heap.FixedBufferAllocator` is
both, and only the curated model can lower it. With no std tree configured, anything non-curated errors
clearly.

**`@import("builtin")` and `@import("root")`** resolve too (road-to-zig-std S3). Neither is a file:
real zig GENERATES `builtin` per compilation to describe the target, and dotcc does the same — a
module written as Zig source at compile start and fed through the ordinary import path, so it parses,
binds and navigates like any other module with no special lowering. It is deliberately **duck-typed**
(bare enum literals and anonymous struct literals carrying the fields std reads) rather than typed
against `std.Target`, which would pull in ~37,000 lines of per-architecture CPU feature tables
describing hardware dotcc does not target — the program it emits is C#. The surface is what std
actually reads, measured across the 195 pinned files that import it: `cpu.arch` (172), `os.tag` (92),
`link_libc` (99), `zig_backend` (87), `single_threaded` (46), `mode` (43), `abi` (42), `target.*`
chaining to the same, plus `strip_debug_info`, `unwind_tables`, `is_test`, `fuzz` and the rest of the
≤5-use tail. **`root` is EMPTY on purpose** — std probes it for optional overrides (`std_options`,
`panic`), and a program that declares none is exactly what an empty module describes. Two deliberate
choices are load-bearing: `link_libc = true` (biases std toward libc-backed paths, which land on
`extern fn`s dotcc's runtime implements) and `mode = .ReleaseFast` (dotcc does not trap integer
overflow, so claiming a safe mode would have std emit checks dotcc does not honour — note this
DIVERGES from `zig build-exe`'s `.Debug` default, the one member a differential program must avoid).
**Cuts (loud):** the `std.Target` METHODS — `cpu.has(…)` (19 uses), `target.isGnuLibC()`,
`ptrBitWidth()`, `stackAlignment()` (~30 call sites total) — each needs a real type to hang on, and
each is a separate decision about what dotcc's target actually is.

**Navigation reaches TYPES, not just functions** (road-to-zig-std **S4d**). A dotted type that no
curated row claims (`util.Point`, `std.ascii.Pair`) resolves through the module graph to the container
that module declares, also as the base of a static call (`h.H.hash(0, 0)`) or as a type alias rooted at an
inline import (`const H = @import("h.zig").H;`, oracle `module_qualified_type_paths`); a module-qualified call in a type slot (`list.Box(u8)`,
`std.array_list.Aligned(u8)` — the shape `std.ArrayList(T)` has) reifies the imported template, in
*its* module's environment, keyed by the type argument resolved in the *caller's*. A navigated type is
usable, not merely nameable: its **methods** are declared on first call (a prepared module deliberately
doesn't declare them up front — an unreferenced decl must stay invisible) and its **enum members**
resolve at a sink typed by it. A resolvable module that declares no such type is a loud error naming
both the file and the type. Oracle `import_type` / `import_type_method` / `import_enum_member` /
`import_generic_type`; example `examples/zig-module-types/`; unit `ZigFrontendTests` +
`ZigCuratedStdVsNavigationTests`. **Generic FUNCTIONS cross the seam too** (road-to-zig-std G3 —
`std.fmt.bufPrint`'s wall): a call `util.f(args)` to a `comptime` / `anytype` export instantiates in
*its* module, while every argument — a type, a comptime value, a comptime string, an `anytype`
argument's inferred type — is read in the *caller's* scope (oracle `import_generic_fn`; unit
`ZigCrossModuleGenericTests`). **A FILE can be a struct type** (road-to-zig-std G3, `std.Io.Writer`'s
shape): a file with container fields at file scope (`vtable: *const VTable, buffer: []u8, end: usize = 0,`)
IS a struct, named by `@This()` at file scope. Its fields register as an ordinary struct
(`Io_Writer__Writer`; a root unit's is `root__<file stem>`), and its top-level functions are its
methods: a decl literal (`var w: Writer = .fixed(buf)`), a static call (`Writer.fixed(buf)`) and a
method call (`w.buffered()`) all reach the one top-level function, lowered on demand like any lazy decl.
The module is named in a type position through its import (`const Box = @import("Box.zig");`) or
through an ALIAS of a module path (`const Writer = std.Io.Writer;`), and a module-path alias navigates
like the import it names in every position (`const math = std.math; math.maxInt(…)`), which is how std
reaches nearly every other file. A container of an IMPORTED module that cannot lower (`std.Io`'s
`Limit` enum, whose `math.maxInt(usize)` member needs the comptime engine) no longer sinks the whole
import: it is withdrawn and raises its original message where something first names it, zig's
analyse-on-reference rule. A lazy module's top-level VALUE `const` (mem.zig's
`use_vectors_for_comparison = use_vectors and !builtin.fuzz`) is recorded raw and lowered where it is
named, its annotation the sink, since zig evaluates a top-level const's initializer at comptime (a
const depending on itself is a loud dependency-loop error; oracle `lazy_value_consts`, unit
`ZigLazyValueConstTests`). A prong's case list may end in a trailing comma (`.a,\n .b,\n => …`), and
`@inComptime()` is `false` in the code dotcc lowers (a type-returning generic's body, which the interpreter runs, keeps both arms and takes the comptime one). A local comptime ALIAS of a function
(`const add = switch (sign) { .pos => math.add, .neg => math.sub };` in std.fmt.parseIntWithSign, a
generic of another module chosen by a comptime switch or `if`) emits no decl, and a call through it
instantiates the function it names (oracle `fn_alias_and_comptime_values`; unit `ZigFnAliasTests`).
Around it: a comptime BOOL bound from a type comparison (`const is_comptime = @TypeOf(x) ==
comptime_int;`), a value `if` whose condition folds lowering only its taken arm, a value switch with a
`return` arm, variadic `@min` / `@max` folding when comptime-known, and a parenthesized type
(`fn add(…) (error{Overflow}!T)`). **The CLOSURE idiom and comptime FUNCTION parameters** (std.mem.sort's
`comptime lessThanFn: fn (@TypeOf(context), lhs: T, rhs: T) bool` given `std.sort.asc(u8)`): a function whose
body is `return struct { pub fn inner … }.inner;` stands for that method (its anonymous struct reifies when the
instance is created, with the instance's seeds); a comptime function argument (a function name, a comptime
function parameter passed along, or such a call) keys the callee's instance and is called directly; the idiom
also works in expression position (`const inc = struct { fn f … }.f;`). With it: an inline `@import("…").name`
re-export, `noalias` (ignored), by-reference pair captures `|*a, *b|`, a nested `switch` as a statement prong
body whose prongs return (std.math.sqrt; oracle `nested_statement_switch`), and a `comptime { … }` prong body
(oracle `closure_idiom_fn_params`; unit `ZigClosureIdiomTests`). **Cut:** the expression-position idiom's
methods drain without the enclosing instance's seeds. Oracle `import_file_struct`; example `examples/zig-file-struct/`; unit
`ZigFileStructTests`. **A GENERIC top-level function is a method too** (`w.print(fmt, args)`, with
`fn print(w: *Writer, comptime fmt: []const u8, args: anytype)`): called on an instance it instantiates
in its own module with the receiver as the runtime first argument (auto-ref'd like any method's), and
called through the type (`Box.addAny(&b, …)` via the import, `Writer.print(w, …)` via the module's own
`@This()` alias) it is an ordinary exported generic call (oracle `file_struct_generic_method`). A method
the resilient parse SKIPPED raises its parse error instead of "no method". **A pointer to a container that could not lower is
OPAQUE** (`void*`): zig never needs a pointee's layout to size a pointer, so `Writer.VTable.sendFile`'s
`*File.Reader` lowers although `File` sits on the platform floor (`handle: std.posix.fd_t`). A container
that holds a failed one BY VALUE (`File.Reader`'s `file: File`) fails with it, to a fixpoint, and is not
emitted; a use that needs the layout (a field access, a deref) still fails loudly, on the `void*`, whose
message does not yet name the original failure. Unit `ZigOpaquePointerTests`. **RE-EXPORTS resolve** (road-to-zig-std G3/G5; 633 in the pin): a top-level
`const NAME = name;` or `const NAME = mod.name;` names a declaration made elsewhere, and a lookup of
NAME follows the chain to the module that owns it: a function or generic (`pub const indexOfScalar =
findScalar;`, which instantiates in its own module), a type-returning generic
(`pub const AutoHashMap = hash_map.AutoHashMap;` in a type position), a container type
(`pub const Pair = inner.Pair;`, also through a root alias `const P = lib.Pair;`). A root alias of an
imported function is a call, not a runtime global; a same-file `const f2 = f;` keeps its
function-pointer global. Oracle `import_reexports`; unit `ZigReexportTests`. **Curated std namespaces
claim only their curated members:** with a std tree configured, `std.mem.X` / `std.debug.X` /
`std.testing.X` for a member dotcc does not model lowers from real source instead of stopping at "not
modeled yet" (unit `ZigCuratedStdVsNavigationTests`). **Imported functions are
module-qualified in the emitted C#** too (`util__f`, `util__maxOf__u8`), since every module's functions
share one emitted class. **Cuts (loud):** a cross-module container `const` (`k.Cfg.MAX`).
**Container names are
module-qualified in the emitted C#** (2026-09-23): an imported module's containers, reified generic instances
and inline anonymous structs emit as `<module>__<Name>` (`fmt__Alignment`, `list__Box__u8`; the module's
path relative to the std root, else its file stem), while its own code and its importers still spell the
plain name — so two modules may declare the same name, and a root-module type named like one of dotcc's
runtime types (`Allocator`, `Slice`, …) emits as `root__Allocator`. (A type-returning
body that returns another, module-qualified type-returning CALL — `return array_list.Aligned(T, null);`,
what `std.ArrayList` itself is — resolves since the W4 lift.) See
[`plans/deferred.md`](plans/deferred.md).

### Test runner — `dotcc zig test`

A source file's `test "name" {}` / `test ident {}` / `test {}` blocks (at top level, or as a MEMBER of a
struct / enum / union body, as std.math.Order's `test invert { … }` is) are **parsed and dropped in a
normal build** (they are analysis-only there), but `dotcc zig test <file.zig>` compiles them and **runs
each**, reporting `OK`/`FAIL` per test and a summary, exiting non-zero if any fails — the harness for
running real `std` tests from source. Each block lowers to a runnable `anyerror!void` function; a test
PASSES when its body returns normally and FAILS when it returns an error (a propagated `try`, an explicit
`return error.X`) or panics. `main` is ignored in test mode (as in real `zig test`). The curated
assertions — `expect(ok)`, `expectEqual(expected, actual)`, `expectError(expected_error, result)`,
`expectEqualStrings(a, b)`, and `expectEqualSlices(T, a, b)` — each return an error union so `try`
propagates a failure to the test boundary; more grow on demand. The runner's output is dotcc's own shape
(real `zig test` output is timing-dependent, so it is not byte-matched).

**Permanent:** `async`/`suspend`, inline assembly (the managed-target root below —
that reasoning still holds and is not relitigated), and `std`'s **platform floor**
(`std.os` / `std.fs` / `std.Thread` bottom out in raw syscalls, per-OS structs, and
`std.os.linux` inline `asm`; a managed target has no syscall surface, so these must be
*redirected* onto the BCL — exactly as C's libc already is — not lowered from source).

**Buildable, not built (deliberately NOT called permanent — the earlier "full `std`
stays curated-paths forever" line overstated it):** `std` *is* Zig, so it flows through
the same front-end as user code — there is no *language* barrier. Its comptime-light,
platform-free leaves (`std.mem`, most of `std.math`, `ArrayList`) can increasingly be
**compiled from the upstream source** as the front-end fills in; the curated runtime
types (`ZigList<T>`, the `std.mem` helpers) are a *shortcut*, not a wall. The one real
unbuilt brick between here and pervasive `std` is std's **comptime-reflection codegen** —
a `switch (@typeInfo(T))` over a type's shape that `std.fmt` / `std.meta` lean on
top-to-bottom — which is buildable on the Milestone-T interpreter but not built yet
(W6 side-steps it for `std.debug.print` via a lowering-time format-string parse).

(`opaque` type declarations stay 🚫 too — neither tier claims them; a small
demand-driven item, unrelated to the comptime arc.)

(For orientation, things once on this line that ARE now supported: the
union kinds — tagged `union(enum)`, explicit-tag `union(SomeEnum)`, and untagged
`union { … }` — are all ✅ as of Milestone R; data-only `struct`/`enum`/`union`
**with methods** — struct/enum/union methods + the `const Self = @This();` self-type alias
+ namespaced VALUE `const`s + namespaced mutable `var`s ARE supported — see below.
Explicit error-SET declarations `error{A,B}`,
inferred `!T`, and `error.X` ARE supported — Milestones N/X, see the error-unions rows.
Both `.{…}` and typed `T{…}` init lists ARE supported, including `&T{…}` —
address-of-a-temporary — via a materialized block-local temp.)

### Why these are out (and which half is coming back) — the reasoning, not just the line

The cuts aren't arbitrary. dotcc is a **syntax-directed transpiler**: it lowers parsed
syntax to a C-shaped IR and emits C# (or wat). It hosts a compile-time **value**
evaluator (Milestone T) but no type-level semantic analyzer, and it targets a
**managed VM**, not native machine code. Every item above falls out of one of those
two roots — and the two roots now have different fates: the comptime root is a
**planned arc** (`fable-wall.md`, 2026-07-05), the managed-target root is permanent.

**The comptime root (planned)** — catches comptime **types**, generics / `anytype`, and a
curated-only `std`. comptime splits cleanly in two by one rule: does the evaluation produce a
**value** or a **type**? Value-comptime — `comptime EXPR`, a `comptime fib(10)` call, comptime
constants and array bounds — **IS supported** (Milestone T): dotcc hosts a small tree-walking
interpreter over its own typed IR (the same engine the C `#if` / array-bound folder uses), with
call frames, loops, and an eval-step budget. What isn't lowered yet is comptime **types**: a
`comptime` expression that produces or consumes a `type` (a `type`-returning fn, `comptime T:
type`, comptime struct construction). That half is *generative* — it monomorphizes the IR.
An earlier revision of this section ruled it out as "needs an interleaved semantic analyzer
(Zig's `Sema` shape), a different compiler than dotcc's bottom-up pipeline" — **that
conclusion was too strong**. A *general* Sema, yes; but Zig generics are comptime-driven
**monomorphization** — a fresh, differently-*shaped* function body instantiated per type at
each call site — and that specific shape fits dotcc: **demand-driven, memoized re-lowering of
retained ASTs**, keyed per call site's resolved comptime arguments, with the Milestone-T
interpreter providing the binding frames (the C front-end already retains function subtrees;
`_Generic`'s lowering-time type matcher and `constexpr`'s ConstEval substitution are the other
accumulated ingredients). What remains true: a generic body can't map onto an **open C#
generic** (C# can't change a body's shape per `T`, and has no *value*- or *type*-valued
generic argument) — so user code emits one concrete method/struct per instantiation
(`max__i32`), and open C# generics appear only in runtime types WE author (`Slice<T>`,
`ErrUnion<T>`, the planned `ZigList<T>`). Generics ⊂ comptime. `std` itself is
*Zig* — it flows through the same front-end as user code, so "curated" is a **shortcut, not a
language wall**. It splits into three honest tiers, not one flat "curated forever":
(1) **leaf std** — the comptime-light, platform-free pieces (`std.mem`, most of `std.math`,
`ArrayList`) — can increasingly be **compiled from the upstream source** as the front-end
fills in; the curated runtime types (`ZigList<T>`, the `std.mem` helpers) are just a
head-start, and the honest move is to drop the curation as coverage grows.
(2) **std's comptime-reflection core** — `switch (@typeInfo(T))` codegen over a type's shape,
which `std.fmt` / `std.meta` lean on top-to-bottom — is the one real *unbuilt* brick: buildable
on the Milestone-T interpreter (return a comptime type-info struct the interpreter can switch on
and emit per-field code from), just not built, and the reason "compile real `std.fmt`" isn't
free today. (3) **std's platform floor** — `std.os` / `std.fs` / `std.Thread` bottoming out in
syscalls + inline `asm` — genuinely can't be *lowered* onto a managed VM; it must be *redirected*
onto the BCL, the same seam C's hand-written libc runtime already is. So the arc *widens* the
curated set (`std.ArrayList(T)`, `std.debug.print` with a lowering-time format-string parse
instead of comptime reflection) while the reflection engine is unbuilt, and leans on upstream
source where it can — only tier (3) is truly permanent. The biggest tuple/`.{…}` consumer —
`std.fmt`'s `print("{} {}", .{a, b})` — lives in tier (2) (comptime reflection over the arg
tuple), and is side-stepped today by routing formatting through `extern fn printf` + libc.

**The managed-target root (permanent)** — catches inline assembly and `async`/`suspend`. Inline `asm`
emits raw target machine code; the C# and wat backends run on a VM with nowhere to put it
(C# has no inline-asm escape hatch) — untranslatable by construction, the same wall the C
front-end hits. `async`/`suspend` is a double miss: Zig's stackless coroutines with an
explicit, caller-owned `@Frame` (take `&frame`, store it, `resume` it by hand) don't map
onto .NET's scheduler-driven `async`/`await` without a lossy translation — *and* async was
removed from the pinned Zig, so it's a feature the reference compiler doesn't even have
(the differential oracle couldn't validate it anyway).

**The soft case** — destructuring assignment was "not yet", not "can't", and it has now
landed (Milestone G — see the **Tuples** section). It needed tuple types (positional anonymous
structs), which lower cleanly onto C# `ValueTuple` for the **runtime** subset — value semantics,
positional access, comptime-known fixed arity, and native deconstruction. Only the comptime
*flavor* of tuples stays out (type-valued / `comptime_int` fields, and the `std.fmt` reflection
idiom above) — the comptime root again.

## Mixed `.c` + `.zig` translation units

A single invocation may mix C and Zig: `dotcc main.c helper.zig -o app`. Both
groups lower into **one** IR module (the C group builds the module — preprocessor,
structs, globals — and the Zig group lowers into it), so the program emits once and
a call across the language boundary resolves at the C# level (every function is a
`DotCcProgram` method called by bare name). Each side declares the other's functions
the normal way — a C prototype (`int add(int, int);`) for a Zig function, an
`extern fn` for a C function — and they link. C structs/enums are preserved.

```c
// helper.c
int add(int a, int b);            // defined in add.zig
int main(void){ return add(40, 2); }
```
```zig
// add.zig
pub fn add(a: c_int, b: c_int) c_int { return a + b; }
```

Limits: `-shared` / `-l` import mode combined with a mixed set is not validated yet
(single-language only); cross-language **struct/type sharing** is moot until the Zig
front-end emits aggregates.

### C↔Zig shared-heap interop (Milestone V)

The allocator abstraction is shared with C at the **heap** level: `std.heap.c_allocator`
devirtualizes to a direct `Libc.malloc`/`free`/`realloc`, which is the *same* heap C's
`malloc`/`free` use. So in a mixed program, memory crosses the seam in every direction:

| Pattern | Why it works |
|---|---|
| Zig `a.alloc(T, n)` (`c_allocator`) → C `free(p.ptr)` | both are the one `Libc` heap |
| C `malloc` → Zig reads the `[*c]T` → C `free` | a C pointer indexes/reads in Zig directly |
| Zig `a.create(T)` → C reads/writes `*T` → `a.destroy` | a single-object heap cell, shared |
| Zig `a.realloc(slice, n)` of a heap slice; C `sum`s the result | `realloc` is the shared `Libc.realloc` |
| a Zig fn taking an **opaque** `std.mem.Allocator` param, fed `c_allocator`, its buffer handed to C | the default materializes `ZigAlloc.CHeap()`; the buffer is plain heap memory |
| a C `lua_Alloc` (`extern fn`) wrapped in a Zig custom-vtable `std.mem.Allocator` (Milestone W, part 2) | the adapter's vtable `alloc`/`free` call the imported C fn-pointer, bound by bare name across the seam |

**Only `c_allocator` is cross-seam-safe.** Real zig's `page_allocator` is mmap/VirtualAlloc —
a *different* heap from C's `malloc` — so freeing its memory with C `free` would be UB. dotcc
happens to back both with `Libc.malloc`, but a portable mixed program must use `c_allocator`
for any memory that crosses the boundary. Example: `examples/zig-c-heap` (a mixed program
where a Zig `c_allocator` buffer is summed + freed by C, and a C `malloc` buffer is read by
Zig), and `examples/zig-lua-alloc` (a C `lua_Alloc` realloc allocator consumed by Zig as a real
`std.mem.Allocator` via a hand-written adapter — Milestone W, part 2); the `ZigOracleTests` mixed
differential (`mixed_shared_heap`, `mixed_create_realloc`, `mixed_alloc_param`, `mixed_lua_alloc`)
re-checks each against real zig 0.17.

## Error unions — `try` is exception-based (the setjmp pattern, reused)

A `!T` function lowers to the runtime value type `ErrUnion<T>` (`DotCC.Libc/ZigErr.cs`,
auto-spliced like `CBool`) — either a payload (`Code == 0`) or a non-zero error code.
`return e;` → `ErrUnion<T>.Ok(e)`, `return error.Foo;` → `ErrUnion<T>.Err(code)`, and a
`!void` body that falls off the end returns `ErrUnion<Unit>.Ok(default)` (C# has no
generic-over-void, so `!void` uses the `Unit` payload).

The hard part is `try e`: it must unwrap the payload OR abort the *current* function with
the error, from **any** expression position (`const v = try f() + 1;`). Structured C# can't
express early-return-out-of-an-expression — so dotcc reuses the exact mechanism the C
front-end uses for `setjmp`/`longjmp`: `try e` lowers to `ErrUnion.Try(e)`, which throws a
private `ZigErrorReturn` on error, and every `!T` function body is wrapped in
`try { … } catch (ZigErrorReturn __e) { return ErrUnion<T>.Err(__e.Code); }`, converting the
propagated error back into an error-union return. `e catch fallback` doesn't propagate, so it
lowers to the plain `ErrUnion.Catch(e, fallback)` instead.

A bare `error.Foo` is now a first-class VALUE (Milestone N, part 1): the named set is still
erased, so an error value IS its flat `ushort` code (typed `CType.ErrorSet`), and error-value
equality `e == error.Foo` / `e != error.Foo` compares codes. This makes a `const`-bound error and
a USED `else |e|` capture usable (the latter un-erases the Milestone M part-3 cut — a named `|e|`
compared against an error is finally valid in both compilers). Because an error value is its code,
a `switch (e)` on an error (Milestone N, part 2) lowers to an ordinary integer `switch` on the code
(`error.Foo` → `case <code>:`, `else` → `default:`) — it rode in on the part-1 representation.

`catch` now supports a SIDE-EFFECTING fallback and a `catch |e|` capture (Milestone N, part 3):
both lower lazily (hoist the union to a single-eval temp, run the fallback only on error via a
ternary; the capture binds `e` to the flat error code, usable as `e == error.Foo`). These need a
statement context (a `const`/`var` initializer).

**V1 limits** (documented, not silent): the error SET is erased to one flat global code
space (`!T` / `anyerror!T` / `E!T` lower identically; an `error.Foo` value is its code); the
payload must be a value type (an error union over a *pointer* is deferred — a C# generic can't
take a pointer arg); a side-effecting / capturing `catch` lowers lazily. In a SUB-expression
(`x + (a catch b())`) it — and the control-flow fallbacks `catch return`/`orelse return` — are
hoisted to a `__anf` temp before the enclosing statement (the ANF statement-hoist), preserving
eval order: hoisting past a side effect evaluated earlier in the same statement is a clear error
(bind it to a `const` first) rather than a silent reorder. The control-flow fallbacks `catch return
[v]` / `orelse return [v]` (Milestone N part 6) also work at a full RHS or statement (an early
`return` on the error / none path); `catch |e| return e` is just `try` (use `try`). A `catch |e| switch (e)
{ … }` over a `!void` whose result nobody binds (array_list's `shrinkAndFree`) is a STATEMENT switch on the
error path, so its prongs may be void blocks (oracle `void_catch_switch_and_member_tests`). An
error-union `main`
(`!void` / `!u8`, Milestone N part 4) IS supported — an error from main reports its flat code to
stderr and exits 1 (real zig prints the error NAME + a trace; the name awaits the un-erased set).
Explicit `error{A, B}` set declarations (Milestone N part 5) are supported — dotcc erases the set,
so the decl is comptime (registers names, emits nothing) and `E!T` lowers like `anyerror!T`.
**`@errorName(e)` is supported** (Milestone X, part 1): even though the set stays erased to a flat
code, dotcc carries the code→name map into the emit as a `__zigErrorName(code)` helper, so
`@errorName` returns the real name as `[]const u8` — a `ConstSlice<byte>` over the RVA-pinned UTF-8
name bytes (`L("Foo"u8)`). A **set-qualified `E.member`** reference is supported too (Milestone X,
part 2): `MyError.Boom` resolves to the same flat code as the bare `error.Boom` (membership erased,
so it's the same value — as real zig treats it), usable as a value, in a comparison, and in `return`
position. **Error-set membership is now CHECKED** (Milestone X, part 3) — dotcc keeps the flat
runtime code but is a good compiler and rejects illegal programs real zig also rejects: a
`return error.X` / `return E.X` of an error outside a function's DECLARED set
(`fn f() error{A}!u8 { return error.B; }` → error), and an `E.member` whose member isn't declared in
`E`. An inferred `!T` / `anyerror!T` stays unconstrained (any error — real zig infers the set).
An **error set used as a plain VALUE type** is supported too (Milestone X, part 3b): a named set
(or `anyerror`) as a parameter, a `var`/`const`, or a non-`!T` return (`fn worst() MathError`)
denotes the error VALUE itself — lowered to the same flat `ushort` code (NOT an `ErrUnion`), so it
passes, compares, and `switch`es like any error value. The **exhaustive error `switch` without
`else`** rides on it: real zig proves the prongs cover every member, so no `else` is allowed; dotcc
can't prove coverage over the erased code, so a switch EXPRESSION collapses its last prong to the
`_` default (semantics-preserving for the exhaustive program — only that prong's values reach it),
keeping the emit warning-clean (also applied to enum switch-expressions). STILL deferred: real error
NAMES in a `main` error-trace (the trace still prints the flat code); a statement-form set-`switch`
whose every prong returns (the function-exhaustiveness collapse, like the union switch, isn't yet
applied to a non-union statement switch); and set-checking an error that flows in through a CALL or
`try` (only the direct `return` form is checked). `errdefer |e|` capture is NOT pursued (current Zig
removed the syntax).

## Allocators — devirtualize the default, vtable for the rest

dotcc models Zig's `std.mem.Allocator` as a fat pointer `{ ptr, vtable }` (the runtime
`Allocator` value type in `DotCC.Libc/ZigAlloc.cs`, auto-spliced) whose high-level
`a.alloc(T, n)` / `a.free(s)` dispatch through a vtable of raw function pointers (the real-zig
4-fn `{ alloc, resize, remap, free }` `std.mem.Allocator.VTable` shape — see the custom-allocator
note below); `alloc`
returns `Error![]T` (an `ErrUnion<Slice<T>>`, composing with `try`/`catch` above). **Three**
allocators ship: the C heap (the `std.heap.page_allocator`/`c_allocator` default, backed by
`Libc.malloc`/`free`), `std.heap.FixedBufferAllocator` (a deterministic bump allocator over a
caller buffer), and `std.heap.ArenaAllocator` (Milestone U — a growing arena over a backing
allocator that frees wholesale at `deinit()`; it grows like zig's, the current chunk in place through the
backing allocator first, else a new chunk sized from what is needed, task #120). The full method surface is `alloc`/`free`,
`create`/`destroy` (single-object, Milestone U), and `realloc` (Milestone U).

**Devirtualization is the optimization layer.** At each allocator-method site the lowering asks
*can it prove which concrete allocator `a` is?*

| Form | Lowers to |
|---|---|
| `const a = std.heap.page_allocator;` / `c_allocator` | comptime binding (no decl); `@import("std")` likewise |
| `a.alloc(T, n)` / `a.free(s)` on the **provable** default | **DIRECT** `ZigAlloc.AllocCHeap<T>` / `FreeCHeap<T>` (a `Libc.malloc`/`free`, no vtable) |
| `a.create(T)` / `a.destroy(p)` on the provable default | **DIRECT** `ZigAlloc.CreateCHeap<T>` / `DestroyCHeap<T>`. `create`'s `Error!*T` is carried as `ErrUnion<nuint>` (a pointer can't be an `ErrUnion<T>` generic arg); `try a.create(T)` casts the unwrapped address back to `*T` |
| `a.realloc(slice, n)` on the provable default | **DIRECT** `ZigAlloc.ReallocCHeap<T>` (a `Libc.realloc`); preserves contents up to the smaller of old/new |
| `var fba = std.heap.FixedBufferAllocator.init(&buf);` | `FixedBufferAllocator.Init((byte*)buf, N)` |
| `var arena = std.heap.ArenaAllocator.init(backing);` … `arena.deinit();` | `ArenaAllocator.Init(backing)` … `ZigAlloc.ArenaDeinit(&arena)` (frees the chunk chain; pairs with `defer`) |
| `const a = fba.allocator();` (a **provable** FBA local) | **DEVIRTUALIZED** (Milestone U): no decl; `a.alloc/alignedAlloc/free/create/destroy/realloc/resize/remap` → direct `ZigAlloc.*Fba<T>(&fba, …)` (no vtable) |
| `fba.allocator()` / `arena.allocator()` (passed, not bound) | `ZigAlloc.FbaAllocator(&fba)` / `ArenaToAllocator(&arena)` → a runtime `Allocator` (opaque) |
| any method on an **opaque** `a` (a `std.mem.Allocator` param, an arena's allocator, a passed `fba.allocator()`) | **INDIRECT** `a.Alloc<T>` / `Free<T>` / `Create<T>` / `Destroy<T>` / `Realloc<T>` / `Resize<T>` / `Remap<T>` — the genuine `vtable->…(ctx, …)` dispatch (`Realloc` emulated via alloc+copy+free; `Resize`/`Remap` call the vtable's own `resize`/`remap` fn) |
| `a.alignedAlloc(T, alignment, n)` | lowered like `alloc` (devirt or indirect per the receiver); the comptime alignment arg is dropped — dotcc's alignment model caps at 16, honored up to natural (see **Alignment fidelity** below) |
| the default passed to an opaque `std.mem.Allocator` sink | materialized `ZigAlloc.CHeap()` (a runtime fat pointer; its vtable still reaches the C heap) |
| `std.mem.Allocator{ .ptr = &state, .vtable = &vt }` + a `std.mem.Allocator.VTable{…}` literal (Milestone W, part 1b) | a runtime `Allocator` over the user's 4-fn vtable (`alloc`/`resize`/`remap`/`free` bound as `delegate*` fields, each carrying `std.mem.Alignment` + `[]u8` + `ret_addr`); methods dispatch **INDIRECT** through it |

So the C-heap default AND a provable `fba.allocator()` site stay direct calls; only a genuinely
runtime-selected allocator pays the indirect dispatch. Examples: `examples/zig-alloc`,
`examples/zig-create`, `examples/zig-arena`, `examples/zig-realloc`,
`examples/zig-resize-remap` (in-place resize/remap on a provable FixedBufferAllocator),
`examples/zig-opaque-allocator` (the same resize/remap through an opaque `std.mem.Allocator` param — indirect vtable dispatch),
`examples/zig-custom-allocator` (a hand-written bump allocator behind a user `std.mem.Allocator`).

**V1 limits** (documented, not silent): two allocator sites are *provable* — the C-heap default
(`page_allocator`/`c_allocator`) and a bound `const a = fba.allocator();` over a known
`FixedBufferAllocator` local (Milestone U); every `std.mem.Allocator` parameter, an arena's
`allocator()`, and a cross-function FBA stay opaque (indirect). `resize` (bool, in-place) and
`remap` (`?[]T`) are wired on **both live forks**: a **provable FixedBufferAllocator** devirtualizes
to real zig's deterministic last-allocation logic (`ZigAlloc.FbaResize`: grow/shrink the top block,
shrink-a-non-last is a no-op success, grow-a-non-last fails; `remap` is resize +
return-same-pointer-or-null), and an **opaque** `std.mem.Allocator` dispatches through the vtable's
own `resize`/`remap` fn (`Allocator.Resize<T>`/`Remap<T>`) — the answer is whatever the concrete
runtime allocator gives (an FBA behind the opaque handle is deterministic; dotcc's C heap / arena
report no-in-place, so the caller copies). Only the **statically-known C-heap default** (a *devirt*
site, `page_allocator`/`c_allocator`) stays a clear deferred error: its in-place result is
allocator-page-dependent (real zig answers from `malloc_usable_size` / page rounding), so a
*devirtualized* guess would diverge from the oracle — use `realloc` there, or route through an
opaque allocator / an FBA. `arena.reset(mode)` and a non-allocator backing are deferred.

**Alignment + free fidelity (fixed 2026-07-17, from the runtime audit):** the `std.mem.Alignment`
parameter threaded through the vtable is now **honored** by `FixedBufferAllocator` — `FbaAlloc` aligns
its bump pointer up exactly like real zig's `alignPointerOffset` (allocate 1 byte then a `u64` → the
`u64` lands at the next 8-aligned offset), and the devirtualized `AllocFba`/`CreateFba`/`ReallocFba`
sites feed the real per-type alignment. `ArenaAlloc` (16-aligned data start + 16-rounded bumps) and
`CHeapAlloc` (`malloc`, ≥16-aligned) satisfy every request *by construction* because dotcc's
`AlignOf<T>` caps alignment at 16 — an `align(32+)` request is still over-modeled rather than exact
(one theoretical cut, unreachable from dotcc's type-derived alignment). And FBA's `free` now
**reclaims the most-recent allocation** (real zig's `isLastAllocation` — the freed region ends at the
bump cursor ⇒ rewind), so `alloc; free; alloc` reuses the space; freeing an earlier region stays a
correct no-op. `std` is a known-paths resolver, not a real std model — anything outside the
allocator paths above errors clearly. The C **heap** IS shared with the C front-end (Milestone V):
`std.heap.c_allocator` and C `malloc`/`free`/`realloc` are the same heap, so in a mixed `.c` + `.zig`
program memory allocated by one side is read / freed / resized by the other (see **C↔Zig shared-heap
interop** below). A **user-constructed custom allocator** also works (Milestone W, part 1b): the
runtime `Allocator.VTable` is now the real-zig 4-fn `{ alloc, resize, remap, free }` shape — each
fn carrying `std.mem.Alignment` + `[]u8` + `ret_addr` — so a hand-written
`std.mem.Allocator{ .ptr = &state, .vtable = &my_vtable }` with its own `std.mem.Allocator.VTable{…}`
literal lowers to a runtime `Allocator` over the user's `delegate*` functions and dispatches
indirectly, matching real zig. A **C `lua_Alloc` behind a Zig allocator** works too (Milestone W,
part 2 — the deep bridge): import a C realloc-style allocator via `extern fn`, hand-write a
custom-vtable adapter whose `alloc`/`free` call the imported C fn-pointer, and it dispatches across
the mixed `.c` + `.zig` seam (the C function binds by bare name, Milestone V) — matching real zig,
which needs the same explicit adapter (it has no auto C-fn-ptr → `std.mem.Allocator` coercion).
A user-constructed custom allocator stays **opaque** (indirect dispatch) by design: *devirtualizing*
a hand-written vtable that happens to bottom out in `malloc`/`free` is a **documented non-goal** —
the genuine common case (an allocator that IS the C heap) is already direct via `c_allocator`, so the
remaining case is one a program wouldn't hand-write, and proving it would need fragile
interprocedural body analysis for no observable gain. The **reverse** direction — a C function
consuming a Zig `std.mem.Allocator` fat pointer by value (C must know the `{ptr, *vtable}` layout +
4-fn ABI) — also stays cut; the safe direction (Zig allocates via `c_allocator`, C reads/frees)
already works (Milestone V). (`defer a.free(buf)` / `defer arena.deinit()` — the idiomatic
every-path release — work; see the **Defer** section.)

## Tuples — runtime tuples → C# `ValueTuple`

A Zig tuple is an anonymous **positional** struct — `.{ a, b }`, type `struct { T1, T2 }`, accessed
`t[0]`/`t[1]`. dotcc lowers the **runtime** subset directly onto C# `System.ValueTuple<…>`
(Milestone G): the same value semantics, positional access, comptime-known fixed arity, and native
deconstruction. The headline use is multiple return values.

| Form | Lowers to |
|---|---|
| tuple TYPE `struct { T1, T2, … }` (return / param / var) | `System.ValueTuple<T1, …>` (any arity: empty → non-generic `ValueTuple`, > 7 nests via `TRest`) |
| positional literal `.{ a, b }` | `new System.ValueTuple<…>(a, b)` — element types from the tuple sink, or inferred from the elements |
| `t[N]` (literal `N`) | `.ItemN+1` (ValueTuple's 1-based fields) |
| `const a, const b = e;` | a single-eval temp + per-binder `.ItemN` reads (a brace-less `Seq`, so the binders land in the enclosing scope) |

So a function returns `struct { u8, u8 }`, the caller writes `const lo, const hi = minmax(…);`, and
both sides are plain `ValueTuple` — no custom runtime. Example: `examples/zig-tuple/main.zig`.

**Why `ValueTuple` and not a `Span`-style type:** a `ValueTuple` is a value type (copied on
assignment), positional, fixed-arity, deconstructs natively, and is `unmanaged` when its elements
are (so it can be a struct field / cross the ABI) — the same property that justified `Slice<T>`. The
fit is exact for the runtime subset; only the comptime *flavor* of tuples (type-valued /
`comptime_int` fields, and the `std.fmt` `.{…}` reflection idiom — already handled via
`extern fn printf`) stays out, the comptime root again.

The full destructure surface is in (Milestone S): assign-to-existing lvalues (`a, b = e;`), mixed
new+existing, typed binders (`const a: T, …`), and the `_` discard. A tuple-literal RHS lowers
element-wise in source order (faithful to Zig's sequential, non-snapshotting semantics — so a swap
needs `a, b = .{ b, a }` to *not* swap, which it doesn't); a non-literal tuple RHS single-evals.

**V1 limits** (documented, not silent): arity 1..7 (an empty tuple and arity > 7 — which would need
ValueTuple's `TRest` nesting — are deferred); destructuring a non-tuple aggregate and nested
destructure are deferred; a literal that mixes positional + named fields is rejected; and a runtime
(non-literal) tuple index is rejected.

## Defer / errdefer — scope-exit cleanup → C# try/finally + try/catch

`defer Stmt;` registers a cleanup that runs when control leaves the enclosing block — on EVERY
exit (fall-through, `return`, `break`, `continue`, or a propagating error), in LIFO declaration
order. `errdefer Stmt;` is the same but fires only when the block exits via a **propagating
error**. The two share one LIFO cleanup stack (a later-declared `errdefer` runs before an
earlier `defer`). The headline use is pairing an allocation with its release:
`const buf = try a.alloc(u8, n); defer a.free(buf);`.

dotcc lowers a block's defers by **restructuring**: each `defer`/`errdefer` wraps the statements
that follow it within its block, nested in lexical order — so the nesting itself yields the LIFO
order, the same shape as the C front-end's `setjmp` try-guard.

| Form | Lowers to |
|---|---|
| `defer cleanup;` | `try { rest-of-block } finally { cleanup }` (C#'s finally fires on every exit) |
| `errdefer cleanup;` | `try { rest-of-block } catch (ZigErrorReturn) { cleanup; throw; }` (the rethrow keeps the error propagating to the `!T` boundary) |
| `return error.X;` in a fn that has an `errdefer` | `throw new ZigErrorReturn(code);` (NOT a direct `Err` return — see below) |

**The `errdefer` ⇄ `return error.X` seam.** An `errdefer` is a C# `catch`, which only fires on a
THROWN error. But `return error.X;` normally lowers to a *direct* `ErrUnion<T>.Err(code)` return
(Milestone B2), which a catch can't observe. So when the enclosing function contains an `errdefer`,
its error returns are instead routed through a thrown `ZigErrorReturn` — propagating through the
errdefer catch(es) on the stack, with the existing `!T` boundary catch still converting it back to
an `Err`. This unifies both error-exit paths (`try`-propagation and explicit `return error.X`) to one
mechanism. A function with **no** `errdefer` keeps B2's elegant, exception-free direct `Err` return
untouched. (`defer` needs no such rewrite — a C# `finally` fires on a direct return too.) Example:
`examples/zig-defer/main.zig`.

**V1 limits** (documented, not silent): `errdefer |e| …` payload capture is deferred (the grammar's
`errdefer Stmt` has no `|e|`); a control-flow statement inside a defer (`defer return;` /
`break` / `continue` — which Zig itself rejects) would emit an illegal C# `finally { return; }`
(CS0157) rather than a faithful loud reject (a later polish).

## Strictness — dotcc tracks Zig where it matters

dotcc lowers Zig onto the same C-shaped IR + C# backend as C, which is C-lenient by
default (it silently inserts narrowing casts, applies C's default argument promotions,
etc.). On top of that core the Zig front-end layers the few **Zig-specific strictness
rules** the differential oracle proves we need — dotcc should reject what real `zig`
rejects, not silently accept more.

- **Variadic literals must be cast.** A bare integer/float literal is a `comptime_int` /
  `comptime_float` (no fixed-size ABI type), so Zig forbids passing it to a C-variadic.
  `printf("%d", 42)` is an error in both zig and dotcc; pass `@as(c_int, 42)`, a typed
  value, or any expression with a concrete-typed leaf (`x`, `f()`, `a + x`). dotcc emits
  zig's exact message through the same diagnostics channel C uses for constraint
  violations. (This one was found by the CI oracle catching dotcc being too lenient.)

## Validation

- **Always-on emit tests** — `DotCC.Tests/ZigFrontendTests.cs` pins dotcc's
  lowered C# for the supported surface (parameters, signedness, if/while, the
  if-expression, calls + forward refs, `extern fn` libc calls, variadic
  `printf`).
- **Differential oracle (opt-in)** — `DotCC.FunctionalTests/ZigOracleTests.cs`
  compiles + runs each program through dotcc **and** the real `zig` compiler and
  asserts they agree on exit code **and** stdout (`DOTCC_RUN_ZIG_ORACLE=1`; skips
  when no `zig` is on PATH). CI runs it on linux-x64 + windows-x64 against a
  pinned `zig 0.16.0`.
- **Examples** — `examples/zig-hello`, `examples/zig-extern` (putchar),
  `examples/zig-printf` (variadic printf), `examples/zig-optional` (`?T` / `null` /
  `.?` / `orelse`), `examples/zig-errunion` (`!T` / `try` / `catch` / `error.X`),
  `examples/zig-controlflow` (while-cont / `break` / `continue` / `switch` / range-`for`),
  `examples/zig-struct` (`struct` + `.{…}` + field access, `enum` + `.member` + `@intFromEnum` + enum `switch`),
  `examples/zig-struct-typed` (typed `T{…}` literal in value + sink-less positions),
  `examples/zig-methods` (struct methods + UFCS: static `init`, pointer-receiver `scale`, `@This()` value receiver),
  `examples/zig-union` (tagged `union(enum)`: payload + void variants, `switch` with `\|x\|` capture),
  `examples/zig-union-tagged` (explicit-tag `union(Kind)`: an existing named enum as the discriminant,
  with non-zero/out-of-order tag values driving the dispatch),
  `examples/zig-union-multi-capture` (MULTI-variant capture prong `.circle, .square => |r|`, Milestone
  Z: same-payload-type variants bind one `|r|` via the first variant's aliasing field),
  `examples/zig-struct-layout` (struct layout modifiers: `extern struct` → C-ABI `[StructLayout(Sequential)]`
  vs `packed struct` → byte-packed `[StructLayout(Sequential, Pack=1)]`, with matching `@sizeOf` + field access),
  `examples/zig-union-untagged` (untagged `union { … }`: a bare overlapping-storage overlay struct, each
  value kept to a single active field — write-then-read the same field, no type-punning),
  `examples/zig-export-extern` (FFI declaration surface: `extern "c" fn printf` + `export fn` /
  `pub export fn` callable locally),
  `examples/zig-decl-modifiers` (declaration modifiers, all no-ops on the managed target: `callconv(.c)`
  on a function, `align(8)` on a local, `linksection(".mydata")` on a global),
  `examples/zig-container-var` (container-level `var` namespaced mutable global `Cfg.counter` +
  a sibling const referenced by bare name `const doubled = base * 2`),
  `examples/zig-slices` (`[]const u8` slices: `.len`/`.ptr`, index, `s[lo..hi]`, `for (s) \|b\|`),
  `examples/zig-open-slice` (open-ended slicing `s[lo..]`: the high bound is the source length — a
  slice's `.len`, an array's element count; alongside a closed re-slice),
  `examples/zig-many-ptr` (many-item pointers `[*]T`: index `p[i]`, closed-slice `p[lo..hi]`, bind a
  slice's `.ptr`),
  `examples/zig-sentinel` (sentinel-terminated `[*:0]T` / `[:0]T`: a `[:0]const u8` string-literal
  slice + a `[*:0]const u8` C-string pointer scanned to the NUL),
  `examples/zig-sentinel-array` (sentinel arrays `[N:0]T`: a `[5:0]u8` literal reserving the N+1th
  slot for the sentinel, summed over its logical length, with `buf[N]` reading the sentinel back),
  `examples/zig-sentinel-nonzero` (NON-ZERO sentinel arrays `[N:s]T`, Milestone Z: `[3:9]i32` /
  `[2:5]i32` literals materializing the sentinel in the trailing slot, read back at index len),
  `examples/zig-for-idx-byref` (`for (s, 0..) |*e, i|`, Milestone Z: by-reference element capture
  WITH the usize index — `e.* = …` writes through to the slice),
  `examples/zig-stack-slice` (the non-escaping stack-slice peephole: a constant-size, freed,
  non-escaping `page_allocator` byte slice demoted to a `stackalloc` — heap alloc/free elided),
  `examples/zig-alloc` (allocators: devirt'd `page_allocator`, a `FixedBufferAllocator` via the
  indirect vtable, an opaque `std.mem.Allocator` param + materialized default),
  `examples/zig-tuple` (tuples: a `struct { u8, u8 }` multiple-return + `const lo, const hi = …`
  destructure, a tuple-typed parameter, an inline-literal destructure, a literal `t[N]` index),
  `examples/zig-destructure` (destructure completeness: assign-to-existing `a, b = .{ b, a }` with
  Zig's sequential no-swap semantics, a 3-way rotate, mixed new+existing, typed binders, the `_`
  discard, and a non-literal `pair()` RHS single-eval),
  `examples/zig-defer` (defer/errdefer: `defer a.free(buf)` pairing a FixedBufferAllocator
  allocation with its release, plus an `errdefer` step that fires on the error path),
  `examples/zig-literals` (bool + char literals: `true`/`false` driving a branch, char-codepoint
  arithmetic for an ASCII case fold, the common escapes),
  `examples/zig-compound-assign` (compound assignment: `i += 1` as the `++` replacement, a
  `+=`/`-=` chain, single-eval on the target lvalue),
  `examples/zig-globals` (top-level globals: a typed `const`, a const initialized from an earlier
  const, and a mutable `var` accumulator bumped by a function through its bare name),
  `examples/zig-lexer` (lexer & literals: `0x`/`0o`/`0b` radix + `_` separators, a hex float, an
  escaped quote + `\u{…}` unicode escape, and a `\\` multiline string),
  `examples/zig-builtins` (result-location cast builtins: `@intCast`/`@truncate`/`@floatFromInt`/
  `@floatCast`/`@intFromFloat`/`@bitCast`/`@ptrCast`+`@alignCast`/`@enumFromInt` + `@sizeOf`,
  each inferring its target from the binding it flows into),
  `examples/zig-typeinfo` (comptime reflection: `switch (@typeInfo(T))` dispatching on the kind
  — folded away entirely, each call emitting its constant — a `|i|` payload capture with
  `signedness`, `child` in a type position, `len`, `bits` read off the source spelling so
  `@typeInfo(u21)` answers 21 rather than the 32 dotcc lowers `u21` to (through a `comptime T: type`
  param too, where `bitsOf(u21)` and `bitsOf(u32)` are distinct specializations), the parallel member
  lists `field_names`/`field_types`/`field_values` with `.len` + a comptime index, a spelled enum
  `tag_type`, and `@hasField`/`@hasDecl`/`@field`),
  `examples/zig-inline-for-lists` (`inline for` over a comptime list — the loop is unrolled at lowering
  time, so a `field_types` capture can bind a TYPE and a `field_names` capture a comptime STRING
  that `@field(v, name)` reads: one generic `sumFields(T, v)` reaches every field of a struct it
  has never seen, and the emitted C# holds nothing but the field accesses. Shows all four operand
  shapes — a single list, two lists in parallel, a list with its indices, and a `[_]type{…}`
  literal),
  `examples/zig-target-builtin` (the synthetic `builtin` module: `@import("builtin")` is generated,
  not a file, and every read of it is settled at COMPILE time — look at the emitted C# and there is
  no `if` left in `main` at all. Shows the two shapes a platform conditional is written in, an `if`
  chain and a `switch` over `os.tag`, plus the nested `target.abi` chain),
  `examples/zig-reify` (the reification builtins: `@Int(.unsigned, 21)` builds a type no keyword
  spells, `@bitSizeOf` reports the DECLARED width rather than the storage width dotcc widened it to,
  and the two round-trip — take a type apart with `@typeInfo`/`@bitSizeOf` and put it back with
  `@Int`. Every `@compileError` in the file sits in a branch comptime folding removes — a `switch`
  `else` prong, a comptime-known `if`, and a deprecation tombstone nothing names — which is why it
  compiles at all, in dotcc and in real zig alike),
  `examples/zig-arrays` (array literals & aggregate globals: `.{…}` at a `[N]T` sink, typed
  `[N]T{…}` / inferred `[_]T{…}` locals, plus literal-array / inferred-array / `undefined`-array
  and struct globals routed through the pinned global store),
  `examples/zig-switch-expr` (switch as an expression: a return-position enum switch with
  `.member` labels + `else`, and a typed-decl int switch with a multi-value prong → C#'s native
  switch expression),
  `examples/zig-labeled-block` (labeled block as a value: typed-decl / inferred / return /
  assignment `blk: { …; break :blk v; }`, including a conditional `break :blk` from inside an `if`),
  `examples/zig-value-switch` (value-position `if`/`switch` with BLOCK-BODIED branches, Milestone Y
  part 1: a return-position switch mixing a block-bodied prong, a multi-value prong, and a
  block-bodied `else`, plus a value-position `if` with a labeled value-block then-branch → a
  STATEMENT switch/if filling a result temp),
  `examples/zig-value-loop` (value-position LOOPS `while/for (…) … else …`, Milestone Y part 2: an
  unlabeled `while … else` with `break v`, a labeled `outer: while … else` yielding from a nested
  loop via `break :outer v`, and a `for (slice) |x| … else …` search loop → a STATEMENT filling a
  result temp `__lv`),
  `examples/zig-labeled-loop` (labeled loops: `break :outer` and `continue :scan` from nested loops
  → `goto` to a break label after the loop / a continue label at the body's end),
  `examples/zig-switch-range` (switch ranges: a char-classifier switch EXPRESSION `'0'...'9' => …`
  and a statement switch with `lo...hi` ranges + a multi-value prong → C# relational patterns),
  `examples/zig-if-capture` (optional payload capture in `if`: value optional then/else/`_`/no-else
  + a niche optional-pointer capture written through),
  `examples/zig-while-capture` (optional capture-`while`: a value-optional iterator-style loop
  `while (nextLT(&i, 9)) |v|` + a `_` discard capture-while),
  `examples/zig-while-completion` (capture-while completions: a while-`else` clause, a `: (cont)`
  continue-expression, an error-union capture-while `else |e|`, and a non-zero for-slice index `5..`),
  `examples/zig-error-capture` (error-union capture in `if`: payload `|x|` on success + the error
  branch on failure via `else |_|`),
  `examples/zig-byref-capture` (by-reference capture `|*x|`: doubling a slice in place via
  `for (s) |*e|` + mutating a tagged-union payload via `switch (b) { .i => |*p| … }`),
  `examples/zig-error-value` (error values as comparable values: a USED `else |e|` capture compared
  against `error.Bad`, plus a `const`-bound bare error value tested with `==`/`!=`),
  `examples/zig-error-switch` (`switch` on an error value: a captured `else |e|` switched over
  `error.Zero` / `error.Negative` / `else` → an integer switch on the flat code),
  `examples/zig-catch-capture` (`catch |e|` capture using `e == error.Bad` for a bool fallback, plus
  a lazy side-effecting `catch dflt()` whose call runs only on the error path),
  `examples/zig-errunion-main` (error-union `main`: `pub fn main() !u8` with `try` inside, the payload
  as the process exit code; an error would propagate to the entry and exit 1),
  `examples/zig-error-set` (a named `const MathError = error{ Overflow, Negative };` used as a
  `MathError!i32` return type — the erased set, members returned via `error.X` and handled with `catch`),
  `examples/zig-catch-orelse-return` (control-flow fallbacks: `mk(a) catch return error.NoX` (error
  union early-return) + `pick(b) orelse return 0` (optional early-return) inside a `!i32` function),
  `examples/zig-anf-subexpr` (catch/orelse in SUB-expression positions — the ANF statement-hoist: a
  side-effecting `catch` and an `orelse return` used inside `x + (…)`, lifted to a temp before the
  enclosing statement, eval-order-preserving),
  `examples/zig-wrap-ops` (wrapping arithmetic: `+%=`/`-%=`/`*%=` overflow on `u8`, a `z -% 2`
  underflow, and a `u8 +% u8` that wraps at the operand width before widening to `u32`),
  `examples/zig-overflow-ops` (overflow-detecting `@addWithOverflow`/`@subWithOverflow`/`@mulWithOverflow`/
  `@shlWithOverflow` → `struct { T, u1 }`, destructured + indexed, incl. the `std.ArrayList` `addOrOom` shape),
  `examples/zig-sat-ops` (saturating arithmetic: unsigned `+|=`/`-|=`/`*|=` clamp to 255 / floor at
  0, signed `+|=`/`-|=` clamp to 127 / -128, and a `u8 +| u8` clamped at the operand width),
  `examples/zig-fn-ptr` (function-pointer types + `anyopaque`: two ops sharing
  `fn (ctx: *anyopaque, by: i32) i32` bound to `*const fn (…) i32` values and called indirectly,
  each treating its opaque ctx as a `*i32` accumulator via `@ptrCast(@alignCast(ctx))`),
  `examples/zig-custom-allocator` (a user-constructed `std.mem.Allocator`: a hand-written bump
  allocator whose `Bump` state lives behind the opaque `ctx`, bound to a real 4-fn
  `std.mem.Allocator.VTable{…}`, used through the standard `a.alloc` / `a.free` surface),
  `examples/zig-lua-alloc` (a C `lua_Alloc` behind a Zig allocator — Milestone W, part 2: a C
  realloc-style allocator `extern fn`-imported and wrapped in a custom-vtable adapter whose
  `alloc`/`free` call the C fn-pointer across the mixed `.c` + `.zig` seam),
  `examples/zig-error-name` (`@errorName` — Milestone X, part 1: `@errorName(error.Ok)` returns the
  real name "Ok" via the emitted code→name table; exit content-sensitive on a name byte + length),
  `examples/zig-error-member` (`E.member` — Milestone X, part 2: a set-qualified `MyError.Boom`
  resolves to the same flat code as bare `error.Boom`, as a compared value and a `return`),
  `examples/zig-error-set-type` (an error set as a plain VALUE type — Milestone X, part 3b:
  `fn worst() MathError` returns the error value, `fn weight(e: MathError)` takes the set as a param,
  and an exhaustive `switch` EXPRESSION over the members with NO `else` — dotcc injects the `_` default),
  `examples/zig-std-mem` (curated `std.mem` helpers + mem builtins: `std.mem.eql` equality,
  `std.mem.copyForwards`, `std.mem.span` (C-string → slice), `std.mem.zeroes` (scalar + struct),
  `@memset`/`@memcpy`, plus the `&array` → `[]const u8` slice coercion).
