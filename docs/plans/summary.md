# Plans — status index

One row per campaign plan in this directory: where it actually stands, and what is left.
`/plan-status` renders this as a sorted table; **this file is the thing that has to be
kept true** (the skill only reformats — it never re-checks a claim against the code).
Update a row in the same commit that changes a plan's real state.

Not plans, so not rows here: [`deferred.md`](deferred.md) (the cross-cutting ledger of
deliberate cuts still on the books — parse-only lowering gaps, deferred grammar,
runtime-fidelity divergences) and the two committed probe reports
([`std-parse-probe.report.txt`](std-parse-probe.report.txt),
[`wasm-surface-probe.report.txt`](wasm-surface-probe.report.txt)), which are regenerated
data rather than plans. Permanent out-of-scope lives in the SUPPORT docs.

Last verified against `main` on **2026-09-23** (`d7f00f4`, plus the statement-shaped `catch`/`orelse` fallbacks on `feat/zig-catch-switch`).

| Plan | Status |
|---|---|
| [fable-wasm](fable-wasm.md) | **NOT STARTED ~10%** — binary-`.wasm` front-end (`.wasm` → IR → C#), end-goal consuming Embedded Swift. WF0 (the surface probe) is ✅ done and its findings are committed; nothing is built. **Remaining:** WF1 the strict binary reader (next, self-contained), then WF2 the lifter, WF3–WF8. |
| [road-to-zig-std](road-to-zig-std.md) | **PARTIAL ~48%** — compiling real upstream zig std from source. ✅ S0 probe, S1 module graph, S2 lazy decl-driven lowering, S4a/b/c comptime-value + captured-`if` folding, S4d type-position module navigation, S5a `@typeInfo` + the comptime `switch` over it (scalar kinds), S5b a zig integer's declared width riding its type binding (so `bits` answers through a `comptime T: type` param, and `u21`/`u32` key distinct instances), S5c the member lists (`field_names`/`field_types`/`field_values`/`tag_type`) + `@hasField`/`@hasDecl`/`@field`, S6 `inline for` over a comptime list (single, parallel, indexed, and `[_]type{…}` literal — the capture binds comptime, so one generic body reaches every field of a struct it has never seen), S7 the reification builtins `@Int`/`@bitSizeOf` + the compile-time diagnostics (`@compileError` fires only where comptime folding leaves it reachable — which is zig's own rule — with a poisoned-NAME tombstone for the one shape that needs one), S3 the synthetic `builtin`/`root` modules + the comptime struct constants they need (a platform conditional now FOLDS, so only the taken arm lowers — without which std's per-arch asm and syscalls would all be lowered), several S9 parse bricks (probe at 32.0% of 553 files), G1 (`std.ascii` from real source, oracle-identical), G4's tentpole (a reified generic carries methods), the W4 lift (a type-returning body is EVALUATED at comptime — a delegating `return Aligned(T, null);`, a folded `switch`/`if`, value consts — so `std.meta.Child`/`Elem` and `std.math.Log2Int` compile from real source, oracle-identical; G4 blocker 2), and nested containers as full containers (any kind, methods, sibling-field + qualified resolution — `std.fmt.Number`'s shape); and the target-identity segment T1 to T5 (a typed `builtin.cpu: std.Target.Cpu` filled from .NET's intrinsics, `std.simd.suggestVectorLength` evaluated over it, `@Vector` as .NET's `Vector128/256<T>`), so `std.array_list` and `std.mem.indexOfScalar`'s SIMD path run from source, == zig (T6, arm64 hosts, is backlog);
and G3's goal, **`std.fmt.bufPrint` runs from real std, == zig** (the comptime format engine plus std's integer
printing), as do **`std.mem.sort`** (block and insertion sort, custom contexts) and **`std.AutoHashMap`** (growth,
removal, iteration). **Remaining:** S4 interpreter `TypeVal`, S8 the platform-floor redirect table, the ranked S9 bricks, G2/G3/G5 (G3's `bufPrint` — which PARSES since the statement-shaped `catch`/`orelse` fallbacks (~1,000 std sites, probe 32.0% → 33.8%), and now gets past its cross-module generic call (comptime/`anytype`/comptime-STRING args read in the caller) with decl literals (`.fixed(buf)`) and module-qualified naming in place, and past the `std.Io.Writer` TYPE since a file with top-level fields is a struct (module-path aliases, qualified function names and analyse-on-reference for a lazy module's containers landed with it); plus re-exported declarations and per-member curated std namespaces (opening `std.AutoHashMap` / `std.mem.indexOfScalar` from source), a skipped-declaration diagnostic, and the std syntax behind them (`comptime if`, anon enums, `{}` + erased void, `align(E)` / general sentinels / trailing commas / `inline fn` so std.ArrayList parses, member `comptime {}`, block continues) plus reified TYPE const members; then (PR #125) the comptime-call engine (`std.math.maxInt` from source), opaque pointers to containers that cannot lower, fn-pointer field calls, generic file-as-struct methods, and `Writer.print`'s syntax (jump prongs, `comptime switch`, static `&.{…}`), with a silent break-in-switch miscompile fixed on the way; its measured next wall: `@typeInfo` over a TUPLE in `print`, then the comptime format engine proper), and G4's blockers 3/5. |
| [fable-web](fable-web.md) | **DONE ~95%** — the Blazor-WASM in-browser sandbox, live at sebgod.github.io/dotcc (landing + sandbox with the C/Zig toggle + coverage + story pages), AOT-published by CI. WEB0–4, WEB6, WEB7 all ✅. **Remaining:** WEB5 stretch flexes only, none scheduled — runtime-less NativeAOT-LLVM `dotcc.wasm` (must be an x64 CI spike), offline PWA, a compiler-explorer diff view, direct binary `.wasm` emit. See [`fable-web.findings.md`](fable-web.findings.md). |
| [fable-wall](fable-wall.md) | **DONE** — the generative Zig core (comptime types / generics / `anytype`), W0–W6, the whole planned arc merged; monomorphization over retained ASTs, no Sema. Non-arc follow-on work moved to road-to-zig-std. |
| [fable-c](fable-c.md) | **DONE** — the C follow-up batch (`unreachable()`, `int_least`/`fast`, `[[nodiscard]]`, `-Wimplicit-fallthrough`) all shipped; the plan is exhausted. C89/C99 complete and the C11/C23 roadmap column is empty — what is left is nice-to-have (`_BitInt(N)`, decimal FP, the multibyte↔wide conversions) or deliberately out of scope. |
| [fable-zig](fable-zig.md) | **DONE** — the Zig quick-wins inventory; every implementable item shipped and the formal milestone roadmap (I→T, ß, U/V/W/X/Y/Z) is exhausted. Its "the wall" section was relitigated by fable-wall and is now history. |
| [import-mode](import-mode.md) | **DONE** — clang-shaped `-l`/`-L` native import mode: dynamic GOT-style fn-ptr binding over `NativeLibrary`, static `.a`/`.lib` via `DllImport` + `DirectPInvoke` (NativeAOT publish only). Its honest limits (calls-only, no variadic/extern-data imports, no link-order interposition) are documented in the plan, not deferred work. |
