MSVC's C frontend has no /std:c23, so it keeps <stdbool.h>'s bool/true/false
macros (prints "bool-macro") rather than the C23 keyword behavior this fixture
asserts. The gcc oracle (-std=c2x) covers it instead.
