cl.exe compiles this under /std:clatest but keeps <stdbool.h>'s macro
definition of bool (prints "bool-macro") rather than the C23 first-class
keyword behavior this fixture asserts ("bool-keyword") — verified on MSVC
toolset 14.51, VS 2026. The gcc oracle (-std=c2x) covers it instead.
