cl.exe does not implement the bare C23 keyword constants (`bool`/`true`/
`false`/`nullptr`) even under /std:clatest — C2065 "'bool': undeclared
identifier" (verified on MSVC toolset 14.51, VS 2026). The gcc oracle
(-std=c2x) covers this fixture instead.
